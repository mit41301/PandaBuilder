





                             _________________________
                            /                         \
                           /   H  H  AA   SSS  M   M   \
                          /    H  H A  A S     MM MM    \
                         /     HHHH AAAA  SSS  M M M     \
                         \     H  H A  A     S M   M     /
                          \    H  H A  A  SSS  M   M    /
                           \___________________________/
                            \_________________________/


                               ______________________
                              /                      \
                             /   H  H  SSS  I M   M   \
                            /    H  H S     I MM MM    \
                           /     HHHH  SSS  I M M M     \
                           \     H  H     S I M   M     /
                            \    H  H  SSS  I M   M    /
                             \________________________/
                              \______________________/






                                Anleitung / Referenz
                      Tabellenkonfigurierbarer Makro-Assembler
                            mit integriertem Linker und
                           Zugeh”riger Simulator/Debugger

                               (c) Dr.-Ing. H.P. Hohe
                                91332 Heiligenstadt
                                    Burggrub 28

                                    August, 1998




                                  Sharewareversion
                                  ----------------
                     Private Nutzung und Einsatz in der Lehre,
                  sowie Nutzung zu Testzwecken beliebig gestattet.

                                    Vollversion
                                    -----------
                  Kommerzielle Nutzung nur mit einer k„uflich bei

                                     PohlTronik
                                Dipl. Ing. Sven Pohl
                                   Schlehenweg 6
                                 31812 Bad Pyrmont

                                erworbenen Version.





                                     - Seite 1 -




                                  Inhaltsverzeichnis


         I.   šberblick / Leistungsmerkmale .......................3

         II.  Einrichten auf der Festplatte .......................5

         III. HASM - Assembler ....................................8
              A. Einleitung .......................................8
              B. HASM Schnell-šberblick ...........................8
              C. Datenstrukturen .................................10
              D. Kommandozeile ...................................12
              E. Filenamen .......................................15
              F. Preprozessordirektiven ..........................16
              G. Assembler-Direktiven ............................18
              H. Erstellen eigener Assembler-Tabellen ............25
              I. HASM Eigenheiten fr einzelne Prozessoren .......34
              J. Ausgabeformate ..................................34
              K. Historie des Programms ..........................35

         IV.  HSIM Simulator/Monitor .............................37
              A. Einleitung ......................................37
              B. Kommandozeile ...................................37
              C. Oberfl„che des Simulators/Monitors ..............38
              D. Stimulus-File ...................................41
              E. Terminal-Fenster ................................42
              F. Status-Editor ...................................44
              G. Synchrones Simulieren mehrerer Prozessoren ......45
              H. Connection-Editor ...............................46
              I. Scriptfiles .....................................48
              J. Lokale Erweiterungen der Simulator-/Monitortabelle51
              K. Syntax von Ausdrcken im Simulator/Monitor ......52
              L. Simulatortabelle erstellen ......................54
                 1.  Speichersegmente anlegen ....................54
                 2.  Abbilden der Segmentarten des Assemblers ....56
                 3.  Anweisungen zur Initialisierung .............57
                 4.  Befehlssatz des Prozessors ..................57
                 5.  Cyclische Anweisungen anlegen ...............63
                 6.  Inhalt des Register-Fensters ................64
                 7.  Interface zu anderen Modulen Vorbereiten ....65
                 8.  Einfacher Zugriff auf CPU Register ..........68
              M. Bausteintabelle erstellen .......................69
              N. Monitortabelle erstellen ........................70
                 1.  Serielle Schnittstelle vorbereiten ..........71
                 2.  Speicherbereiche und Register abbilden ......73
                 3.  Abbilden der Segmentarten des Assemblers ....74
                 4.  Steuerfunktionen fr den Programmlauf .......74
                 5.  Inhalt des 'Register'-Fensters festlegen ....76
              O. Beispielsitzung im Monitorbetrieb ...............76
              P. Historie des Programms ..........................77

         V.   Nachwort ...........................................78









                                     - Seite 2 -








     I.  šberblick / Leistungsmerkmale

       Crossassembler HASM mit zugeh”rigem Simulator/Monitor HSIM fr eine
       ganze Reihe von Mikrocontrollern (MCS51, 80HC11, PIC, ..). Sowohl der
       Assembler als auch der Simulator werden durch Tabellen an den
       jeweiligen Controller angepaát. Man kann vorhandene Tabellen nutzen
       oder eigene Tabellen erstellen. Als Betriebssystem kann DOS, eine DOS-
       Box unter Win 3.1, Win 95 oder Linux verwendet werden.

       HASM fr den Anwender:
        + Ein Assembler fr viele Prozessoren
        + Assemblerdirektiven fr alle Prozessoren gleich
        + Untersttzt alle wichtigen Preprozessor-Direktiven
          (Bed.Assemblierung)
        + Volle Makrountersttzung mit lokalen Labels (Nicht nur ber #DEFINE
          !)
        + Makro-Aufrufe mit Argumentmasken sind m”glich
        + Untersttzt voll verschiebbare, segmentierte Programmierung
        + Integrierter Linker kann automatisch oder separat verwendet werden
        + Volle Untersttzung von lokalen Labels
        + Fehlermeldungen mit Quellenangabe fr intelligenten Editor
        + Punkt-Vor-Strich bei Ausdrcken
        + Ausdrcke k”nnen Floatzahlen, Strings und Integerzahlen enthalten
        + Steuerung des Linkvorganges durch ORG-Anweisung die auch Formeln
          enthalten kann (Alignnachbildung !)
        + Spezielle Befehle zur Generierung ganzer Wertetabellen
        + Einbindung einzelner Module aus einer Library je nach Bedarf.
        + Prozessoren die noch nicht untersttzt werden k”nnen vom Anwender
          selbst integriert werden.
        + Zugeh”riger Source-Level-Debugger/Simulator (-> HSIM !)

       HASM fr neue Prozessoren:
        + Die Befehle eines Prozessors k”nnen auf Bit-Ebene definiert werden
        + Bedingungsabh„ngige Codel„nge fr Prozessorbefehle
        + Konstanten fr den jeweiligen Prozessor direkt in der Tabelle
          m”glich
        + Frei definierbare Operatoren fr den Assembler
        + Physikalische und virtuelle Segmente k”nnen beliebig definiert
          werden
        + Segment-Mapping ist bei allen Segmenten m”glich
        + Nachbildung eines anderen Assembler m”glich -> Quellenbernahme
          einfach

       HASM Grenzen:
        Maximall„nge fr Labels                          250 Zeichen
        Maximal ansprechbarer Adressraum                 -> 32-Bit Zeiger
        Maximale Anzahl geschachtelter Include           10
        Maximale Source-Line L„nge                       250 Zeichen
        Maximale Ausdruck-L„nge                          250 Zeichen
        Maximale L„nge fr Pfadangaben                   79 Zeichen
        Maximal Definierbare Maschinenbefehle            1000
        Maximalspeicher fr Befehle                      64 kB
        Maximale Anzahl an Makroargumenten               39
        Maximale L„nge fr Segmentgruppennamen           19 Zeichen
        Maximale Segmentzahl                             100


                                     - Seite 3 -




        Maximale Verschachtelungstiefe bei Makros        5
        Maximale L„nge von Zeichenkettenkonstanten       120 Bytes

       HSIM fr den Anwender:
        + Tabellenkonfigurierbarer Simulator fr verschiedene Prozessoren
        + Momentan drei Tabellen: 80C552, PIC16C57, 68HC11)
        + Tabellenkonfigurierbarer Monitor fr verschiedene Prozessoren
          (Momentan zwei Tabellen: Eigenbau 80C552-Platine
           und Phytec miniCON-196 )
        + Erm”glich das synchrone Simulieren mehrerer Prozessoren
        + Auch Hardwareverbindungen k”nnen Simuliert werden !!
        + Fensterorientierte Oberfl„che (allerdings noch ohne Maus !)
        + Identische Oberfl„che in beiden Betriebsarten (keine Umstellung)
        + Einfaches Source-Level-Debugging in beiden Betriebsarten
        + Setzen und l”schen von Breakpoints (bleiben erhalten beim Beenden)
        + Singlestep-Betrieb (Im Simulatorbetrieb auch Rckw„rts m”glich !!)
        + Watch-Ausdrcke werden im RUN-Modus st„ndig aktualisiert
        + Watch-Ausdrcke bleiben beim Verlassen des Simulators erhalten
        + Variablenfenster mit den aktuellen lokalen Variablen
        + Registerfenster mit den Prozessorregistern
        + Nachbildung von Hardwaresignalen durch Stimulusfile
        + Ver„ndern von Registern und Fenstern w„hrend der Programmausfhrung
        + Terminal-Fenster zur Entwicklung eines Talkers fr Monitorbetrieb !
        + schnelle Einbindung einer Tabelle ins EXE-File durch Autor m”glich
          (-> 5-10 fache Geschwindigkeit)
        + Hardwareverbindungen zwischen verschiedenen Prozessoren m”glich



































                                     - Seite 4 -








     II. Einrichten auf der Festplatte

       Das Einrichten von HASM/HSIM ist nicht schwierig. Sie gehen dazu am
       einfachsten wie folgt vor:

        1. Stellen Sie sich in das Unterverzeichnis in dem sich sp„ter das
          Unterverzeichnis HASM befinden soll !

        2. Starten sie das Auspacken von HASM mit dem Befehl
          "Pfad\Hasm??.exe", wobei fuer ?? die aktuelle Versionsnummer
          eingesetzt werden muss. Die Angabe des Pfades kann entfallen, wenn
          sich die Archivdatei (HASM??.EXE) im aktuellen Verzeichnis
          befindet.
          Bei der Archivdatei handelt es sich um ein selbstextrahierendes
          Archiv. Man ben”tigt also zur Installation keinerlei weitere
          Software.
          Nach der Sicherheitsabfrage, die zum Auspacken mit `x` beantwortet
          werden muss wird das Unterverzeichnis HASM mit allen darin
          befindlichen Files erzeugt. Haben Sie an dieser Stelle bitte etwas
          Gedult - das Auspacken ben”tigt etwas Zeit. Keine Angst - beim
          Auspacken werden weder Systemdateien (Autoexec.bat, Config.sys)
          modifiziert noch in irgendwelchen anderen Verzeichnissen Dateien
          angelegt oder veraendert. Sie k”nnen also die gesamte Installation
          durch l”schen des Unterverzeichnisbaumes 'HASM' wieder entfernen.

       Das Gespann HASM/HSIM ist nach dem auspacken des Archivs bereits
       Einsatzbereit. Sie k”nnen sich die Beispielprogramme in 'HASM\DEMO'
       anschauen und die beiden Demo-Batches (demo\68hc11\buffalo\demo.bat
       und demo\multi\demo.bat) ablaufen lassen.

       Verzeichnisstruktur:

                 ..\ ____ HASM __________ TABELLEN
                                \
                                 \_______ SCRIPT
                                  \
                                   \_____ DOC
                                    \
                                     \___ INIT
                                      \
                                       \_ DEMO

         Inhalt:     HASM         - Hasm.exe        Assembler
                                  - Hsim.exe        Simulator/Monitor
                                  - Hsim.hlp        Onlinehilfe fr Hsim

                     DOC          - HasmHsim.txt    Dokumentation
                                  - Readme.txt      Schneller Ueberblick
                                  - Files.lst       Liste der enthalt. Files

                     TABELLEN     - Z80.TAB         (div. Assemblertabellen)
                                  - MCS-48.TAB
                                  - MCS-51.TAB
                                  - MCS-96.TAB
                                  - 80C552.TAB


                                     - Seite 5 -




                                  - 80C517.TAB
                                  - 80C166.TAB
                                  - 80C552.TAS      80C552 Simulatortabelle
                                  - 80C552.TAM      80C552 Monitortabelle
                                  - PIC16C57.TAS    PIC16C57 Simulatortabelle
                                  - u.s.w

                     SCRIPT       ...               Globale Scriptfiles
                                                    (=Tastaturmacros)

                     INIT         ...               Setup Support fr
                                                    Editoren

                     DEMO         ...               Test und
                                                    Beispielprogramme


       Der Aufruf von Assembler bzw Simulator muss nach dieser sehr einfachen
       installation noch mit vollstaendigem Pfad erfolgen. Dies ist
       insbesondere bei der Programmentwicklung sehr l„stig.

       Einfacher Aufruf unter DOS:
        - Entweder den Pfad fr HASM.EXE mit in die 'PATH' Umgebungsvariable
          aufnehmen (h„ufig nicht erwnscht, weil die PATH-Variable dann sehr
          lang wird)
        - oder einen Batch mit vollst„ndigem Pfad in ein Unterverzeichnis,
          welches sich bereits in der PATH-Variable befindet.
          H„ufig gibt es zu diesem Zweck bereits ein Unterverzeichnis mit dem
          Namen 'BAT'oder 'BATCH'. In dieses Unterverzeichnis kann man dann
          eine kleine Datei mit dem Namen 'HASM.BAT' legen mit folgenden
          Inhalt:

             @echo off
             c:\hasm\hasm.exe %1 %2 %3 %4 %5 %6 %7 %8 %9

          Der Pfad zu 'Hasm.exe' muá natrlich den tats„chlichen
          Gegebenheiten angepaát werden.
          Entsprechend verf„hrt man mit 'Hsim.exe' !

       Integration in die Workbench von Microsoft fr C/C++ 7.0:
        - Inhalt der Datei 'MSC70.ini' in das File 'TOOLS.ini' der Workbench
          importieren. Dabei die Tabelle fr den gewnschten Prozessor gleich
          richtig einsetzen.
        - Pfadangaben in diesem Abschnitt berprfen und n”tigenfalls an die
          Gegebenheiten anpassen.
        - Beim Installieren fr mehrere Prozessoren fr jeden Prozessor einen
          Block in die Datei 'TOOLS.ini' einfgen.

       Benutzen von Microsoft Windows 3.1 als Programmierumgebung:
        - Ohne groáen Aufwand ist es m”glich Windows 3.1 als einfache
          Programmierumgebung zu verwenden.
        - Zum EDITIEREN der Sourcefiles (Endung '.ASM') sollte im
          Dateimanager eine Verknpfung zwischen der Endung '.ASM' und einem
          Texteditor hergestellt werden. Wenn man mit einer maximalen
          Textgr”áe von 64 kByte auskommt, so kann man als Texteditor den mit
          Windows mitgelieferten Editor 'Notepad.exe' verwenden. Zum
          Einstellen der Verknpfung kann man im Dateinmanager eine Datei mit
          der Endung '.ASM' markieren und dann im Datei-Men den Unterpunkt
          Verknpfen ausw„hlen. Es erscheint nun ein Fenster in dem die


                                     - Seite 6 -




          Erweiterung '.ASM' steht und eventuell ein Programm welches mit
          dieser Erweiterung verknpft ist. Mit dem Button 'Durchsuchen' kann
          nun der gewnschte Editor (Notepad.exe) ausgew„hlt werden. Durch
          Doppelklick auf eine Datei mit der Endung '.ASM' wird diese nun
          automatisch im gew„hlten Editor angezeigt.
        - in „hnlicher Weise sollte man auch mit den Endungen '.LST' und
          '.MAP' verfahren.
        - Im Zusammenhang mit Windows als einfache Programmierumgebung ist
          m”glicherweise auch kein MAKE-Utility vorhanden. In diesem Fall
          kann man fr jedes Projekt ein Make-Batch erstellen. Bei diesem
          Make-Batch handelt es sich um eine Reihe von DOS-Befehlen die
          nacheinander ausgefhrt werden mssen um das gewnschte Programm
          aus den entsprechenden Sourcefiles zu erzeugen.

             Beispiel: 'demo\68HC11\float\demo.bat'

                     Inhalt dieses Files:
                            call hasm demo.asm /?obj >Out.lst
                            call hasm float.asm /?obj >>Out.lst

                            call hasm demo.obj float.obj /link >>Out.lst

          Das Beispielprogramm besteht aus den beiden Sourcefiles 'demo.asm'
          und 'float.asm'. Zu den beiden Sourcefiles mssen zun„chst die
          zugeh”rigen OBJ-Files erzeugt werden. Anschlieáend muá das Programm
          bestehend aus den zwei OBJ-Modulen zusammengelinkt werden. Alle
          Ausgaben von HASM sollen dabei nicht am Bildschirm erscheinen
          sondern in das File 'Out.LST' geschrieben werden. (ACHTUNG: Bei der
          ersten Ausgabeumleitung '>Out.LST' ist nur ein '>' bei allen
          weiteren sind es zwei '>>'.) Durch die Option '/?obj' wird von HASM
          zun„chst geprft, ob sich das angegenbene Sourcefile seit der
          letzten Assemblierung ge„ndert hat oder nicht. Nur bei einer
          Ver„nderung wird die Assemblierung neu durchgefhrt. Das spart viel
          Zeit !
          Das vollst„ndige šbersetzen des Programms wird mit einem
          Doppelklick auf diese Batchdatei ausgel”st. Die Meldungen beim
          šbersetzen k”nnen mit einem Doppelklick auf 'Out.LST' angeschaut
          werden.
        - Wenn man nun noch die Endung '.SIM' mit dem Programm 'HSIM.EXE'
          verbindet, so startet ein Doppelklick auf das entsprechende '.SIM'-
          File sofort den Simulator.

       Geschwindigkeit optimieren:
        - Bei einer Installation ohne Festplattencache (Smartdrv,Qcache,..)
          kann es Vorteile bringen eine Umgebungsvariable 'TMP' zu definieren
          die auf ein RAM-Laufwerk zeigt. (fr Tempor„re Dateien !)















                                     - Seite 7 -








     III.    HASM - Assembler


      A. Einleitung

       Ansatzpunkt fr die Entwicklung dieses Assemblers war es, ein Werkzeug
       zu schaffen, mit dem es m”glich ist unterschiedlichste Arten und
       Ausfhrungen von Prozessoren, Mikrokontroller und Signalprozessoren zu
       programmieren ohne fr jeden Prozessor einen eigenen Assembler
       anschaffen und erlernen zu mssen. Speziell wenn man kurzfristig und
       flexibel auf andere Kontrollertypen wechseln m”chte bringt dieser
       Ansatz Vorteile. Man muá nicht fr jeden Prozessor die Eigenheiten
       eines anderen Assemblers erlernen. Alle Anweisungen an den Assembler
       bleiben gleich. Nur der Befehlssatz des jeweiligen Prozessors muá
       erlernt werden.

       Die Anpassung des Assemblers an den jeweiligen Prozessor geschieht
       dabei ber eine Tabelle, die es erlaubt den Assembler in sehr weiten
       Bereichen zu konfigurieren. Diese Tabelle ist direkt lesbar. Eine
       Dokumentation fr die Erstellung solcher Tabellen ist vorhanden, so
       daá es m”glich ist eigene Tabellen fr neue Prozessoren zu erstellen.

       Besonderer Wert bei der Entwicklung des Assemblers wurde auf eine
       leichte Portierbarkeit von Quellcode eines anderen Assemblers auf
       diesen Assembler gelegt.

       In der hier vorliegenden Anleitung zur Benutzung von HASM wird nur auf
       Befehle eingegangen die direkt auf HASM einwirken und fr alle
       untersttzten Prozessoren gleich sind. (Prozessorspezifische Daten wie
       zum Beispiel der Befehlssatz des jeweiligen Prozessors kann man den
       Herstellerdatenbchern entnehmen oder in der entsprechenden Tabelle
       *.tab nachschauen !)

       Wenn Sie eigene Tabellen erstellt haben, so w„re es sch”n wenn Sie
       diese auch anderen Programmierer zug„nglich machen wrden !
       Einfachster Weg:Schicken Sie sie an mich, ich werde sie dann zusammen
             mit der n„chsten Version des Programms weiterverbreiten !


      B. HASM Schnell-šberblick

       KOMMANDO-Zeile:

             HASM src_file [/Option1] [/Option2] ...


       Optionen:










                                     - Seite 8 -




             /Ttabelle[.tab]      Verbindung zur gewnschten Assemblertabelle
             /Hfile[.hex]         Gewnschter Name der Intel-Hex-Datei
             /Czahl               Gewnschte Code-Breite im Listing
             /Fzahl               Maximale Anzahl der anzuzeigenden Fehler
             /Symbole             Fgt am Ende des Listings eine
             Symboltabelle an
             /NOCode              Unterdrckt Code und Adresse im Listing
             /EMakros             Makroaufrufe im Listing expandieren
             /NOSegments          Keine Segment-Tabelle im Listing erzeugen
             /OBJ                 Nicht Linken - Nur Objektfile erzeugen
             /LINK                Angegebenen Objektfiles Linken zum Hexfile
             /P[Format]           HEX-File-Format:
             INH(=default),INHX8M,INHX32
             /Q[uiet]             Keine Punkte mehr beim Assemblieren


       Preprozessor-Direktiven:

             #INCLUDE <filename> Importiert <filename> als Quell-Code
             #DEFINE <label> [<expr>] Definiert ein Label
             #IFDEF <label>  Assembliert das Folgende wenn <label> DEFINE'd
             #IFNDEF <label> Assembliert das Folgende wenn <label> nicht
             DEFINE'd
             #IF <expr>      Assemblierung des Folgenden wenn <expr> nicht 0
             #ELSE           Block der ersatzweise Assembliert werden soll
             #ENDIF          Ende der Bedingten Assemblierung
             #ASSEMBLER <filename> Bestimmt die Assemblertabelle (CPU).
             #SYMLIST        Fgt am Ende des Listings eine Syboltabelle an.
             #[NO]EXPANDMAKROS  Makroaufrufe im Listing expandieren oder
             nicht
             #[NO]LISTMAKROS           Makrodefinitionen ins Listing oder
             nicht
             #[NO]LIST       Quelltext ins Listing oder nicht


       Assembler-Direktiven:

             Name SEGMENT <Seg_Art> \ Aktiviert den aktuellen
                 ?SEG [AT <expr>]   |   Code/Daten-Zeiger
                 ?SEG Name          /    eines Segments
                 ORG <expr>         Setzt den aktiven Code/Daten-Zeiger
                 DB <string>        Reserviert & initialisiert mehrere Bytes
             Label EQU <expr>       Ordnet einem Label einen Wert zu
             Label SET <expr>              Ordnet einem Label einen Wert zu
                 DS <expr>          Reserviert Speicher ohne Initialisierung
             Name MACRO <args       Anfang einer Makrodefinition
                 ENDMACRO           Ende einer Makrodefinition
                 TABELLE <var>,...  Tabellenerzeugung (Argumente -> Hasm.DOC)
                 LOCAL              Anfang einer Variablenabgrenzung
                 ENDLOCAL           Ende einer Variablenabgrenzung
                 PUBLIC  <var>[,<var>[,..]] Scope Lokaler Variablen
             vergr”áern
                 EXTERN  <var>[,<var>[,..]] Verbindung zu anderem Objektmodul
                 OBJECT <obj_file>  Einbindes eines Objektmoduls in ein
             Programm
                 LIBRARY <lib_file> Hinweis auf eine Library





                                     - Seite 9 -




      C. Datenstrukturen

       Der Assembler kennt 3 unterschiedliche Datentypen aus denen dann
       beliebige Ausdrcke gebildet werden k”nnen:

                                                    Speicherbedarf
             Integerzahlen (=Ganze Zahlen)             4 Bytes
             Floatzahlen (Flieákommazahlen)            8 Bytes
             Strings (Zeichenketten)                  <128 Bytes

       Integerzahlen:

                           <digits><suffix>
                             \        \
                              \        \_ Suffix  b/B  o/O d/D/-  h/H
                               \          ----------------------------
                                \         Basis    2    8   10    16
                                 \
                                  \_ digits = 0-9,a-f,A-F

          oder

             '<Zeichen>'
               \
                \
                  Bis zu 4 Zeichen aus: a-z,A-Z,0-9,”„Öš
                                       #á@+-*/!$%&()=?^.[]{},.;:_
                  ESC-Sequenzen:       ' = \'
                                       " = \"
                                       \ = \\
                                 Klingel = \a  (= 7d)
                               Backspace = \b  (= 8d)
                              Neue Seite = \f  (= 12d)
                              Neue Zeile = \n  (= 10d)
                            Zeilenanfang = \r  (= 13d)
                      Horzontal Tabulator = \t  (= 9d)
                       Vertikal Tabulator = \v  (= 11d)
                     (beliebiges Zeichen) = \xx (xx = 2-stellige Hexzahl)

        - Default (also ohne Suffix) wird als Dezimalzahl interpretiert.
        - Der Assembler versteht auch das Prefix '$' fr Hexadezimale Zahlen
          und das Prefix '%' fr Bin„re Zahlen (Beispiel: %1100 = $C = 12).
          Bei eigenen Programmen sollte diese Schreibweise m”glichst nicht
          verwendet werden.
        - Bei mehr als einem Zeichen werden alle Zeichen als Stellen einer
          Zahl im System mit der Basis 256 interpretiert. (z.B.: '0 \'' =
          (48*256+32)*256+39 = 3153959d = 302027h)

       Floatzahlen:

                     [-]<digits>[.][<digits>][e/E[-]<digits>]

        - Bei dieser Art von Zahlen tritt in Zusammenhang mit Bitadressen bei
          einigen Prozessoren (MCS-51,MCS-96,..) eine besonderheit auf. Der
          Punkt wird bei diesen Prozessoren n„mlich als Bitoperator verwendet
          (Beispiel: 25.4 = 4.Bit auf der Adresse 25d). Das angefhrte
          Beispiel k”nnte nicht von der Floatzahl 25.4 unterschieden werden.
          Aus diesem Grund muá bei Dezimaladressen im Zusammenhang mit
          Bitadressierung beim obigen Beispiel 25d.4 geschrieben werden. Bei


                                     - Seite 10 -




          anderen Zahlenbasen tritt dieses Problem nicht auf da dort ohnehin
          ein Suffix angegeben werden muá.

       Zeichenketten:

             "<Zeichen>"
               \
                \
                  Bis zu 128 Zeichen aus: a-z,A-Z,0-9,”„Öš
                                         #á@+-*/!$%&()=?^.[]{},.;:_
                  ESC-Sequenzen:       ' = \'
                                       " = \"
                                       \ = \\
                                 Klingel = \a  (= 7d)
                               Backspace = \b  (= 8d)
                              Neue Seite = \f  (= 12d)
                              Neue Zeile = \n  (= 10d)
                            Zeilenanfang = \r  (= 13d)
                      Horzontal Tabulator = \t  (= 9d)
                       Vertikal Tabulator = \v  (= 11d)
                     (beliebiges Zeichen) = \xx (xx = 2-stellige Hexzahl)


       Operatoren:

             Operator     Priorit„t     Beschreibung
                       Int Float String
             -          9   9     -     Vorzeichen
             ~          9   9     -     1-er Komplement
             ^          -   8     -     Potenzieren
             *          7   7     -     Multiplizieren
             /          7   7     -     Dividieren
             %          7   -     -     Modulo (Rest nach Division)
             -          6   6     -     Subtrahieren
             +          6   6    (6)    Addieren
             <<         5   -     -     links schieben
             >>         5   -     -     rechts schieben
             <          4   4     -     Kleiner als
             <=         4   4     -     Kleiner oder gleich
             >          4   4     -     Gr”áer als
             >=         4   4     -     Gr”áer oder gleich
             ==         4   4     -     Gleich
             !=         4   4     -     Nicht gleich
             =          4   4     -     Gleich
             &          3   -     -     Bitweise 'UND'
             ^          3   -     -     Bitweise 'EXOR'
             |          3   -     -     Bitweise 'ODER'


       Funktionen:

             int()        \
             float()       \ Umwandlung zwischen den verschiedenen
             string()      /     Datentypen


             Mathematische Funktionen:
             abs(),sin(),cos(),tan(),asin(),acos(),atan()
             sinh(),cosh(),tanh(),exp(),log(),log10()


                                     - Seite 11 -




       Namen:   fr Label, Segmente, usw.
        - Zugelassen sind Buchstaben, Zahlen und '_'(Underscore)
        - Das erste Zeichen muá ein Buchstabe sein.

             Beispiele: So_muá_ein_Label_aussehen   ; o.K.
                        L1_a                       ; o.K.
                        4DOS                       ; Fehler !

        - Label- oder Makronamen sollten in der ersten Spalte beginnen.
        - Kommentar kann in jeder Zeile angefgt werden und muá mit ';'
          beginnen.
        - Mit Ausnahme von Makronamen und Maschinenbefehlen (beides Mnemonics
          !) wird zwischen Groá-und Kleinschreibung unterschieden !

       Ausdrcke:
          <expr> = mathematisch sinnvoller Ausdruck betehend aus Zahlen,
            Operatoren, Labels und runden Klammern.
        - Vergleichende Operatoren liefern 0 fr falsch und -1 fr wahr. (Bei
          Integerzahlen bedeutet -1 = FFFFFFFFh)

             Beispiele: 10h+10>>10b     ergibt 6
                        (10h+10b)>>1    ergibt 9
                        ((w1=1)&2)      ergibt 2 wenn w1=1 sonst 0

             (weitere Beispiele fr Ausdrcke im Demofile: 'misc.asm')



      D. Kommandozeile

       Der Minimal-Aufruf des Assemblers lautet:     HASM src_file
        - Dieser Aufruf Assembliert das angegebene Source-File mittels einer
          im Source-File angegebenen Assembler-Tabelle. (-> #Assembler)
        - Defaultm„áig wird bei diesem Aufruf ein List-File erstellt. Das
          List-File tr„gt den Namen des Source-Files mit der Erweiterung
          '.LST'.
        - Zus„tzlich wird bei diesem Aufruf ein Intel-HEX-File erzeugt. Der
          Name entspricht dem des Source-Files mit der Erw. '.HEX'.
          (Defaultformat ist (INH) - Andere Formate -> Option '/P')

       OPTION: HASM src_file /Ttabelle[.tab]
        - Wenn im Source-File keine Assemblertabelle festgelegt wird, kann
          auch in der Aufrufzeile mit der Option /T.. eine Tabelle festgelegt
          werden. (z.B.  HASM src_file /T80C552  )
        - Die zulaessigen Tabellennamen entsprechen den Filenamen mit der
          Endung `.Tab` im Unterverzeichnis `Tabellen` (`.Tab` weglassen !).
          Wenn Sie also eine Tabelle integrieren wollen, so gengt es sie ins
          Unterverzeichnis `Tabellen` zu kopieren.

       OPTION: HASM src_file /Hfile[.hex]
        - Mit dieser Option kann fr das Intel-HEX-File ein anderer Name
          festgelegt werden. (z.B. HASM src_file /Hziel.txt )
        - Wenn nur /H angegeben wird, so wird das HEX-File NICHT erzeugt.

       OPTION: HASM src_file /Czahl
        - Mit dieser Option kann die fr Adresse+Code zur Verfgung stehende
          Breite im Listing festgelegt werden.




                                     - Seite 12 -




        - Standard-Breite sind 16 Zeichen. Das ergibt folgendes Format:
          'AAAAAAAA CCCCCC ' + Originalzeile
          (8 Spalten Adresse, Space, 6 Zeichen Code, Space, Originalzeile)
        - Beispiel: HASM src_file /C18 ergibt:  'AAAAAAAA CCCCCCCC ' +
          Originalzeile
        - Probleme k”nnen in Verbindung mit Texteditoren entstehen die
          Tabulator-Zeichen verwenden. In einem solchen Fall sollte die
          Breite so gew„hlt werden daá sie einem vielfachen dieser
          Tabulatorbreite entspricht.

       OPTION: HASM src_file /Fzahl
        - Mit dieser Option kann die Anzahl der anzuzeigenden Fehlermeldungen
          auf ein 'ertr„gliches' Maá beschr„nkt werden.
        - Standardm„áig ist eine Fehlerzahl von 10 eingestellt.

       OPTION: HASM src_file /Sy[mbole]
        - Mit dieser Option wird an das Ende des Listings eine Tabelle der
          vom Programmierer verwendeten Symbole angefgt.
        - Die Symbole aus der Tabelle werden nicht mit aufgefhrt !

       OPTION: HASM src_file /Si[mulator-File]
        - Durch diese Option wird der Linker angewiesen ein fr den Simulator
          HSIM geeignetes Eingangsfile zu erzeugen

       OPTION: HASM src_file /NoC[ode]
        - Mit dieser Option wird im Listing die Angabe von Adresse und Code
          unterdrckt.
        - Makros werden weiter Expandiert.
        - Mit dieser Option kann ein 'Komplett'-Source-File erstellt werden
          in dem:
          - alle Include-Files enthalten sind
          - alle Bedingten Assemblierungsanweisungen ausgewertet sind
          - alle Define-Ausdrcke ersetzt sind
          - alle Makros expandiert sind und
          - ein Fehlercheck durchgefhrt wurde.

       OPTION: HASM src_file /LInt[ernals]
        - Mit dieser Option werden ins Listing zus„tzlich Assemblerinterne
          Werte aufgenommen.
        - die einzelnen Ausdrcke bei den Argumenten
        - Segmentlisten w„hrend des Linkvorganges

       OPTION: HASM src_file /LT[abelle]
        - Mit dieser Option werden ins Listing zus„tzlich alle relevanten
          Zeilen der Tabelle ins Listingfile aufgenommen.
        - Beim Verwenden einer neuen Tabelle kann man damit alle in der
          Tabelle verwendeten Makros und Segmentreservierungen anzeigen
          lassen, und zwar auch die, die der Tabellenersteller durch
          geeignete Direktiven normalerweise nicht immer anzeigen will.

       OPTION: HASM src_file /NoS[egments]
        - Mit dieser Option wird die Liste der benutzten Segmente, die
          defaultm„áig im Listing erscheint, unterdrckt.

       OPTION: HASM src_file /[?]Obj[ekt]
        - Mit dieser Option wird der Assembler angewiesen nur ein Objekt-File
          mit zugeh”rigem Listfile zu erstellen (und nicht zu linken ! ).
        - Mit Angabe des '?' untersucht HASM zun„chst ob das Quellfile seit
          dem letzten Assembliervorgang ver„ndert wurde. Wenn keine


                                     - Seite 13 -




          Ver„nderung stattgefunden hat, wird kein neues Objektfile erzeugt.
          Eine eventuelle Ver„nderung wird am Datum von Objekt- und Quellfile
          erkannt. Wenn das Quellfile neueren Datums ist als das Objektfile,
          so wird eine Ver„nderung angenommen und folglich ein neues
          Objektfile erstellt.

       OPTION: HASM src_file /P[Formatkennung]
        - Mit dieser Option kann das Ausgabeformat ge„ndert werden. Momentan
          gibt es folgende 3 Formatkennungen:
          INH: Das Intel-Hex-Format ist g„ngig bei sehr vielen 8-Bit CPU's
            und wird praktisch von allen Programmierger„ten verstanden. Diese
            Format wird automatisch als Default erzeugt wenn diese
            Kommandozeilenoption nicht verwendet wird.
          INHX8M: Erweitertes Intel HEX-Format (16-Bit-Daten wobei die
            Adresse mit zwei multpliziert ist und bei den Daten zun„chst das
            Low- und dann das High-Byte kommt).
          INHX32: Entspricht dem INHX8M-Format enth„lt aber zus„tzlich
            Segmentadressen, so daá mehr als 64k adressiert werden k”nnen.
          BIN: Nur Vollversion ! Das Augabefile besitzt die Erweiterung
            '.BIN' und enth„lt ein direktes Abbild des Codespeichers. Nicht
            benutzte Bereiche werden mit Einsen aufgefllt.

       OPTION: HASM src_file /LM[acros]
        - Mit dieser Option wird der Assembler angewiesen alle
          Makrodefinitionen mit ins Listing aufzunehmen.

       OPTION: HASM src_file /EM[acros]
        - Mit dieser Option wird der Assembler angewiesen bei allen
          Makroaufrufen die Menemonics der Makros mit einzufgen.

       OPTION: HASM src_file /Q[uiet]
        - Mit dieser Option gibt der Assembler keine Lebenszeichen (Punkte,
          Doppelpunkte) w„hrend der šbersetzung mehr von sich.
        - Meldungen werden dadurch nicht beeinfluát.
        - Im Zusammenhang mit intelligenten Editoren verkrzt sich damit das
          zu analysierende Antwortfile !
        - Der Zeitbedarf fr eine Assemblierung nimmt nur geringfgig ab !

       OPTION: HASM obj_file1 [obj_file2] [..] /Link
        - Mit dieser Option wird der Assembler zu einem Linker.
        - Bei den angegebenen OBJ-Files muá es sich um Files handeln, die
          'HASM' mit der Option '/OBJ' erstellt hat.
        - Als Ergebnis werden ein Map-File (.MAP) und ein Hex-File (.HEX)
          erzeugt. Als Name fr die Ergebnis-Files wird der Name des ersten
          OBJ-Files verwendet.
        - Alternativ kann man dem Linker die zu linkenden Dateien auch in
          einer Datei bergeben.

                   Beispiel:  HASM /Link @job.lnk
                   Inhalt von 'job.lnk': obj_file1
                                        obj_file2
                                        ..

          Diese Art von Aufruf wird auch von den meisten Utilities fr
          Programmentwickler benutzt.(z.B. Programmers Workbench von
          Microsoft)
        - Beim Linken k”nnen auch Libraries verwendet werden. Unter Library
          versteht man eine Einheit aus mehreren OBJ-Files auf die man beim
          Linken verweist. Der Linker bindet dann automatisch die Module ins


                                     - Seite 14 -




          Programm die zum Abs„ttigen von Referenzen (Externals) ben”tigt
          werden.
        - Das Erzeugen der Libraries geschieht nicht wie bei anderen
          Assemblern mit einem gesonderten Programm, sondern einfach mit dem
          DOS-Befehl COPY.
        - Angenommen man will die Objektmodule MODUL_1.OBJ, MODUL_2.OBJ und
          MODUL_3.OBJ zu einer Library verbinden, so geschieht dies mit:
            COPY MODUL_1.OBJ+MODUL_2.OBJ+MODUL_3.OBJ MODULE.LIB
        - Wenn man sp„ter noch ein viertes Modul (MODUL_4.OBJ) hinzufgen
          will:
            RENAME MODULE.LIB TMP.LIB
            COPY TMP.LIB+MODUL_4.OBJ MODULE.LIB
            DEL TMP.LIB


      E. Filenamen

       Ausgangsfile fr HASM ist immer der Quelltext, der die Erweiterung
       ".ASM" besitzen sollte. Beim Assemblieren des Quelltextes mit der
       Option "/OBJ" entstehen zwei Files die den Namen des Quelltextes und
       als Endungen ".OBJ" und ".LST" besitzen. Das LST-File enth„lt den
       gesamten Quelltext und zus„tzlich bestimmte Ausgaben des Assemblers.
       Unter anderem sind in disesm LST-File auch alle Fehlermeldungen
       enthalten, die beim Assemblieren aufgetreten sind.

             xxx.ASM ---> (HASM ... /OBJ) -------> xxx.LST
                                          \
                                           ----> xxx.OBJ
                                            \
                                             --> Pass1.LST (Tempor„r)


       Das File mit der Endung ".OBJ" enth„lt alle fr den anschlieáenden
       Linkvorgang n”tigen Daten. Ein oder mehrere solcher OBJ-Files werden
       durch HASM mit der Option "/LINK" zu einem ausfhrbaren Programm
       verbunden. Die noch verschiebbaren Objektmodule werden auf eine feste
       Adresse im Speicher des Zielsystems abgebildet und in Form eines HEX-
       oder BIN-Files ausgegeben. Dieses Ausgabefile kann nun direkt in das
       Zielsystem geladen und zur Ausfhrung gebracht werden.

             xxx.OBJ ---> (HASM ... /LINK) ---------> xxx.MAP
                                           \
                                            ------> xxx.HEX (oder xxx.BIN)
                                             \
                                              ----> xxx.SIM
                                               \
                                                --> Pass1.LST (Tempor„r)


       Das MAP-File enth„lt Angaben ber den Ort, an den der Linker die
       Objektmodule geschoben hat. Durch Angabe der Option "/Simulator" wird
       beim Linken ein weiteres File mit der Endung ".SIM" erzeugt. Dieses
       File enth„lt grunds„tzlich auch alle Angaben die sich im HEX- oder
       BIN-File befinden. Zus„tzlich enth„lt es aber noch weitere
       Informationen, die den Zusammenhang zum(zu den) Quellfile(s)
       herstellen. Mit Hilfe dieses SIM-Files ist es HSIM (Simulator/Monitor)
       m”glich sowohl im Simulatorbetrieb als auch im Monitorbetrieb eine
       Quellkodeorientierte Fehlersuche zu realisieren.



                                     - Seite 15 -




       Bei jedem Aufruf von HASM wird ein File mit dem Namen "Pass1.LST"
       erzeugt oder aktualisiert. Dieses File wird im aktuellen Verzeichnis
       angelegt oder kann mit Hilfe der Umgebungsvariable "TMP" in ein
       beliebiges anders Verzeichnis (RAM-Laufwerk ?) verlegt werden. Dieses
       File kann gefahrlos nach dem Assemblieren wieder gel”scht werden.


      F. Preprozessordirektiven

       #INCLUDE <filename>
        - Importiert <filename> als Source-Code ins Aktuelle Source-File
        - Beispiel:  #Include "zusatz.ASM"

       #DEFINE <label>
       #DEFINE <Text_alt> <Text_neu>
        - Definiert ein Preprozessor-Label fr die Auswertung ber
          Anweisungen zur bedingten Assemblierung.
        - Kann aber auch zum Ersetzen bestimmter Ausdrcke vor der
          Assemblierung im Quell-Text verwendet werden.
        - Ersetzen im Quelltext wird immer nur dann durchgefhrt, wenn ein
          Ersatzausdruck (Text_neu) angegeben ist.

                   Beispiel: #Define Lab_alt Lab_neu

                            Lab_alt DJNZ R1,Lab_alt
                                    MOV A,Lab_alt/256

                   Nach dem Preprozessor (1. Schritt von HASM):
                            Lab_neu DJNZ R1,Lab_neu
                                    MOV A,Lab_neu/256


       #IFDEF <label>
       #IFNDEF <label>
        - Nur wenn die Konstante (nicht) definiert ist, wird der folgende
          Source-Text weiter verarbeitet.
        - Bedingte Assemblierungsanweisungen k”nnen bis zu einer
          Verschachtelungstiefe von 15 eingesetzt werden.

       #IF <expr>
        - Nur wenn der Ausdruck erfllt (wahr) ist, wird der folgende Source-
          Text weiter verarbeitet.
        - Bedingte Assemblierungsanweisungen k”nnen bis zu einer
          Verschachtelungstiefe von 15 eingesetzt werden.

       #ELSE
        - Alternativer Anweisungsblock fr bedingt Assemblierung.

       #ENDIF
        - Ende der bedingten Assemblierung.











                                     - Seite 16 -




             Beispiel: (Bedingte Assemblierung)
                     #DEFINE Monitor

                     #IFDEF Monitor
                     #INCLUDE "TV.ASM"
                     #ELSE
                     #INCLUDE "LCD.ASM"
                     #ENDIF

                            JB Anzeige_init,weiter
                     #IFDEF Monitor
                            LCALL TV_init
                     #ELSE
                            LCALL LCD_init
                     #ENDIF
                     weiter:

             Nach der Assemblierung:
                     ...       \ Inhalt von 'TV.ASM'
                     ...       /
             75 200003      JB Anzeige_init,weiter
             78 120000      LCALL TV_init
             7B        weiter:


       #CPU <tabellen_name>
       #ASSEMBLER <tabellen_name>
        - Legt die Assemblertabelle fest mittels der der vorliegende Source-
          Text Assembliert werden soll.
        - šberschreibt eine unter Umst„nden in der Kommandozeile angegebene
          Option mit dem gleichen Zweck !
        - Bei einem File-Namen ohne Pfadangabe wird im Unterverzeichnis
          'TABELLEN' nach der Tabelle gesucht.
        - Defaulterweiterung ist '.TAB'.

       #SYMLIST
        - Mit dieser Direktive wird an das Ende des Listings eine Tabelle der
          vom Programmierer verwendeten Symbole angefgt.
        - Die Symbole aus der Tabelle (= feste interne Symbole) werden nicht
          mit aufgefhrt !

       #EXPANDMAKROS
       #NOEXPANDMAKROS
        - Mit dieser Direktive wird der Assembler angewiesen bei allen
          Makroaufrufen die Menemonics der Makros mit einzufgen (oder
          nicht).

       #LISTMAKROS
       #NOLISTMAKROS
        - Mit dieser Direktive wird der Assembler angewiesen alle
          Makrodefinitionen ins Listing aufzunehmen (oder nicht).

       #LIST
       #NOLIST
        - Mit dieser Direktive wird der Assembler angewiesen alle folgenden
          Originalzeilen ins Listing aufzunehmen (oder nicht).
        - Default ist #List im normalen Programmtext und #NoList innerhalb
          von Tabellen.



                                     - Seite 17 -




      G. Assembler-Direktiven

       DB <expr>[,<expr>][,<expr>]...
     - Reserviert die entsprechende Anzahl an Bytes im Speicher.
     - Fhrt gleichzeitig eine Initialisierung des betreffenden Bereichs aus
     und kann deshalb nicht in Belegungssegmenten verwendet werden.
     - Zeichenketten-Konstanten drfen maximal 120 Zeichen ( oder Escape-
     Sequenzen ) enthalten.

             Escape-Sequenzen:
                      \a    Alert (Bell)
                      \b    Backspace
                      \f    Formfeed
                      \n    Newline
                      \r    Carriage return
                      \t    Horizontal tab
                      \v    Vertical tab
                      \xx   Zweistellige Hexzahl fr ein Zeichen
                            (Wenn das 2.Zeichen keine Hexziffer mehr
                             ist wird nach dem ersten Zeichen bereits
                             die Umsetzung beendet !)


                   Beispiel:
                                    ORG 100
                            Start   DB "ABC\'\"\r\n\0"

                   Nach der Assemblierung:
                   -----------------------
                   64               ORG 100
                   64 414243
                   67 27220D
                   6A 0A00  Start   DB "ABC\'\"\r\n\0"

             (Weitere Beispiele im DEMO-File: 'demo\80c552\misc\misc.asm')


       <label> TABELLE <var>,<formel>,<von>,<schritt>,#<anzahl>[,<befehl>]
       <label> TABELLE <var>,<formel>,<von>,#<anzahl>,<bis>[,<befehl>]
        - Mit diesem Befehl k”nnen Tabellen erzeugt werden.
        - Im Rahmen dieses Befehls wird fr 'var' eine Real-Variable
          angelegt. Konsequenterweise sind damit fr 'von','schritt' und
          'bis' auch Real-Zahlen m”glich.
        - Mit 'anzahl' wird die Anzahl der gewnschten Tabellenwerte
          festgelegt.
        - 'var' legt die Laufvariable fr die Formel fest.
        - 'formel' kann ein beliebiger numerischer Ausdruck sein
        - Der Tabellebefehl erzeugt eine Folge von Werten die ber 'befehl'
          in den Assemblertext eingebaut werden k”nnen. 'befehl' kann dabei
          ein Maschinenbefehl oder ein Makro sein welches genau ein Argument
          ohne Sonderzeichen und ohne sonstige feste Ausdrcke erwartet. (
          Beispiele -> Demoprogramm: 'demo\80c552\tabellen\Tabellen.asm')
        - Beim Weglassen von <befehl> wird automatisch 'DB _' verwendet.

       DS <expr>
        - Reserviert die durch <expr> angegebene Anzahl an Einheiten im
          Speicher. Die absolute Gr”áe einer Einheit wird durch das Segment
          vorgegeben in dem diese Anweisung steht. In einem normalen
          Codesegment wird mit der Anweisung 'DS 1' genau 1 Byte reserviert.


                                     - Seite 18 -




          In einem Bitsegment wie es die MCS-51 Controllerfamilie besitzt,
          reserviert die gleiche Anweisung genau 1 Bit im Speicher.

             Beispiel:
                        DS 4*bufferlaenge

          Diese Anweisung reserviert an der aktuellen Stelle Platz fr vier
          Bufferspeicher deren L„nge durch die Konstante 'bufferlaenge'
          vorgegeben wird.
        - Fhrt in keinem Fall eine Initialisierung durch. Dient also
          ausschlieálich der Organisation des Speichers.

       <label> EQU <expr>
        - Definiert eine Konstante mit dem Namen 'label' und ordnet ihr den
          Wert <expr> zu.
        - Ausdrcke werden nicht sofort berechnet sondern erst wenn die
          eigentliche Codeumsetzung beginnt.

       <label> SET <expr>
        - Genau wie mit 'EQU' nur kann hier ein bereits existierendes Label
          auch mehrfach umdefiniert werden.

       ORG <expr>
        - Setzt den aktiven Segment-Zeiger auf den durch <expr> festgelegten
          Wert.
        - Damit wird aus einem leeren Segment automatisch ein Festes.
        - Wenn das Segment in dem die 'ORG'-Anweisung steht nicht leer ist,
          dann wird automatich ein neues (Festes) leeres Segment geschaffen.
          (allerdings ohne Namen ! -> '?SEG')
        - Wenn in einem Ausdruck unmittelbar oder auch mittelbar ber andere
          Variablen auf die feste Assemblerinterne Variable '@0' zugegriffen
          wird, so wird das betreffende Segment NICHT Fest ! Es entsteht
          durch das Verwenden der internen Variable '@0' eine besonderen Art
          von Segment welches gesteuert ber eine Formel nur an bestimmten
          Stellen im Speicher allokiert werden kann. Einfachstes
          Anwendungsbeispiel ist das Nachbilden der ALIGN-Anweisung anderer
          Assembler.

             Anderer Assembler:     Align Paragraph
             HASM(direkt):          ORG (@0+16)&(-16)
             HASM Nachbildung:      Paragraph EQU 16 ; Durch diese 4 Zeilen
                              Align Macro Segs     ; kann ALIGN genau wie bei
                                    ORG (@0+Segs)&(-Segs); anderen Assemblern
                                    Endmacro            ; verwendet werden

             Mit der Nachbildung:   Align Paragraph   ;\ 2-mal das Gleiche
                                    Align 16         ;/
                                    Align 256        ; '= Align PAGE'

          Bei der Alignnachbildung ist wichtig zu wissen, daá der Wert von
          '@0' der um eins verminderten ersten zul„ssigen Startadresse beim
          Linken entspricht. Der Ausdruck muá daraus die ERSTE gewnschte
          Startadresse erzeugen. Wenn man das Ergebnis der Formelauswertung
          bei einer zweiten Formelauswertung wieder als '@0' einspeist, muá
          daraus die n„chste gewnschte Startadresse erzeugt werden. Das
          heiát, daá ausgehend von einer vom Assembler vorgegebenen ersten,
          gerade nicht mehr zul„ssigen Startadresse (Startadresse - 1) eine
          und dann nach weiteren Aufrufen weitere gltige Startadressen von
          der Formel zurckgeliefert werden mssen. Der von der Formel


                                     - Seite 19 -




          zurckgelieferte Wert muá immer gr”áer sein als der Wert der
          Variable '@0' beim Aufruf.

       ?SEG [AT <expr>]
       ?SEG <segment_name>
        - Mit dieser Anweisung kann man beliebige Segmente erzeugen und
          beliebig zwischen verschiedenen Segmenten wechseln.
        - Grunds„tzlich gibt es zwei verschiedene Arten von Segmenten:

             1. Feste Segmente und
             2. durch den Linker verschiebbare Segmente.

          1.Feste Segmente:
            Zu festen Segmenten sollten vom Programmierer nur solche Teile
            erkl„rt werden die unbedingt an einer bestimmten Stelle bleiben
            mssen (Beispiel: Tabellen auf die mit einer speziellen
            Adressierungsart zugegriffen werden soll). Erzeugt werden feste
            Segmente durch '?SEG AT <adr>'. Dies hat den Vorteil kurz und
            pr„gnant zu sein, hat aber den Nachteil, daá Segmente die auf
            diese Art erzeugt wurden keinen Namen besitzen. In ein Segment
            ohne Namen kann man wenn man es einmal verlassen hat sp„ter nicht
            mehr hineinwechseln.
            Alternativ kann ein festes Segment auch durch die Anweisungen
            '?SEG <name>' und 'ORG <adr>' erzeugt werden. Diese beiden
            Anweisungen mssen direkt in zwei Zeilen aufeinander folgen und
            erzeugen dann ein festes Segment mit Namen.
          2.Verschiebbare Segmente:
            M”glichst viele (alle ?!) Segmente sollten verschiebbar bleiben.
            Dies ist besonders dann wichtig wenn mit Unterprogramm-
            Bibliotheken gearbeitet werden soll. Ein verschiebbares Segment
            wird mit dem Befehl '?SEG <name>' erzeugt.

        - Neben den grunds„tzlichen Arten von Segmenten werden in der Regel
          sogenannte Segmentgruppen gebildet. Die h„ufigsten unter ihnen sind
          die Gruppe der Code-Segmente und die Gruppe der Daten-Segmente
          (fortsetzbar !). Zu welcher Gruppe ein neu erzeugtes Segment
          geh”ren soll wird durch den ersten Buchstaben der Segmentanweisung
          festgelegt. Beispiel: CSEG fr ein Segment welches zur Gruppe der
          Code-Segmente geh”ren soll.
        - Segment-GRUPPEN spielen eine wichtige Rolle beim Linken der
          einzelnen Segmente zum fertigen Programm. Sie legen mit die
          Reihenfolge und den Ort der Segmente im Speicher fest.

             Beispiel: Beim Programmieren liegen Daten und Code-Segmente
                     bedingt durch Modulare Programmierung und Unterprogramm-
                     Bibliotheken in bunt gemischter Reihenfolge vor.
                     Bei Code-Segmenten kann es aber vorkommen daá sie in
                     ein EPROM gebrannt werden sollen, w„hrend Datensegmente
                     dies normalerweise sehr bel nehmen !!
                     Wenn alle Segmente verschiebbar sind, so kann der
                     Linker die Segmente in eine Anordnung bringen, so daá
                     alle Segmente in einem physikalisch sinnvollen Speicher
                     zu liegen kommen.

          Bei Programmen die nur im RAM-Speicher zur Ausfhrung kommen sind
          Segmentgruppen von untergeordneter Bedeutung. Hier beschr„nkt sich
          die Aufgabe des Linkers meiát darauf, daá er die beweglichen
          Segmente m”glichst geschickt (kleine Lcken im benutzten Speicher)
          zwischen den festen Segmenten unterbringt. Diese Linkart ist in


                                     - Seite 20 -




          HASM als Default installiert. Trotzdem sollte man alle Programme so
          schreiben, daá sie auch im Eprom in etwa das tun was von ihnen
          verlangt wird !? (Wenn sie daá berhaupt jemals k”nnen !)

        - Immer wenn '?SEG' in Verbindung mit einem bereits existieren
          Segmentnamen auftaucht wird in das jeweilige Segment gewechselt
          ohne das Segment zu ver„ndern.
        - Die Segmentzeiger bleiben auch im inaktiven Zustand erhalten !
        - Beim erneuten Wechsel in ein bereits benutztes Segment wird an der
          zuvor verlassenen Stelle fortgefahren.
        - Um zu erfahren welche Segmentgruppen fr einen implementierten
          Prozessor vorhanden sind, sucht man in der entsprechenden
          Prozessortabelle nach Zeilen die mit einem 's' (kleines S)
          beginnen.

             Beispiel: Beim 80C166 von Siemens (80C166.TAB) ergibt
                       die Suche:    s C 0 10000h 0
                                     s CODE 0 10000h 0
                                     s D 0 10000h 0
                                     s DATA 0 10000h 0

          Das erste Argument nach dem 's' ist jeweils die Bezeichnung die bei
          einer Segmentdefinition im Assembler verwendet werden darf. Beim
          '80C166' sind dem nach vier Segmentbezeichnungen (C, Code, D, Data)
          m”glich. Wenn in einer Tabelle keine Zeilen mit 's' enthalten sind,
          so sollte man sich vergewissern, daá in die vorliegende Tabelle
          keine andere Tabelle, die 's'-Zeilen enth„lt, importiert wird.
          Importzeilen beginnen mit 't' worauf als Argument nur der Name der
          zu importierenden Tabelle folgt. Wenn keine andere Tabelle
          importiert wird, so arbeitet der Assembler mit einer
          Defaultsegmentgruppe Namens 'C' (=Codesegment).

       <segname> SEGMENT <segment_gruppe>
        - Alternative zum Erzeugen von Segmenten.
        - Mit dieser Anweisung k”nnen im Gegensatz zu '?SEG' auch Segment-
          Gruppen angesprochen werden die einen L„ngeren Gruppennamen haben.
          Solche Gruppen mssen aber fr den jeweiligen Prozessor in der
          Konfigurationstabelle des Assemblers definiert sein !

       <name>  MACRO    <args>
               ENDMACRO
        - Dient zur Benennung von Befehlssequenzen auf die dann sehr einfach
          zugegriffen werden kann. Wenn der Name der Befehlssequenz im
          Programtext auftaucht, dann wird an dieser Stelle die gesamte
          Befehlssequenz eingefgt. Solche vordefinierten Befehlssequenzen
          bezeichnet man auch als Makros.

             Beispiel 1: (Makro zum L”schen einer Speicherstelle)
                     Reset  Macro arg   ;Anfang der Definition
                            MOV arg,#0  ;\ Makro zum l”schen einer
                            MOV arg+1,#0;/  16-Bit Variable
                            ENDMACRO    ;Ende der Definition

                            DSEG
                            ORG 10      ; Speicher fr die Variable reser-
                     Merker DS 2        ; vieren aber nicht Initialisieren

                            CSEG        ; Variable im Programm
                            Reset Merker;  l”schen (Initialisieren)


                                     - Seite 21 -




             Nach der Assemblierung:

             0              DSEG
             A              ORG 10      ; Speicher fr die Variable reser-
             A        Merker DS 2       ; vieren aber nicht Initialisieren

             0              CSEG        ; Variable im Programm
                            Reset Merker;  l”schen (Initialisieren)
             0 750A00       MOV (Merker),#0
             3 750B00       MOV (Merker)+1,#0

          Diese Beispiel zeigt ein Makro mit einer normalen Variablen. Die
          Variable 'arg' wird im Makro an allen Stellen durch den aktuellen
          Ausdruck (hier: 'Merker') ersetzt. Dabei wird in allen F„llen der
          einzusetzende Ausdruck zus„tzlich in Klammern gesetzt um 'Punkt-
          vor-Strich' Probleme zu vermeiden.
          Bei der Variablenbenennung in der Makrodefinition drfen keine
          festen Mnemonischen Argumente des verwendeten Prozessors verwendet
          werden ! (-> Beispiel 2)

             Beispiel 2: (Feste Mnemonic-Argument, Lokales Label)

                     DEC    Macro DPTR  ; Anfang der Definition
                            PUSH ACC    ;  Einfaches Macro mit einem festen
                            MOV A,DPL   ;  Mnemonic Argument zur Nachbildung
                            DEC DPL     ;  des beim 8031 fehlenden Befehls
                            JNZ Ende    ;  zur Erniedrigung des 16-Bit
                            DEC DPH     ;  Datenzeigers um 1
                     Ende   POP ACC     ;
                            ENDMACRO    ; Ende der Definition


                            CSEG
                            INC DPTR    ; DPTR erh”hen mit Standard-Befehl
                            DEC DPTR    ; DPTR erniedrigen (obiges Macro !!)


             Nach der Assemblierung:
             19             CSEG
             19 A3          INC DPTR    ; DPTR erh”hen mit Standard-Befehl
                            DEC DPTR    ; DPTR erniedrigen (obiges Macro !!)
             1A C0E0        PUSH ACC    ;\
             1C E582        MOV A,DPL   ; \
             1E 1582        DEC DPL     ;  \ Makro: 'DEC DPTR'
             20 7002        JNZ (Ha2sM) ;  / - Das Label 'Ha2sM' wurde
             22 1583        DEC DPH     ; /    vom Assembler vergeben.
             24 D0E0 Ha2sM  POP ACC     ;/

          Dieses Beispiel hat als Besonderheit ein festes Mnemonic-Argument.
          Der verwendete Prozessor kennt einen Befehl 'INC DPTR'. Die
          Funktionsweise des Befehls ist hier zun„chst nebens„chlich, -
          wichtig ist hingegen daá dieser Befehl das feste Mnemonic Argument
          'DPTR' besitzt. šberall wo diese Argument Auftritt wird es nicht
          als Variable behandelt sondern als feste Zeichenkette. Damit lassen
          sich sehr leicht 'Zusatzbefehle' (=Makros) realisieren die in die
          Nomenklatur des verwendeten Prozessors passen. Dieses Makro besitzt
          ein Lokales Label. Dieses Label wird vom Assembler bei jeder
          Verwendung des Makros neu vergeben und natrlich berall im Makro
          eingesetzt.


                                     - Seite 22 -




             Beispiel 3: (Festes Mnemonic-Argument + Sonderzeichen)

                     MOV    Macro A,@arg  ;Anfang der Definition
                            MOV A,R0      ;
                            PUSH ACC      ;
                            MOV R0,#arg   ;
                            MOV A,@R0     ;
                            XCH A,R0      ;
                            POP ACC       ;
                            XCH A,R0      ;
                            ENDMACRO      ;Ende der Definition

                            CSEG
                            MOV A,@10 ;Indirekter Zugriff ber Register 0Ah


             Nach der Assemblierung:

                       MOV A,@10   ; Indirekter Zugriff ber Register 0Ah
             26 E8     MOV A,R0
             27 C0E0   PUSH ACC
             29 780A   MOV R0,#(10)
             2B E6     MOV A,@R0
             2C C8     XCH A,R0
             2D D0E0   POP ACC
             2F C8     XCH A,R0

          Zu bedenken ist bei diesem Beispiel, daá es beim hier verwendeten
          8031 Prozessor sowohl den Befehl 'MOV A,<expr>' als auch den Befehl
          'MOV A,#<expression>' schon gibt ! Die Sonderzeichen die bei
          solchen Definitionen verwendet werden k”nnen sind: '#@[{}]' und in
          besonderen F„llen '+-*/'. (Weitere Beispiele -> Demofile:
          'demo\80c552\makro\makro.asm')

       LOCAL
       PUBLIC <arg1>[,<arg2>[,..]]
       ENDLOCAL
        - Dient zum Begrenzen des Gltigkeitsbereiches von Variablen.
        - Bei komplexeren Programmen ist es sinnvoll sie in kleinere Bl”cke
          zu zerlegen und Variablen zu schaffen die nicht allen anderen
          Modulen zug„nglich sind. Eine Methode dies zu erreichen ist das
          Aufsplitten in mehrere Quellfiles, die erst am Ende durch Linken
          verbunden werden. Eine andere Methode die auch innerhalb eines
          Quellfiles angewendet werden kann ist mit den Befehlen Local,
          Public und Endlocal realisierbar.
















                                     - Seite 23 -




             Beispiel 1: (Grundlagen Lokale Variablen)

                            CSEG AT 100
                            LOCAL       ; Anfang des lokalen Bereichs
                            PUBLIC Sub1 ; 'Sub1' nach auáen sichtbar machen
                     Sub1   JB P3.0,Sprung
                            MOV A,#Wert     ; Benutzen der lokalen Variable
                     Sprung Ret
                            ENDLOCAL        ; Ende des lokalen Bereichs

                     Wert   EQU 27          ; Globale Variable
                            MOV A,Wert
                            JB P4.0,Sprung
                            LCALL Sub1
                     Sprung CLR C


             Nach der Assemblierung:
             64               CSEG AT 100
             64               LOCAL
             64               Public Sub1
             64        Wert   EQU 27
             64 20B002 Sub1   JB P3.0,Sprung
             67 741B          MOV A,#Wert
             69 22     Sprung Ret
                              ENDLOCAL
             6A E51B          MOV A,Wert
             6C 20C003        JB P4.0,Sprung
             6F 120064        LCALL Sub1
             72 C3     Sprung CLR C

        - Durch lokale Variablen k”nnen Konflikte die durch gleiche
          Variablennamen entstehen vermieden werden. Die Variable 'Sprung'
          tritt sowohl im lokalen Bereich als auch im Globalen Bereich auf.
        - Auf die globale Variable 'Wert' kann sowohl global als auch lokal
          zugegriffen werden.
        - Um wie im Beispiel ('Sub1') eine lokale Variable Global ansprechen
          zu k”nnen, muá sie explizit als 'Public' deklariert werden.
        - Lokale Bereiche k”nnen beliebig geschachtelt werden.
        - Eine Verwendung der 'Public'-Deklaration macht die Entsprechende
          Variable eine Lokalebene h”her bekannt.
        - Das verwenden der Public-Anweisung im Globalen Bereich kann dazu
          verwendet werden den Gltigkeitsbereich einer Variablen ber das
          aktuelle File hinaus zu vergr”áern. (-> Linken !)
        - Die entsprechende Variable ist dann nicht nur Global in Bezug auf
          das momentane File, sondern Global in Bezug auf das gesamte
          Programm.

       OBJECT <obj_file>
        - Mit diesem Befehl kann ein Objektmodul direkt in ein Programm
          eingebunden werden, „hnlich dem Linken.

       LIBRARY <lib_file>
        - Mit diesem Befehl kann eine Library bereitgestellt werden die zur
          Abs„ttigung offener Referenzen herangezogen wird.
        - Es werden immer ganze Module eingebunden.

       EXTERN <arg1>[,<arg2>[,..]]



                                     - Seite 24 -




        - Mit diesem Befehl werden Referenzen zu anderen Objektmodulen beim
          Assemblieren zun„chst ohne wirklichen Wert abges„ttigt, damit keine
          Fehlermeldungen erzeugt werden. 'Extern' hat also nur den Sinn dem
          Assembler mitzuteilen welche Variablen erst beim Linkvorgang, aus
          einem anderen Modul einen Wert zugewiesen bekommen.
        - Variablen die mit Extern deklariert wurden, werden auch
          herangezogen um Module aus einer Library auszuw„hlen um sie
          anschlieáend einzubinden ins Programm.


      H. Erstellen eigener Assembler-Tabellen

       Die Konfigurationstabelle fr den Assembler (auch kurz 'Assembler-
       Tabelle' genannt) ist eine elegante M”glichkeit fr erfahrene
       Programmierer(innen) um mit diesem Assembler auch exotische 8-Bit /
       16-Bit Prozessoren, (einige)Signalprozessoren, Bitslice-Elemente oder
       Eigenbau-Ablaufsteuerungen programmieren zu k”nnen.

       Weniger gebte Programmierer brauchen sich mit dem Innenleben der
       Assemblertabelle (wenn sie es nicht wollen) nicht zu besch„ftigen.
       Solche Programmierer verwenden einfach eine fertige Tabelle fr den
       gewnschten Prozessor (wenn sie schon existiert !).

       Doch nun zum Innenleben der Konfigurationstabelle.
        - Die Tabelle ist Zeilenorientiert. Das bedeutet, daá jeweils das
          erste Zeichen in einer Zeile welches kein Leerzeichen ist, festlegt
          was mit dem Rest der Zeile zu geschehen hat.
        - Der Strichpunkt darf in jeder Zeile auftauchen. Alles was nach dem
          Strichpunkt kommt wird als Kommentar betrachtet und nicht weiter
          bbeachtet (weggeworfen !).
        - Achtung: Im Gegensatz zur Programmebene kann in der Tabellenebene
          nur mit Integer-Ausdrcken und Zahlen gearbeitet werden ! (Integer
          = 32-Bit Zahlen vom C-Typ 'long int')

       In der vorliegenden Programmversion sind folgende Schlsselzeichen
       definiert:
          '  <Apostroph> Meldezeilen
          ;  <Strichpunkt> Leitet Kommentar ein (just vor Fun !)
          f  Spezielle Fehlermeldungen definieren.
          .  <Punkt> Leitet eine Prozessorbefehldefinition ein.
          =  <Gleich> Leitet eine Labeldefinition ein
          o  <kleines O> Leitet eine Operatordefinition ein
          s  <kleines s> Segmentgruppen & Clusterdefinitionen
          :  <Doppelpunkt> Quelltextzeilen erzeugen
          t  Importieren einer anderen Assemblertabelle
          w  Wortbreite des Prozessors definieren

       ###
       #'# Der Apostroph leitet Meldezeilen ein
       ###
        - Meldezeilen werden beim Starten des Assemblers auf dem Bildschirm
          angezeigt und erlauben es Eigenheiten und Besonderheiten von
          eigenen oder modifizierten Tabellen wieder ins Ged„chtnis des
          Programmierers zu rufen wenn er die betreffende Tabelle benutzt.
        - Meldezeilen werden zur Dokumentation auch mit ins Listing
          aufgenommen.
        - Auf die eigentliche Assemblierung haben Meldezeilen keinen Einfluá.




                                     - Seite 25 -




       ###
       #;# Der Strichpunkt leitet Kommentarzeilen ein
       ###
        - Auf die Assemblierung haben Kommentarzeilen keinen Einfluá.
        - Kommentarzeilen dienen nur zur Orientierung in der Tabelle.
        - Nicht zu viele Kommentarzeilen einfgen, da sie das lesen der
          Tabelle etwas verlangsamen !

       ###
       #f# Text fr Fehlermeldungen definieren
       ###
        - Beispiel:   fmeldung1 Bitadressen sind hier nicht erlaubt.
          In diesem Beispiel wird der Kennung 'meldung1' die Fehlermeldung
          'Bitadressen sind hier nicht erlaubt.' zugewiesen.
        - Das Verwenden der definierten Fehlermeldungen wird bei den
          Prozessorbefehlen erl„utert. (->Befehl mit spezieller
          Fehlermeldung)

       ###
       #.# Der Punkt leitet Prozessorbefehle ein
       ###
        Befehl ohne Argumente
        - Prozessorbefehle werden fr den Assembler in Form von mnemonischen
          Abkrzungen definiert.

             Beispiel 1: 'CLR C'

        - Befehl eines Prozessors zum l”schen des Carry-Bits. Der Prozessor
          selbst versteht diese Textsequenz natrlich nicht. Aus diesem Grund
          muá diese Sequenz in den entsprechenden HEX-Code umgesetzt werden
          den der Prozessor verarbeiten kann. Nehmen wir an, daá der Hex-Code
          der den Prozessor veranlaát das Carry-Bit zu l”schen '27h' ist. Die
          Anweisung daá der Assembler die richtige Umsetzung durchfhrt
          lautet:

             .CLR C {27h:8}

        - Die Zeichenkette 'CLR C' ist diejenige Zeichenkette die der
          Asembler ab diesem Moment als Befehl versteht.
        - Die Eigentliche Umsetzungsanweisung muá in geschweifte Klammern
          gesetzt werden.
        - Die Anweisungen in der geschweiften Klammer haben immer die Form
          <expr>:<bits>. Zun„chst der Wert und dann die Anzahl der Bits die
          fr diesen Wert verwendet werden sollen.

        Befehl mit Argument
        - Das erste Beispiel war sehr einfach, verdeutlicht aber schon die
          Grunds„tzliche vorgehensweise bei der Umsetzung von Befehlen. Im
          zweiten Beispiel tritt im Assembler-Befehl ein variables Argument
          auf, welches bei der Umsetzung jeweils aktuell eingesetzt werden
          muá.

             Beispiel 2: 'MOV A,<arg>'

        - Befehl zum Laden des Akku mit dem Inhalt einer Speicherstelle.
          Nehmen wir an, es sei ein Befehl der eine 8-Bit Adresse als
          Argument erwartet und als HEX-Code '33h' besitzt.

             Umsetung:  .MOV A,à {33h:8 @1:8}


                                     - Seite 26 -




        - Die ersten 8-Bit werden auf 33h gesetzt.
        - Das Argument 1 wird in der Definition mit '@1' angesprochen und
          direkt in weitere 8 Bits umgesetzt.
        - Eine Verwendung des Befehls  'MOV A,10' wrde mit dieser Definition
          umgesetzt in '33 0A'.

       Befehl mit Argumenten
        - Das geht natrlich auch mit mehr als einem Argument.

             Beispiel 3: 'ADD #<arg1>,<arg2>,<erg>'


             Umsetzung: .ADD #à,à,à {34h:8 @2:8 @1:-8 @3:8}


             Verwendung: ADD #10b,11,11+1


             Code: 340B020C

        - Die Reihenfolge der Argumente kann innerhalb der Umsetzung beliebig
          ver„ndert werden.
        - Besonderheit -8 Bits beim Argument '@1' Bei allen bisher erwarteten
          Argumenten wurden Adressen bergeben die in jedem Fall positive
          Werte enthalten muáten und deshalb eine Fehlermeldung bei negativen
          Zahlen (wie sie vom Assembler erzeugt wird !) durchaus
          wnschenswert ist. Nicht so bei direkten Werten, hier kann der
          Programmierer durchaus einen negativen Wert (z.B. -3) verwenden der
          dann als Zweierkomplementzahl eingesetzt werden soll. Um eine
          Fehlermeldung des Assembler zu verhindern muá die Bitzahl als
          negativer Wert angegeben werden. Der zul„ssige Wertebereich fr
          Argumente wird damit auf den Bereich von -128 bis +255 ausgedehnt.
          Bei einer anderen Bitzahl natrlich entsprechend.

       Spezielles Argument @0 (Programmz„hler)

             Beispiel 4: 'RJMP <ziel>'      (relativer Sprung)

             Umsetzung: .RJMP à {0E4h:8 @1-@0-2:8}
                                    ;Achtung DEMO nicht optimal !

             Verwendung: Label      RJMP Label     (Endlosschleife !)

             Code: E4FE

        - Bei '@0' als Argument handelt es sich um den Programmz„hler der
          speziell bei relativen Sprngen ben”tigt wird.
        - Hier tritt jetzt wieder ein „hnliches Problem auf wie bei den
          direkten Werten. Die Differenz '@1-@0-2' kann bei Rckw„rts-
          Sprngen negativ werden und wrde damit eine Fehlermeldung
          ausl”sen. Eine ':-8' anstelle von ':8' wrde das Problem der
          Fehlermeldung zwar beheben aber falsche Sprnge erzeugen. Grund
          dafr ist, daá der durch ':-8 erweiterte Wertebereich nun Sprnge
          zul„át die mehr als 127 Bytes nach vorne reichen. Eine L”sung des
          Problem bietet folgende Umsetzung.

             bessere Umsetzung: .RJMP à {0E4h:8 (128+@1-@0-2)^80h:8}




                                     - Seite 27 -




        - Mit dieser Umsetzung generiert der Assembler die richtigen
          Fehlermeldungen und l„át alle gltigen Werte zu.
        - Fr <exp> in der Abbildung k”nnen durchaus auch komplexere
          Ausdrcke verwendet werden.
          Im Beispiel berechnet der Assembler bei der Umsetzung:

             ( 128 + <ziel> - PCounter - 2) EXOR 80h

        - Wichtig ist, bei einer Umsetzung genau darauf zu achten, daá bei
          unzul„ssigen Werten eine Fehlermeldung generiert wird !
        - Die Bitzahl der einzelnen Argumente muá nicht 8 sein !
        - Die Gesamtzahl der Bits eines kompletten Befehls muá aber durch
          acht ohne Rest teilbar sein !

             Beispiel 5: 'JBS <adr>,<bit>,<ziel>'

        - relativer Sprung Sprung wenn ein bestimmtes Bit einer bestimmten
          Speicherstelle gesetzt ist.(80C196)

             Umsetzung: .JBS à,à,à   {7:5 @2:3 @1:8 (128+@3-@0-3)^80h:8}

             Verwendung: Port    EQU 78h
                         Label   JBS Port,3,Label ;Warten bis Bit Nr.3 Low

             Code: 2B78FE


             Aufschsselung der Einzelbits im Code:

                Bin„r: 0010 1011 0111 1000 1111 1110
                       \    /\ / \      / rel.Sprungadresse
                        \  /  |   Speicherstelle
                         \/  Bitadresse
                          Op-Code


       Befehl mit variabler Codel„nge
        - Abh„ngig von der Gr”áe eines Arguments wird ein Befehl erzeugt mit
          langer (16-Bit) oder kurzer (8-Bit) Adresse.

             Beispiel 6: 'ADD A,<adr>'

        - wenn die Adresse kleiner als 256 ist soll automatisch ein Befehl
          mit Zero-Page-Adresse erzeugt werden.

             Umsetzung: .ADD A,à {?@1>255{20h:8 @1:16}{21h:8 @1:8}}}

             1. Verwendung: ADD A,374h   ; Adresse > 255 -> 3 Code-Bytes

             Code: 210374

             2. Verwendung: ADD A,45h    ; Adresse < 256 -> 2 Code-Bytes

             Code: 2045

        - Das allgemeine Format fr Bedingungen lautet:

             {?<bed1>{ wenn bed1 wahr ! }{ wenn bed1 falsch ! }}



                                     - Seite 28 -




        - Bedingungen k”nnen beliebig geschachtelt werden.
        - ABER !!  Diese Realisierung ist zwar kurz hat aber einen kleinen
          Nachteil - wenn sich der Wert des Argumentes welcher zur festlegung
          der Codel„nge fhrt w„hrend der Assemblierung „ndert, fhrt das zu
          einer Fehlermeldung:

             'Adressenfehler im Segment ...'

          die meist schlecht behebbar ist.
        - Besser !

             Umsetzung: .ADD A,à {?@1>255{"ADDL A,à"}{"ADDZ A,à"}}
                              .ADDZ A,à {21h:8 @1:8}
                              .ADDL A,à {20h:8 @1:16}

        - Man erzeugt fr alle unterschiedlich langen Befehle einen eigenen
          Mnemonic Code (ist bei vielen Prozessoren ohnehin vorgesehen !)
        - Zum einen generiert der Assembler bei dieser Umsetzung eine
          Fehlermeldung an der richtigen Stelle im Quelltext und zum anderen
          kann die Fehlermeldung nun durch direkte Anwahl des richtigen
          Befehls (durch den Programmierer) behoben werden.

       Befehl mit Abkrzungen (Makros)
        - Da Prozessoren meist eine ganze Reihe Befehle mit gleichen oder
          „hnlichen Argumenten besitzen ist es, speziell w„hrend der
          Entwicklung der Tabelle, sinnvoll bestimmte Abbildungssequenzen als
          Makros zu definieren.

             Beispiel: .!WR1    {@1&1:0 @1:8}   ; MAKRO: Word-Register
                       .!WR2+   {@2&1:0 @2|1:8} ; MAKRO: Word-Register mit LSB
                       .ADD à,[à]+  {66h:8 '!WR2+' '!WR1'}
                       .ADDC à,[à]+ {0A6h:8 '!WR2+' '!WR1'}
                       .CLR à       {1:8 '!WR1'}

        - Damit erreicht man neben einer krzeren und effizienteren
          Schreibweise auch, daá sich nderungen an den Makros sofort auf
          alle Befehle auswirken, die diese Makros verwenden.

       Befehl mit spezieller Fehlermeldung
        - Damit Fehlermeldungen dem Programmierer einen Hinweis auf die
          Fehlerursache geben k”nnen sollten bei prozessorspezifischen
          Fehlern auch prozessorspezifische Fehlermeldungen produziert
          werden.

             Beispiel:
                 fa Arg1: Als GPR-Wort-Adresse sind nur R0-R15 zul„ssig.
                 .CPL à {91h:8 (@1&0FFE1h)!=0FFE0h:0a (@1&01Fh)>>1:4 0:4}
                                                   ^
                                                   |
                                    Verbindung zur Fehlermeldung `a`
                                      (die in der oberen der beiden
                                          Zeilen definiert wird)

        - Normalerweise gibt der Assembler beim šberschreiten des
          Wertebereichs fr ein Element eines Befehlscodes eine einzige immer
          „hnliche Bereichsfehlermeldung aus, die als Zusatzinformation nur
          den Teil der Tabellenzeile enth„lt in der der Fehler aufgetreten
          ist. Fr Programmierer die sich mit den Tabellen nicht belasten
          m”chten, sind diese Fehlermeldungen relativ nichtssagend. Lediglich


                                     - Seite 29 -




          die Ortsangabe im Quelltext und die Tatsache daá mit einem Argument
          etwas nicht stimmt k”nnen entnommen werden.
          Wenn im Anschluá an die Bitzahl die eine Sequenz erzeugen soll eine
          Verbindung zu einer Fehlermeldung angegeben ist, so wird bei einer
          Bereichsberschreitung die entsprechende Fehlermeldung ausgegeben.
        - Im obigen Beispiel dient die Sequenz '(@1&0FFE1h)!=0FFE0h:0a' sogar
          nur zur šberprfung ob das Argument in einem bestimmten
          Wertebereich liegt. Im Fehlerfall wird die entsprechende
          Fehlermeldung erzeugt.
        - Damit kann je nach Aufwand den man treiben will eine
          Assemblertabelle mehr oder weniger gute Hinweise fr den
          Programmierer geben der sie verwendet.
        - Weitere Beispiele fr solche speziellen Fehlermeldungen befinden
          sich praktisch in allen Assemblertabellen !

       Zugriff des Assemblers auf die Tabelle
        - Der Assembler erkennt beim Einlesen einer Tabelle ob die Befehle
          sortiert vorliegen oder nicht und greift dementsprechend
          sequentiell oder ber einen intelligenten Algorithmus auf die
          Tabelle zu.
        - Deshalb k”nnen w„hrend der Entwicklung der Tabelle die Befehle in
          bunter Reihenfolge angegeben werden.
        - Wenn die Tabelle vollst„ndig ist, sollte sie mit einem Texteditor
          sortiert werden (nur den Befehlsteil !) damit der Assembler
          schneller auf die Tabelle zugreifen kann.
        - Dabei hat es sich als Zweckm„áig erwiesen vor dem Sortieren das
          Zeichen 'à' (ASCII: Alt+224) durch die Zeichenfolge 'z!z' zu
          erstzen und nach dem Sortieren diese Ersetzung wieder Rckg„ngig zu
          machen.

       ###
       #=# Gleichheitszeichen leitet Labeldefinition ein
       ###
        - Beispiel: =Port_A 0FF01h
          In diesem Beispiel wird dem Label 'Port_A' der Wert 0FF01h
          zugewiesen.
        - Labeldefinitionen k”nnen dazu verwendet werden, dem Assembler
          bestimmte feste Bezugsadressen des jeweiligen Assemblers bekannt zu
          machen. Paradebeispiele sind dabei Adressbelegungen fester
          Hardwarekomponenten die sich auf dem Chip des Prozessors befinden
          oder feste Interrupt-Einsprungadressen.
        - Wenn man viel fr einen Controller in einer bestimmten Hardware
          Umgebung programmiert kann man deren feste Adressen auch in die
          Tabelle mit aufnehmen. Dieser ge„nderten Tabelle sollte man aber
          einen anderen Namen verpassen oder zumindest entsprechende
          Meldezeilen einfgen damit ein anderer Programmierer auf diese
          Tatsache hingewiesen wird.
        - Diese Art von Definition fr feste Adressen bei einem Prozessor ist
          zwar sehr einfach, hat aber den Nachteil, daá der
          Simulator/Debugger nur den entsprechenden festen zugeordneten Wert
          anzeigen kann und NICHT den Wert der unter dieser Adresse in einem
          bestimmten Segment zu finden ist, was eigentlich wnschenswert
          w„re. Abhilfe schafft hier eine Segmentbezogene Definition mit dem
          ':' Operator. Ein Beispiel fr eine solche Definition ist in der
          Tabelle fr den Prozessor 80C552 (TABELLEN\80C552.TAB) zu finden.

       ###
       #o# <kleines o> Leitet eine Operatordefinition ein
       ###


                                     - Seite 30 -




        - Ohne zwingenden Grund sollte man sehr sparsam mit dieser (sehr
          leistungsf„higen) M”glichkeit umgehen. (Wenn man den Ausdruck '5+4'
          in einem Assemblerquelltext verwendet und als Ergebnis 1 erh„lt ist
          das nur mit sehr guten Argumenten zu erkl„ren !)
        - Ein willkrliches Umdefinieren ist zwar m”glich aber nicht im Sinne
          des Erfinders (Programmierers).
        - Die meisten Assembler fr Prozessoren der MCS-51 Serie untersttzen
          einen Operator zum ansprechen bestimmter Bitpositionen im
          bitadressierbaren Bereich des Prozessors.

             Beispiel: 23h.2  soll der Bitadresse 1Ah entsprechen

        - Allgemein l„át sich die geforderte Umsetzung mit folgender
          Abbildung erreichen:

             Bitadr := (Byteadr << 3) + Bitpos

        - Diese Abbildung weiát man nun dem Punktoperator mit folgender
          Definition zu:

             o. (@1<<3)+@2

        - Jetzt macht der Assembler die gewnschte Umsetzung automatisch.
        - Unglcklicherweise ist die Umsetzung in Bitadressen beim Beispiel
          Prozessor nicht konsequent. Mit der realisierten Umsetzung lassen
          sich nur die RAM-Flags erreichen. Die Special-Funktion-Bits
          ben”tigen folgende Umsetzung:

             Bitadr := Byteadr + Bitpos

        - Die gewnschten Umsetzungen realisiert folgende Definition fr
          beide Arten von Bitadressen. Zus„tzlich ist diese Definition gleich
          so modifiziert, daá der Assembler an den Richtigen Stellen
          Fehlermeldungen liefert.

             o. ((2=@1>>4)&(100h^@2+@1<<3))|((80h=@1&0FF87h)&(@2+@1))

        - Im konkreten Fall sehr ntzlich - ABER !! nicht bertreiben !

       ###
       #s# <kleines s> Definiert Segment-Gruppen (Cluster)
       ###
        - FORMAT:
          s <Name> <Start> <Ende> <Clust-Nr> [-> <ZName> <Start> <L„nge>]
        - Um die Definition einer Segmentgruppe zu verstehen muá man zun„chst
          wissen, daá HASM neben der Zuordnung der vom Programmierer
          erzeugten Segmente zu bestimmten Segmentgruppen, diese
          Segmentgruppen wiederum zu Gruppenclustern zusammenfaát.













                                     - Seite 31 -




             Beispiel:       (Gilt so beim 80c552 von Philips!)
                                      Seg1\
                                      Seg2 \__
                                      Seg3 /   Codesegmente   \
                                      ... /                   \ Gruppen-
                                      Seg4\                   / Cluster 0
                                      Seg5 \__ XDatensegmente / (Code
                                      Seg6 /                     erzeugend)
                                      ... /

                                      Seg7\
                                      Seg8 \__
                                      Seg9 /   Stacksegmente   \
                                      ... /                    \
                                      Seg10\                    \ Gruppen-
                                      Seg11 \                   / Cluster 2
                                      Seg12  \                 / (kein Code)
                                      ...     \ Internsegmente /
             Seg13\                   Sega    /
             Seg14 \__      (Mapping) Segb   /
             Seg15 / Bitsegmente ->   Segc  /
             ...  /(Gruppencluster 1)  ...  /

        - Die Segmente Seg1-Seg15 sollen vom Programmierer angelegt worden
          sein.
        - Die Segmente Sega-Segc werden vom Assembler nach einer
          entsprechenden Mappingvorschrift in der Assemblertabelle erzeugt.
          Allgemein k”nnen ber den Mappingmechanismus beliebige virtuelle
          Segmentgruppen erzeugt werden, die dann ihre jeweilige Entsprechung
          im physikalisch vorhandenen Speicher selbst erzeugen. Dabei kann
          sowohl ein Offset als auch eine ge„nderte L„nge bercksichtigt
          werden.
        - Das wichtigste der Gruppencluster ist das mit der Nummer 0. Dieses
          und nur dieses Cluster erzeugt ausfhrbaren Maschinencode.

             Beispiel: s CODE  0 10000h 0
                       s XDATA 0 10000h 0
                        \\  /  \\    /  \
                         \\ /    \\ /    Cluster-Nummer
                          \\     \\Endadresse der Segmentgruppe
                           \\      Startadresse der Segmentgruppe
                            \Bezeichnung der Segmentgruppe
                            Kennung fr Segmentgruppendefinition

        - Dies ist eine typische Code-Cluster-Definition fr eine CPU die nur
          RAM-Speicher besitzt mit einer Adressbreite von 16-Bit. Erm”glicht
          werden damit Code- und Daten-Segmente die in bunter Reihenfolge
          auftreten k”nnen.

                 - Beispiel:   s CODE 0 1000h 0
                              s DATA 1000 2000h 0

        - Dies ist eine Code-Cluster-Definition fr eine CPU die 4k-Byte
          EPROM-Speicher ab  Adresse 0 und 4k-Byte RAM-Speicher beginnend ab
          Adresse 4096 besitzt. Mit dieser Definition werden Code und
          Datensegmente vom Linker sauber getrennt in die jeweils dafr
          vorgesehenen Bereiche.
        - Als weitere Clustergruppe bietet sich bei Mikro-Controllern meist
          das interne RAM an.


                                     - Seite 32 -




             Beispiel:   s INTERN 0 100h 1

        - Damit k”nnen Belegungssegmente fr das interne RAM eines Prozessors
          (hier 256 Bytes ab Adresse 0) erzeugt werden.
        - Zus„tzlich k”nnen noch virtuelle oder Pseudocluster realisiert
          werden.

             Beispiel: Bitsegment beim 8051 !

        - Bitadressen sind sind Werte die den Zugriff auf einzelne Bits im
          normalen internen Speicher des 8051 erlauben. Wenn also der
          Programmierer ein Einzelbit verwendet, muá der Assembler das
          entsprechende Byte im normalen internen RAM reservieren um eine
          doppelte Belegung zu verhindern.

             Realisierung: s BIT 0 80h 2 -> INTERN 20h+@1/8 (@2/8)+1
                           s INTERN 0 100h 3

        - Mit dieser Definition wird jedes Segment vom Bit-Typ automatisch in
          ein Segment der Gruppe Intern gespiegelt. Dabei erfolgt eine
          Offsetanpassung und eine L„ngenanpassung.
        - HASM bildet die Segmente beginnend bei den h”chsten Gruppennummern
          ab. Prinzipiell sind damit nur Abbildungen von h”heren zu
          niedrigeren Gruppennummern m”glich.
        - Um den Assembler m”glichst flexibel zu gestalten ist es m”glich
          (und auch sinnvoll !) einen physikalisch vorhandenen Adressraum
          ber mehrere Namen ansprechen zu k”nnen. Damit der
          Simulator/Monitor nicht alle Namen untersttzen muá wird fr das
          Simulatorfile nur der Name der jeweils ersten Bezeichnung einer
          Clustergruppe verwendet. Das bedeutet, daá der Debugger nur jeweils
          die erste Bezeichnung fr Segmentcluster untersttzen muá.

       ###
       #:# <Doppelpunkt> Quelltextzeilen erzeugen
       ###
        - Alle Zeilen mit Doppelpunkt werden bei jeder Assemblierung mit der
          entsprechenden Tabelle zu Begin in jeden Quelltext eingefgt.
          (Natrlich ohne den Doppelpunkt !)
        - Findet Verwendung um z.B. dem Assembler bestimmte Makros immer mit
          einfgen zu lassen ohne im Quelltext st„ndig selbst dafr sorgen zu
          mssen.

             Beispiel:    Realisierung der USING-Anweisung fr den 80C51
                     :AR0    EQU BankBase
                     :AR1    EQU BankBase+1
                     :AR2    EQU BankBase+2
                     :AR3    EQU BankBase+3
                     :AR4    EQU BankBase+4
                     :AR5    EQU BankBase+5
                     :AR6    EQU BankBase+6
                     :AR7    EQU BankBase+7
                     :Using  Macro Bank
                     :       BSwitch BankBase,Bank
                     :       Endmacro
                     :BSwitch Macro Tmp,Bank
                     :Tmp    Set Bank*8
                     :       Endmacro




                                     - Seite 33 -




       ###
       #t# Eine andere Assemblertabelle importieren
       ###
        - Mit dieser Anweisung kann eine Assemblertabelle aus mehreren Teil-
          Tabellen aufgebaut werden.
          Beispiel: Es gibt eine ganze Reihe von Prozessoren die auf dem
            MCS-51 Kern aufbauen (80517/80535/80552/..). Diese
            Prozessoren besitzen damit alle den gleichen Befehlssatz und
            unterscheiden sich nur in Art und Lage ihrer SF-Register. In so
            einem Fall bietet es sich an den eigentlichen Befehlssatz nur
            einmal anzulegen und ihn fr alle Controller mit dem gleichen
            Kern zu importieren.


      I. HASM Eigenheiten fr einzelne Prozessoren

       Motorola-Assembler fr 68HC11:
          Das Portieren von Programmen vom Motorola-Assembler nach HASM macht
          relativ viel Arbeit, da der Motorola-Assembler vor Kommentar kein
          Kommentarzeichen ben”tigt ! Das Bedeutet, daá alle Kommentare im
          Quelltext mit einem ';' kenntlich gemacht werden mssen !

          Anmerkung des Autors:
            Eine derartige Kommentarm”glichkeit zu implementiern wrde eine
            spezielle 'Motorola'-Betriebsart des Assemblers bedeuten und w„re
            relativ aufwendig in der Realisierung.


      J. Ausgabeformate

       Intel HEX-File: (INH)
        - Dieses Format enth„lt neben <Zeilenvorschub> und <Return> nur
          darstellbare Zeichen und erleichtert damit den Umgang mit diesem
          Format.
        - Alle Zeilen haben folgendes Format:

             :NNAAAARRHHHHHH...HHHHHHHHHCC

        - Dabei sind:

             :         = Kennung Anfang-Zeile
             NN        = Anzahl der Bytes in dieser Zeile (hex)
             AAAA      = Adresse des ersten Elementes in dieser Zeile (hex)
             RR        = Zeilen Typ (hex, 00=Normale Zeile 01=Letzte Zeile)
             HH..      = Datenelemente bestehend aus Hex-Zeichen
                         Datenelemente k”nnenzur Zeit Bytes (2 Hexzeichen)
                         oder Worte (4 Hexzeichen) sein.
                         Maximal k”nnen in einer Zeile 32 Hexzeichen enthalten
                         sein die Daten repr„sentieren.
                         Bei 16-Bit Elementen kommt zun„chst das High- und
             dann
                         das Low-Byte. Da sich die Adressfortschreibung auf
                         Datenelemente bezieht, ist die Adressdifferenz bei
                         vollen Zeilen mit 16-Bit-Elementen 8, w„hrend sie
                         bei 8-Bit Elementen 16 (=10h) ist.
             CC        = Prfsumme zur Fehlererkennung (hex)

        - Den Abschluá eines Files bildet folgende Zeichenkette



                                     - Seite 34 -




             :00000001FF

        - Die Prfsumme wird nach folgender Formel berechnet:

                 Summe =  Byte_anzahl + Adresse_High + Adresse_Low +
                          Zeilentyp + (Summe aller Datenbytes)
             Prfsumme =  ( ( -Summe ) & 0FFh )

        - Der Name des Ausgabefiles ergibt sich aus dem Namen des Quellfiles
          mit der Erweiterung '.HEX'.

       Erweitertes Intel HEX-File: (INHX8M)
       (Fr Datenelemente gr”áer 8-Bit !)
        - Im Gegensatz zum normalen Intel-Hex-File (INH) werden 16-Bit-
          Elemente aufgeteilt in 8-Bit-Elemente. Um bei dieser Verdopplung
          der Elementezahl eine eindeutige Adresszuweisung zu erhalten, wird
          jede 16-Bit-Element-Adresse mit zwei multipliziert. Dadurch erh„lt
          man zwischen zwei aufeinanderfolgenden 16-Bit-Elementen eine freie
          Adresse. Diese freie Adresse wird nun dem High-Teil des
          vorausgegangenen Elementes zugeordnet. Damit sin aber bei den
          Datenelementen Low- und High-Teil gegenber dem normalen INH-Format
          vertauscht (zun„chst LOW dann High-Teil).
        - Der Name des Ausgabefiles ergibt sich aus dem Namen des Quellfiles
          mit der Erweiterung '.HEX'.

       Intel HEX32-File: (INHX32)
        - Enth„lt zus„tzlich zun INHX8M Segmentadressrecords !
        - Der Name des Ausgabefiles ergibt sich aus dem Namen des Quellfiles
          mit der Erweiterung '.HEX'.

       Bin„res Format: (BIN) - Nur Vollversion !
        - Das bin„re Format enth„lt ein direktes Abbild des Codespeichers.
          Startadresse ist immer 0 (Null). Die L„nge des Bin„rfiles h„ngt von
          der Programmgr”áe ab.
        - Unbenutzte Adressbereiche zwischen einzelnen Programmteilen werden
          mit Einsen aufgefllt (0FFh-Bytes / 0FFFFh-Worte). Unbenutzte
          Bereiche mssen dadurch bei EPROM's nicht gebrannt werden, da sie
          im gel”schten Zustand Einsen enthalten.
        - Bytes werden in der Reihenfolge ihrer Adressen ins Bin„rfile
          geschrieben. Bei 16-Bit-Worten wird erst das High-Byte und dann das
          Low-Byte eingeschrieben. (4-Bit Nibbles werden zu ganzen Bytes
          erweitert)
        - Der Name des Bin„rfiles ergibt sich aus dem Namen des Quellfiles
          mit der Erweiterung '.BIN'.


      K. Historie des Programms

       Version 1.0:
        - Fast v”llig Unbrauchbar durch einen in letzter Minute bei einer
          nderung eingebauten Fehler in der Labelsuche.

       Version 1.1:
        - Erste einigermaáen brauchbare Version.

       Version 1.2:
        - Begrenzung der anzuzeigenden Fehlermeldungen.
        - Bei 'MCS-51.TAB' Segmentdefinitionen (von 80c552) eingefgt. (kennt
          damit Bitsegmente)


                                     - Seite 35 -




        - Verschachtelungstiefe bei Makros begrenzt (Stack-Overflow)
        - Quellzeilenauswertung: Befehle auch in Spalte 1 m”glich Label nicht
          unbedingt in Spalte 1
        - Denkfehler bei der šberlappung von Mappingsementen mit normalen
          Segmenten ausgebgelt.
        - In Ausdrcken sind jetzt nahezu beliebig Leerzeichen m”glich.

       Version 1.3:
        - Anwenderdefinierbare Fehlermeldungen eingebaut.
        - Strukturierte Tabellenorganisation (Importieren von Tabellen)

       Version 1.4:
        - Real-Labels bei Tabellenerstellung werden wieder gel”scht !
          (Verursachte unter bestimmten Konstellationen eine Fehlermeldung
          bei der Symboltabellenerstellung)
        - Prefixes '$' und '%' fr Zahlen eingebaut.
        - Symboltabelle im Listing modifiziert und dabei Segmentreferenzen
          Namentlich eingefgt.

       Version 1.5:
        - Beschr„nkung des SIM-Files auf die jeweils erste Cluster-
          Gruppenbezeichnung um den Simulator bersichtlicher ansprechen zu
          k”nnen.

       Version 1.6:
        - Nur nicht leere Segmente ins Objektmodul schreiben Umschalten auf
          dann fehlende Segmente soll trotzdem m”glich sein ohne
          Fehlermeldung (Fehlermeldung nur bei Benutzung !)
        - Bug der bei INCLUDE das falsche Fehlerfile anzeigt behoben.
        - Auf Anregung zus„tzliche HEX-File Formate eingefgt.

       Version 1.7:
        - Groáer Umbau bei Ausdrcken (jetzt Int, Float & String)
        - Schalter systematisiert

       Version 1.8:
        - ??

       Version 1.9:
        - Jetzt auch Datenbytes deren Wert erst beim Linken berechnet wird.
          (is vorher vergessn worn !)
        - Fehlercheck bei 'DB' verbessert
        - 'Lebenoch'-Zeichen nicht mehr so hektisch.
        - Extended Adressierung beim 80c196 nachgebessert ! (ist vorher
          uebersehen worden !)
        - ORG Expression darf jetzt '@0' enthalten (Align) (Dabei gleich
          einen weiteren Fehler beim Linken beseitigt)
        - Nach Hinweis Inkonsistenz zwischen Programm und Dokumentation bei
          #CPU beseitigt (Tabellenname h„tte in " oder < und > eingeschlossen
          werden muessen)
        - Zus„tzliches Ausgabeformat (BIN = Bin„rfile)

       Version 2.1:
        - Fehler in 'get_label()' beseitigt, der dazu fhrte, daá bei
          Sprngen zwischen verschiedenen nicht festen Segmenten Short-JMPs
          eingebaut wurden, die dann bei der Adressberechnung zu nicht
          erfllbaren Forderungen fhrten.




                                     - Seite 36 -








     IV. HSIM Simulator/Monitor


      A. Einleitung

       Zur Programmentwicklung ben”tigt man neben einem Assembler zumindest
       noch eine Funktionsf„hige Platine mit dem gewnschten Prozessor oder
       einen entsprechenden Simulator um die erstellten Programme testen zu
       k”nnen. Im 'lowcost'-Bereich liefern die Hersteller von
       Controllerboards meist eine Art Terminalprogramm welches in
       Zusammenhang mit einem Monitorprogramm (im Eprom des Controllers !)
       ein einfaches Testen des Programms erm”glichen. Source-Level Debugging
       ist mit diesen Hilfmitteln meistens nicht m”glich. Im Profibereich
       (>2-20kMark) werden zum Testen sogenannte In-Circuit-Emulatoren
       angeboten die ber eine spezielle (teuere) CPU das Testen in Echtzeit
       erm”glichen. Source-Leveldebugging ist dabei der Standard. Zum Testen
       bestimmter Programmteile k”nnen auch sogenannte Simulatoren verwendet
       werden. Dieses Hilfmittel arbeitet, weil es den entsprechenden
       Prozessor ohne Hardware nachbildet, in der Regel relativ langsam. Der
       groáe Vorteil ist aber, daá man v”llig ohne Hardware auskommt. 'HSIM'
       erm”glicht zwei unterschiedliche Betriebsarten. Er arbeitet zun„chst
       als Simulator. In dieser Betriebsart kann er bis zu vier Prozessoren
       (gleiche oder unterschiedliche !) gleichzeitig Simulieren. Diese
       Betriebsart ben”tigt keine zus„tzliche Hardware ist aber langsam.
       Daneben kann 'HSIM' auch als eine Art intelligenter Monitor arbeiten.
       In dieser Betriebsart ben”tigt 'HSIM' einen funtionsf„higen Controller
       inclusive Monitorprogramm mit dem er ber eine serielle Verbindung
       kommunizieren kann. Die Art der Kommunikation mit dem Controllerboard
       muss HSIM in der Monitortabelle `.TAM` mitgeteilt werden. Fr den
       Benutzer erscheint in beiden F„llen die gleiche Source-Level-Debug-
       Oberfl„che.




      B. Kommandozeile

       Der Minimal-Aufruf des Simulators lautet:

             HSIM sim_file1 [sim_file2] ..

        - Dieser Aufruf startet den Simulator. Es ist also keine Hardware
          n”tig. In dieser Betriebsart muá die entsprechende Simulator-
          Tabelle vorhanden sein. Die Simulatortabelle muá den selben Namen
          besitzen wie die Assemblertabelle zum bersetzen und linken der
          Quellfiles mit HASM. Lediglich die Erweiterung muá '.TAS' anstelle
          vom '.TAB'sein.
        - 'sim_file' muá ein von HASM erzeugtes Simulator-File (Erweiterung
          '.SIM') sein. HASM erzeut dieses File mit der Kommandozeilenoption:
          /Simulator beim Linken.
        - In der vorliegenden Version von HSIM k”nnen bis zu vier Prozessoren
          gleichzeitig simuliert werden. Deshalb k”nnen nur bis zu vier
          Simulator-Files angegeben werden.

       Der Minimal-Aufruf des Monitors lautet:


                                     - Seite 37 -




             HSIM sim_file1 /Monitor

        - Dieser Aufruf startet den Monitor. In dieser Betriebsart muá an
          einer seriellen Schnittstelle das entsprechende Controllerboard
          ansprechbar sein. Zus„tzlich muá eine fr dieses Board geeignete
          Monitortabelle vorhanden sein (-> Erstellen einer Monitortabelle).
        - 'sim_file' muá ein von HASM erzeugtes Simulator-File sein. (HASM
          erzeut dieses File mit der Kommandozeilenoption: /Simulator)

       Option:

             /EGA oder /VGA

        - Mit einer dieser beiden Optionen kann die Zeilenzahl des
          Bildschirms vergr”áert werden. (/EGA -> 43 Zeilen) (/VGA -> 50
          Zeilen)


      C. Oberfl„che des Simulators/Monitors

       Beim ersten Start von HSIM mit einem Simulatorfile werden automatisch
       folgende Fenster ge”ffnet:

             - Command-Fenster
             - Register-Fenster
             - Variablen-Fenster
             - Source-Fenster

        Command-Fenster: In diesem Fenster befindet man sich nach den
          Starten. Hier k”nnen eine ganze Reihe von Befehlen direkt
          ausgefhrt werden. Eine Liste der verfgbaren Befehle erh„lt man
          mit der Taste 'F1'. (Mit der Taste 'F1' erh„lt man in jedem Fenster
          eine kurze šbersicht der M”glichkeiten in einem Fenster)
        Register-Fenster: Im Registerfenster werden normalerweise alle
          Prozessor-Register dargestellt. Register die sich seit der letzen
          Darstellung ver„ndert haben werden invers hervorgehoben. Der Inhalt
          dieses Fensters ist fr einen Prozessor immer gleich und wird nicht
          durch das Anwenderprogramm gesteuert. Welche Register wie und wo
          dargestellt werden wird in der entsprechenden Konfigurationstabelle
          festgelegt.
        Variablen-Fenster: Im Variablenfenster werden
          anwenderprogrammspezifische Daten und Variablen dargestellt. Dabei
          werden automatisch zun„chst immer nur die aktuellen lokalen
          Variablen angezeigt.
        Source-Fenster: In diesem Fenster wird ein Ausschnitt des aktuellen
          Quellcodes angezeigt. Der aktuelle Stand des Programm-Counters wird
          im Quellcode angezeigt. Eventuell vorhandene Breakpoints werden
          angezeigt.

       Steuern des Programmlaufs:
        'F8'  Durch Drcken der Taste 'F8' kann das Programm in einzelnen
              Schritten ausgefhrt werden. Das gleiche bewirkt auch der Befehl
              'Trace 1' im Command-Fenster. Wenn mehrere Prozessoren geladen
              sind, wird immer ein Schritt beim aktuellen Prozessor ausgefhrt
              (Das kann bei den anderen Prozessoren je nach dem Verh„ltnis der
              Cycluszeiten keinen oder auch viele Schritte bedeuten !).
        'F10' Die Taste 'F10' bewirkt fast das gleiche wie 'F8' nur bei einem
              CALL-Befehl wird die gesamte Routine in einem schnell
              durchlaufen. In der Betriebsart 'Simulator' gibt es auch eine


                                     - Seite 38 -




              Belegung fr SHIFT-'F10'. Damit lassen sich zuvor ausgefhrte
              Einzelschritte wieder zurcknehmen !
        'F5'  Diese Taste bewirkt ein Starten des Programms. Der Simulator
              fhrt nun die Befehle mit der maximal m”glichen Geschwindigkeit
              aus, ohne die Fenster am Bildschirm zu aktualisieren. (Ausnahme
              ist ein eventuell offenes Watch-Fenster !) Im Monitorbetrieb
              fhrt der Controller das Programm jetzt in Echtzeit aus. Wenn
              der Monitor im Controller Interruptgesteuert arbeitet k”nnen
              Ausdrcke in einem eventuell offenem Watchfenster weiter
              aktualisiert werden. Durch erneutes drcken der `F5`-Taste (oder
              eingeben von `Break` im Command-Fenster) kann ein laufendes
              Programm wieder angehalten werden. 'RUN' im Command-Fenster
              bewirkt das Gleiche.
        'F9'  Wenn man sich im Source-Fenster befindet, so kann mit 'F9' ein
              Breakpoint gesetzt oder gel”scht werden.

       Wechseln des Fensters:
       Jedes Fenster besitzt einen Kennbuchstaben der an den Namen des
       Fensters angelehnt ist. 'R' steht fr Registerfenster, 'C' steht fr
       Command-Fenster und 'V' bedeutet Variablenfenster u.s.w. (Ausnahme:
       'I' bedeutet stImulus-Fenster da 'S' schon fr Source-Fenster und 'T'
       schon fr Terminal-Fenster verwendet wird) Durch Drcken von 'ALT' und
       der entsprechenden Fensterkennung wechselt man in das jeweilige
       Fenster.

             ALT-'C' -> Command-Fenster
             ALT-'S' -> Source-Fenster
             ALT-'V' -> Variablen-Fenster
             ALT-'R' -> Register-Fenster
             ALT-'W' -> Watch-Fenster (Nur wenn vorhanden !)
             ALT-'M' -> Memory-Fenster (Nur wenn vorhanden !)
             ALT-'I' -> stImulus-Fenster (Nur wenn vorhanden !)
             ALT-'T' -> Terminal-Fenster (Nur wenn vorhanden !)


       In der Betriebsart Simulator kann mit 'F6' und SHIFT-'F6' das aktuelle
       Prozessormodul gewechselt werden wenn mehrere Prozessoren simuliert
       werden.

       Groesse und Position der Fenster ver„ndern (MOVE/RESIZE)

       Durch Betaetigen der Tastenkombination Alt+`P` gelangt man in den
       Move/Resize-Modus (und wieder zurueck !). In diesem Modus kann das
       aktuelle Fenster mit den Cursortasten verschoben werden. Beim
       veraendern der Groesse haengt es von den zur Cursorsteuerung
       verwendeten Tasten ab, ob man Shift oder Alt (bzw. Ctrl) zusaetzlich
       druecken muss. Wenn man die separaten Cursorsteuertasten verwendet,
       dann muss man im MOVE/RESIZE-Modus zusaetzlich die ALT- (oder CTRL-)
       Taste druecken um die Groesse des aktuellen Fensters zu veraendern.
       Bei Verwendung der Cursortasten im Ziffernblock (NUM-Lock nicht
       aktiv !) muss man zusaetzlich die Shift-Taste gedrueckt halten um die
       Fenstergroesse zu veraendern.
       Die Position und Groesse der Fenster kann auch mittels Maus veraendert
       werden. Zum Verschieben des aktuellen Fensters muá man mit der Maus in
       die Titelzeile des Fensters, die linke Maustaste druecken und
       gedrueckt halten, die gewuenschte neue Position des Fensters mit der
       Maus anfahren und die Maustaste loslassen. Entsprechend verfaehrt man
       mit der rechten unteren Ecke des aktuellen Fensters zur Ver„nderung
       der Fenstergroesse. Durch einmaliges anklicken eines anderen Fensters


                                     - Seite 39 -




       (nicht aktuellen Fensters) wird dieses Fenster zum aktuellen Fenster !
       Damit koennen durch kurzes vorheriges anklicken des Fensters auch alle
       gerade nicht aktuellen Fenster verschoben werden.

       Weitere Tastenbelegungen:

        'ESC' Verlassen eines geoeffneten Diaglogfensters und verlassen des
              Simulators (wenn kein Dialogfenster geoeffnet ist !). VORSICHT:
              Der Simulator ist schneller verlassen als geoeffnet !
        'F1'  Hilfefunktion (Teilweise Kontextsensitiv !)
        'F2'  Wechseln des Sourcemoduls
        'Shift-F2' Sprung im Sourcefenster an einen bereits existierenden
              Breakpoint (ohne daá Programmbefehle ausgefhrt werden !)
        'F3'  Wenn ein Stimulusfile aktiv ist, wird durch F3 das zugeh”rige
              Stimulusfenster Ein- und Ausgeschaltet. Die Abarbeitung des
              Stimulusfiles bleibt davon unberhrt. Beim drcken von F3 ohne
              aktivem Stimulusfile gleiches Verhalten wie beim drcken von
              Shift-F3 !
        'Shift-F3' Anw„hlen und Öffnen eines neuen Stimulusfiles
        'F4'  Wechseln in den Status-Editor. Der Statuseditor kann wie alle
              Dialogfenster mit des ESC-Taste verlassen werden. (Siehe auch ->
              Status-Editor)
        'Shift-F4' Wechseln in den Connection-Editor. In diesem Editor k”nnen
              Verbindungen zwischen mehreren Modulen aber auch
              Hardwareverbindungen einzelner Pins eines Prozessors definiert
              werden. (Verlassen mit <Esc> !) (Siehe auch -> Connection-
              Editor)

       Fensterspezifische Tasten:
        'F7'  Fensterspezifische Funktionstaste
              Terminal-Fenster: Eingabe eines Zahlenwertes der dann als
                    Zeichen interpretiert wird.
        'Tab' Fensterspezifische Belegung

       Optionale Fenster
        Watch-Fenster: Durch den Befehl 'Watch <expression>' kann ein
              Ausdruck als Watch-Ausdruck definiert werden. Das Ergebnis
              dieses Ausdrucks wird dann st„ndig aktualisiert und dargestellt.
              Mit 'Clear Watch' werden alle Ausdrcke im Watch-Fenster
              gel”scht. Mit "Open Watch" und "Close Watch" kann das Watch-
              Fenster ge”ffnet oder geschlossen erden. L”schen eines einzelnen
              Ausdrucks im Watchfenster:
              - Mit Alt-'W' ins Watchfenster wechseln
              - Mit Cursor UP & DOWN den zu l”schenden Ausdruck in die oberste
                    Zeile Bringen
              - 'Del'-Taste bet„tigen
        Memory-Fenster: Durch den Befehl 'Memory <Segment>,<Start-Offset>'
              kann ein Hexdump eines Speicherbereichs angezeigt werden. Mit
              'Close Memory' wird das Memory-Fenster wieder geschlossen.
              ndern der Anzeigeart:
              - Mit Alt-'M' ins Memoryfenster wechseln
              - Mit 'Tab' die Anzeigeart umstellen
        Stimulus-Fenster: In diesem Fenster wird der momentane Zustand aller
              Stimulusleitungen, der Zeitpunkt des n„chsten Wechsels und der
              n„chste Zustand angezeigt. Das Stimulusfenster kann mit der
              Taste F3 Ein- und Ausgeschaltet werden. Allerding muá zuvor ein
              Stimulus-File ge”ffnet werden.(siehe auch -> Stimulus-File)
        Terminal-Fenster: Das Terminalfenster kann mit dem Befehl
               OPEN TERMINAL ge”ffnet und mit dem Befehl


                                     - Seite 40 -




               CLOSE TERMINAL geschlossen werden.(->Terminal-Fenster)
              Weitere Funktionen kann man der Onlinehilfe entnehmen !
              Erreichbar ist diese Hilfefunktion ber die Taste 'F1' im
              Terminalfenster oder durch den Befehl 'Hilfe Terminal' im
              Commandfenster.


      D. Stimulus-File

       Mit Hilfe des Stimulusfiles kann man jedem Prozessor Hardwaresignale
       an seinen Portanschlssen vorgeben. Zu diesem Zweck muá in einem
       Stimulusfile der zeitliche Verlauf jedes vorzugebenden Signals
       festgelegt werden.
       In der ersten Zeile des Files muá festgelegt werden welche Signale des
       Prozessors vorgegeben werden sollen. Die einzelnen Ausdrcke mssen
       durch Leerzeichen voneinander getrennt sein.

             Beispiel:  P1.0 P1.1 P3


       In diesem Beispiel sollen zwei Einzelleitungen (Bit:0 und Bit:1 des
       Port1) und ein ganzer Port (Port3) in seinem zeitlichem Verlauf
       vorgegeben werden. Natrlich drfen als Vorgabewerte fr
       Einzelleitungen nur einstellige Dualzahlen verwendet werden. Bei P3
       muá ein ganzes Byte vorgegeben werden.
       Wenn in der ersten Zeile, wie in diesem Beispiel, drei Signale
       vorgegeben werden sollen mssen alle folgenden Zeilen im Stimulusfile
       vier Zahlen enthalten. Die folgenden Zeilen mssen also alle einen
       Wert mehr enthalten wie in der ersten Zeile als Vorgabewerte
       festgelegt wurde. Der eine zus„tzliche Wert in jeder Zeile ist der
       erste Wert. Dieser zus„tzliche Wert kann im Gegensatz zu allen anderen
       Werten eine Floatzahl sein, da sie die Zeit vorgibt ab der die in der
       gleichen Zeile stehenden Werte am Prozessor anliegen sollen. Die Zeit
       wird immer in Sekunden angegeben. ACHTUNG die Cycluszeit eines
       Prozessors ist defaultm„áig auf eine Sekunde festgelegt !! ndern im
       Statusfenster (F4) !
       Als Beispiel k”nnte ein Stimulusfile so aussehen:

                     P1.0 P1.1 P3
                 0    0    1    5
                 10   1    1    4
                 20   0    0    3
                 30   1    0    2
                 40   0    1    1
                 50   1    1    0


       Nach jeweils 10 Sekunden „ndert sich das Siganl an P1.0
       Nach jeweils 20 Sekunden „ndert sich das Signal an P1.1
       Das Byte an P3 z„hlt im 10 Sekunden Takt von 5 bis 0 herunter

       Ein derartiges File kann mit dem Befehl

             Stimulus <Filename>


       in einer Simulation verwendet werden. Wenn der Befehl ohne Filename
       ausgefhrt wird, wird ein eventuell aktives Stimulusfile beendet. Man
       kann auch direkt von einem Stimulusfile auf einanderes Stimulusfile


                                     - Seite 41 -




       berwechseln. Die Taste Shift-F3 fhrt auch den Stimulus-Befehl aus,
       indem vorher nach einem Stimulusfilenamen gefragt wird. Nach
       Ausfhrung des Befehls Stimulus <Filename> (oder Shift-F3) wird
       automatisch ein Stimulusfenster ge”ffnet, welches die Zust„nde der
       Stimulusleitungen veranschaulicht, den Zeitpunkt der n„chsten und den
       Zustand nach der n„chsten nderung.
       Durch mehrmaliges Bet„tigen der Taste F3 kann dieses Fenster sehr
       schnell ein- und ausgeschaltet werden. Die Tatsache ob das Stimulus-
       Fenster sichtbar ist oder nicht „ndert nichts an der Abarbeitung des
       Stimulusfiles. Nachdem das gesamt File abgearbeitet worden ist, wird
       eine entsprechende Meldung im Commandfenster ausgegeben und das File
       geschlossen.
       Jedes Stimulusfile kann zu beliebiger Zeit der Programmausfhrung
       aktiviert werden. der Zeitpunkt zu dem das Stimulusfile ge”ffnet wird,
       ist der Zeitnullpunkt im Stimulusfile. Man kann also insbesondere ein
       Stimulusfile auch mehrmals hintereinander abarbeiten.


      E. Terminal-Fenster

       Mit Hilfe des Terminalfensters k”nnen auf der einen Seite
       alphanumerische Ausgaben eines Controllers sichtbar gemacht werden und
       auf der anderen Seite Tastatureingaben an einen Controller
       weitergeleitet werden.

               ____________          ___Terminalfenster__________
              /            \        /                            \
              | Controller |        | Anzeigen der von           |
              |   mit      |------->| Controller gesendeten      |
              | Programm   |        |     Zeichen                |
              |            |<-------| Abschicken von Tastatur-   |
              |            |        | eingaben an den Controller |
              \____________/        \____________________________/
                               /\
                               ||
             Hin- und Rckverbindung des Terminalfensters an den Controller


       Derartige Verbindungen werden von sogenannten Monitorprogrammen
       (Talkern) auf der Controllerseite benutzt um Informationen mit einem
       PC (und damit HSIM als Monitor) auszutauschen. Um ein Terminalfenster
       vollst„ndig an einen Controller anzuschlieáen mssen insgesamt vier
       Aktionen vorbereitet werden.

        1.Der Controller muá ein Zeichen an das Terminalfenster
          bermittelnk”nnen.
        2.Es muá ein Busy-Ready-Mechanismus fr das šbermitteln eines
          Zeichens an das Terminalfenster angelegt werden.
        3.Es muá ein Busy-Ready-Mechanismus fr das šbermitteln eines
          Zeichens vom Terminalfenster an den Controller angelegt werden.
        4.Das Terminalfenster muá ein Zeichen (Tastatureingabe) an den
          Controller bermitteln k”nnen (Wenn dieser das letzte Zeichen
          bereits verarbeitet hat).

        zu 1.: Das šbermitteln eines Zeichens an das Terminalfensters wird
          direkt zur Laufzeit im Simulator gemacht. Man erstellt mit Hilfe
          des Connektion-Editors eine Verbindung zwischen der Speicherstelle
          (oder Register), in der der Controller die Ausgabebytes
          hineinschreibt und dem Terminalfenster. Voraussetzung ist nur, daá


                                     - Seite 42 -




          die entsprechende Speicherstelle in der Simulatortabelle als
          Ausgabeport vorbereitet ist.

             BEISPIEL aus 68HC11.TAS:   Ipo 0@-1 ?XData
             (Diese Zeile erlaubt es alle Speicherstellen im Segment 0 als
             Ausgabeports zu verwenden, eine besondere Maánahme ist damit
             nicht mehr n”tig !)

        zu 2.: Nach jedem Zeichen welches vom Terminalfenster
          entgegengenommen wurde, wird die Funktion 'SERIAL_ACCEPT' einmal
          aufgerufen. Mit Hilfe dieser Funktion k”nnen Flags bedient werden
          die dem Controller signalisieren, daá das Zeichen vom
          Terminalfenster richtig empfangen wurde. Die Funktion muá auch dann
          angelegt werden wenn der Mechanismuá selbst nicht verwendet wird.
          Eigentlich w„re in dieser Richtung kein vollst„ndiger Busy-Read-
          Mechanismus n”tig, da durch den Simulator gew„hrleistet ist, daá
          ein an das Terminalfenster abgeschicktes Byte immer sofort abgeholt
          wird und somit in dieser Richtung kein Stau entstehen kann. Es sein
          denn, das Terminal Fenster ist nicht ge”ffnet !

        zu 3.: Wenn der Controller ein entsprechendes Flag zur Verfgung
          stellt an dem erkannt werden kann ob das letzte, an den Controller
          bermittelte Zeichen bereits abgeholt (verarbeitet) wurde, so kann
          dieses Flag dem Simulator ber die Funktion 'SERIAL_BUSY'
          zug„nglich gemacht werden. Diese Funktion muá einen von 0
          verschiedenen Wert zurckliefern wenn das letzte Byte vom
          Controller noch nicht abgeholt wurde. Wenn dieser Mechanismuá nicht
          benutzt werden soll oder vom Controller nicht zur Verfgung
          gestellt wird, so muá die Funktion trotzdem angelegt werden und sie
          sollte dann den Wert 0 zurckliefern. In diesem Fall kann das
          Terminalfenster aber nicht erkennen ob das letzte abgeschickte
          Zeichen bereits vom Controller verarbeitet worden ist oder nicht
          und berschreibt damit Zeichen die m”glicherweise noch nicht
          verarbeitet worden sind.

        zu 4.: In dieser Richtung muá eine Funktion mit dem Namen
          'SERIAL_SEND' in der Simulatortabelle definiert werden. Die
          Variable 'in' enth„lt das zu bermittelnde Zeichen. Gleichzeitig
          k”nnen (oder mssen) innerhalb dieser Funktion alle entsprechenden
          Flags des Controllers bedient werden die die Eingabe steuern. Dazu
          z„hlen vor allem Flags die dem Controller mitteilen, daá ein
          Zeichen angekommen ist und/oder ein Interruptflag welches die
          Ausfhrung einer Empfangsroutine ausl”st.

       Das Terminalfenster wurde speziell zur Entwicklung und dem Test von
       Monitorprogrammen (Talkern) auf der Controllerseite eingebaut. Diese
       Programmteile k”nnen n„mlich auch sp„ter wenn HSIM in Verbindung mit
       einer entsprechneden Hardware im Monitor-Modus l„uft nicht mehr
       'berwacht' oder getestet werden, da sie vollst„ndig transparent im
       Hintergrund ablaufen (sollten !).

       VERSCHICKEN VON ZEICHEN DIE NICHT šBER TASTATUR ERREICHBAR SIND:
       In vielen F„llen ist es n”tig, Zeichen oder besser Bytes zu
       bertragen, die nicht direkt ber die Tastatur eingegeben werden
       k”nnen oder vom Simulator fr andere Zwecke vorbelegt sind. Die
       fensterspezifische Funktionstaste F7 erlaubt, wenn sie in einem
       Terminalfenster gedrckt wird das Eingeben beliebiger Werte als
       Ausdruck, die dann als ein Zeichen interpretiert und an den Controller
       geschickt werden.


                                     - Seite 43 -




      F. Status-Editor

       Der Statuseditor wird mit der Taste F4 aufgerufen. Nach dem Aufruf
       werden alle wichtigen Zust„nde und Einstellungen des jeweiligen
       Prozessormoduls angezeigt. Alle Eintr„ge die mit einem '+'
       (Pluszeichen) beginnen k”nnen ver„ndert werden.
       Zum Ver„ndern eines Wertes w„hlt man mit den Cursortasten UP & DOWN
       die Zeile aus die ge„ndert werden soll und gelangt dann mit der
       Return-Taste in den nderungsmodus. Nun kann der neue Wert eingegeben
       werden. Beenden des nderungsmodus wieder mit der Return-Taste. Im
       Statuseditor kann man mit den Cursortasten LINKS & RECHTS sehr schnell
       das angezeigte Modul wechseln (natrlich nur wenn mehrere Module
       geladen sind).

        Cycluszeit: In dieser Zeile steht die Zeit die der betreffende
              Prozessor fr einen Cyclus ben”tigt. Man kann hier die
              Cycluszeit in Sekunden eingeben, dann liefert die Lokalzeit
              immer einen absoluten Wert in Sekunden. Wenn man die
              Standardeinstellung beibeh„lt, liefert die Lokalzeit die Anzahl
              der vergangenen Cyclen des Prozessors. Bei einem Prozessor hat
              der Wert nur einen Einfluá auf das eventuell verwendete
              Stimulusfile, denn die Lokalzeit ist die Referenzzeit fr die
              Weiterschaltung des Stimulusfiles. Das bedeutet, daá die
              Cyclenzeit auf das Stimulusfile abgestimmt sein muá. Wenn
              mehrere Prozessoren simuliert werden, so legen die Cyclenzeiten
              das Verh„ltnis der Ausfhrungsgeschwindigkeiten fest (z.B.: Zwei
              gleiche Prozessoren; Cycluszeit_A=1; Cycluszeit_B=3; Bedeutet,
              daá der 2.Prozessor 3-mal so schnell l„uft wie der erste). Bei
              unterschiedlichen Prozessoren muá in der Regel mindestens ein
              Cyclenwert angepasst werden, damit es bestimmten realen
              Verh„ltnissen entspricht.

        Autoload: Dieser Wert ist praktisch immer auf ON und besagt, daá das
              betreffende Prozessormodul beim n„chsten mal automatisch geladen
              wird. Wenn nur ein Prozessor simuliert wird, so ist diese Angabe
              praktisch bedeutungslos. Wenn mehrere Module geladen sind, so
              kann man durch umstellen dieses Wertes auf OFF den Simulator
              dazu veranlassen ein bestimmtes Modul beim n„chsten Start nicht
              mehr zu laden. (weitere Infos -> Simulieren mehrerer
              Prozessoren)

        Stimulus: (und die auf Stimulus folgende Zeile !) Diese beiden Zeilen
              zeigen den Stimuluszustand. In der ersten Zeile wird nur
              angezeigt ob momentan ein Stimulusfile aktiv ist (ON) oder nicht
              (OFF). Die zweite Zeile zeigt den Namen des Stimulusfiles.

        Debugmode: (Ist noch nicht vollst„ndig implementiert !)
              INTERFACE: Zeigt alle internen Aufrufe von Interfacefunktionen
                    im Commandfenster an.
              BAUM: Zeigt die Ausgefhrte Funktion jedes Befehls im
                    Commandfenster an. (Nur wenn nicht OVERLAY !)

        Terminal: Zeigt den Status des Terminalfensters an.
              LEcho: Lokales Echo aller im Terminalfenster eingegebener
                    Zeichen (sonst kein lokales Echo !)
              Hexmode: Nicht darstellbare Zeichen werden hexadezimal angezeigt
                    (sonst Dezimal) Default ist kein Lokales Echo und dezimale
                    Darstellung !



                                     - Seite 44 -




        AktualizeOnRun: Mit diesem Parameter koennen zusaetzlich zum Watch-
              Fenster noch weitere Fenster in ihrer Prioritaet angehoben
              werden, so dass sie w„hrend des Programlaufs st„ndig
              aktualisiert werden. Durch bet„tigen einer der Tasten `s`
              (=Source), `m` (=Memory), `r` (=Register), `v` (=Variablen) oder
              `i` (=Stimulus) auf dieser Zeile kann die Prioritaet des
              betreffenden Fensters veraendert werden (Toggel !). Online-Hilfe
              durch `F1` auf dieser Zeile !

        Overlay: Wenn in dieser Zeile eine von 0 verschiedene Zahl steht, so
              ist beim betreffenden Modul ein Overlay aktiv. Wenn ein Overlay
              aktiv ist, so werden nderungen die man in einer
              Simulatortabelle am Befehlsbaum oder an den Cyclenanweisungen
              macht nicht Wirksam, da die im Overlay befindlichen Teile
              verwendet werden. Ein vorhandenes Overlay kann durch
              auskommentiern der 'O'-Zeile in der Simulatortabelle stillgelegt
              werden. (-> Erstellen einer Simulatortabelle !)

       Die restlichen Zeilen sind nur fr den Autor von Interesse und k”nnen
       von einer Programmversion zur n„chsten ihr Aussehen ver„ndern !


      G. Synchrones Simulieren mehrerer Prozessoren

       Mit HSIM ist es m”glich mehrere Prozessoren synchron zu simulieren und
       damit auch eventuelle Wechselwirkungen zwischen ihnen (->Connections).

       STARTEN EINER MULTIPROZESSORSIMULATION:
       Zu diesem Zweck bersetzt man zun„chst alle Programme der beteiligten
       Prozessoren getrennt, so daá fr jeden zu simulierende Prozessor ein
       abzuarbeitendes Programm ('.SIM'-File) bereitliegt. Nun startet man
       den Simulator ganz normal mit EINEM der bereitligenden Programme. Alle
       weiteren Programme (Prozessoren) l„d man nun nacheinander mit dem
       Befehl 'LOAD <SIM-Name>' im Commandfenster. Fr jedes Modul erscheinen
       beim Laden zumindest das entsprechenden Source-Fenster. Weitere
       Fenster k”nnen bei Bedarf mit OPEN <Fenstername> ge”ffnet werden.
       Dabei ist darauf zu achten das Fenster mit dem entsprechenden Befehl
       nur fr das aktuelle Modul (Prozessor) ge”ffnet werden k”nnen.

       ABARBEITUNGSGESCHWINDIGKEIT DER PROZESSOREN:
       Wenn nur ein Prozessor simuliert werden soll, so spielt es in der
       Regel keine Rolle welche Zeit, absolut gesehen, w„hrend eines
       Prozessorcycluses vergeht (Ausnahme Stimulusfile). Wenn mehrere
       Prozessoren gleichzeitig synchron simuliert werden sollen muá hingegen
       genau festgelegt werden wie schnell jeder Prozessor laufen soll oder
       zumindest in welchem Verh„ltnis die Geschwindigkeiten zueinander
       stehen sollen. Um die absolute Geschwindigkeit eines Prozessors
       festzulegen muá im Statuseditor (Taste F4) die Cycluszeit eingestellt
       werden. Defaultm„áig ist dieser Wert auf eine Sekunde pro Cyclus
       eingestellt. Die Cycluszeit eines Prozessors ergibt sich aus der
       Taktfrequenz des Prozessors und der Anzahl an Taktcyclen die der
       betreffende Prozessor fr einen Maschinencyclus ben”tigt.

        Beispiel: Der MCS-51 Kern des 80C552 ben”tigt 12 Taktcyclen fr einen
              Maschinencyclus, bei einer Taktfrequenz von 6 MHz ergibt sich
              damit eine Cycluszeit von 5us die im Statuseditor eingestellt
              werden muá !




                                     - Seite 45 -




       AKTUELLES MODUL:
       Im Zusammenhang mit der Simulation mehrerer Module bekommt der linke
       untere Rand des Command-Fensters eine wichtige Rolle. Er zeigt n„mlich
       das aktuelle Modul in Form einer Zahl an. Durch das Laden der weiteren
       Module „ndert sich nichts am aktuellen Modul es sollte weiterhin auf
       'Modul 0' stehen. Mit der Taste F6 kann man zyklisch alle Module zum
       aktuellen Modul machen. Aktuelles Modul bedeutet so viel wie oberstes
       oder gerade vom Anwender zug„ngliches Modul. Alle Modulspezifischen
       Befehle beziehen sich auf dieses aktuelle Modul. Beispielsweise das
       setzen eines Breakpoints mit der Taste F9 zeigt im Programmtext des
       aktuellen Moduls die gewnschte Wirkung. Auch der mit Shift-F4
       aufzurufende Connection-Editor zeigt immer nur die Verbindungen des
       aktuellen Moduls an (Alle Verbindungen werden in dem Modul verwaltet
       in dem sie ihr(e) Output-Pins besitzen). Das Erzeugen eines
       Watchausdruckes bezieht sich auf das aktuelle Modul u.s.w.

       SINGEL-STEP:
       Das drcken der Taste F10 (oder F8) fr einen Einzelschritt aus im
       Bezug auf das aktuelle Modul. Abh„ngig von der relativen
       Geschwindigkeit der Prozessoren kann dies Bedeuten, daá bei einem
       anderen Prozessor mehrere oder auch berhaupt kein Schritt ausgefhrt
       wird. Breakpoints machen sich natrlich in jedem Fall, auch in einem
       anderen Modul bemerkbar !

       DAS LADEN EINES MODULS RšCKGNGIG MACHEN:
       Es ist keine M”glichkeit vorhanden ein geladenes Modul direkt wieder
       zu l”schen !
        ABER: Im Status-Editor gibt es eine Zeile mit der Bezeichnung
          'Autoload', die normalerweise auf 'ON' steht. Durch Anw„hlen dieser
          Zeile im Status-Editor und bet„tigen der RETURN-Taste kann dieser
          Wert auf 'OFF' umgestellt werden. Wenn nun der Simulator verlassen
          und wieder neu gestartet wird, so wird das betreffende Modul nicht
          wieder automatisch geladen. Es k”nnen beim erneuten Starten
          Fehlermeldungen auftreten, die Bezug nehmen auf das nun fehlende
          Modul. Diese Fehlermeldungen mssen mit SPACE Quittiert werden.

       VERBINDUNGEN MIT DER UMWELT:
       Bei HSIM gibt es zwei Arten von Verbindungen zur Umwelt:
        - Verwenden eines Stimulusfiles (-> Stimulus-Files)
        - Anbingen von Verbindungen zwischen mehreren Modulen
          (-> Connection-Editor)


      H. Connection-Editor

       Wie der Name (Connection = Verbindung) schon sagt geht es an dieser
       Stelle um einen Editor mit dem man Verbindungen herstellen, l”schen
       oder „ndern kann. Dies ist besonders dann von Interesse wenn mehrere
       Prozessoren gleichzeitig simuliert werden oder Ausgaben an einen
       Portbaustein simuliert werden sollen.

             Beispiel 1:
                     Bei einem Prozessor ist einer seiner Ausgangspins
                     mit einem seiner Eingangspins direkt verbunden.

                     Prozessor A           Prozessor A
                      Output-Pin ---------> Input-Pin




                                     - Seite 46 -




       In diesem Beispiel muá, wenn der Programmierer auf das Output-Pin
       zugreift und dessen Zustand „ndert, im selben Moment auch der Pegel
       des Inputpins seinen Zustand „ndern. Es ist also eine 'Hardware'-
       Verbindung (Connection) notwendig.

             Beispiel 2:
                   Es werden zwei Prozessoren simuliert bei denen der eine
                   Prozessor dem anderen ber eine Hardwareverbindung ein
                   Signal (Interrupt ?) geben soll um dort eine Aktion
                   auszul”sen.

                   Prozessor A           Prozessor B
                    Output-Pin ---------> Input-Pin
                                        (kann auch Interrupteingang sein)


       In diesem Beispiel muá, wenn der Programmierer auf das Output-Pin des
       Prozessors A „ndert das Inputpin des Prozessors B auch seinen Zustand
       „ndern. Es ist wiederum eine 'Hardware'-Verbindung (Connection)
       notwendig.

       Zum Herstellen solcher Hardwareverbindungen ist der Connection- Editor
       da. Alle Pins die beim jeweiligen Prozessor als Ausgabepins dienen
       k”nnen (und in der Tabelle als solche angemeldet sind) k”nnen im
       Connectioneditor mit allen anderen Eingabepins aller Prozessoren in
       der Simulation verbunden werden.

       Um das Vorgehen zum Erzeugen einer Verbindung im Connectioneditor zu
       erkl„ren, soll die Verbindung des Beispiels 1 erzeugt werden.

        1.zun„chst muá der Simulator normal gestartet werden

             ('HSIM <SRC-File>')

        2.Starten des Connection-Editors mit Shift-F4 (oder Befehl 'Connect'
          im Command-Fenster). Wenn die Tastenbelegung nicht bekannt ist dann
          'Tasten' im Command-Fenster eingeben und F1 drcken (oder 'Hilfe
          Tasten' und dann Return). Man gelangt damit, da noch keine
          Verbindung definiert ist, automatisch in ein Auswahlfenster in dem
          alle Output-Pins des simulierten Prozessors angezeigt werden.

        3.Mit Hilfe der Cursortasten UP & DOWN das gewnschte Output-Pin
          ausw„hlen und dann mit Return best„tigen. Wenn es sich beim
          gew„hlten Output-Pin direkt um ein Einzelpin gehandelt hat, so geht
          es jetzt weiter mit Punkt 7. Im anderen Fall handelt es sich um
          eine Dummybezeichnung die fr alle Arten von Memorymapped I/O
          verwendet werden kann. In diesem Fall geht es mit dem n„chsten
          Punkt (Punkt 4) weiter.

        4.Nun muá die Memoryadresse angegeben werden unter der sich das
          Ouput-Port befinden soll. Der zul„ssige Adressbereich wird im Titel
          des Fensters angezeigt ! Wenn die gewnschte Adresse nicht
          angew„hlt werden kann, so ist das nur ein Hinweis darauf, daá in
          der Simulatortabelle fr den verwendeten Prozessor nicht der
          gesamte m”gliche Adressraum des Prozessors allokiert wird. Nachdem
          eine gltige Memoryadresse angegeben und mit Return best„tigt wurde
          gelangt man in die Startbit-Auswahl.




                                     - Seite 47 -




        5.Startbitauswahl: Memorymapped bedeutet daá an der angegebenen
          Adresse immer mehrere (meist 8) Bits angesprochen werden k”nnen.
          Mit dem Startbit wird die Bitposition ausgew„hlt die als Output-Pin
          dienen soll (LSB=0).

        6.Wem bei Punkt 5 die Bezeichnung Startbit fr das auszuw„hlende Bit
          etwas komisch vorkommt der hat im Prinzip recht, bekommt jetzt aber
          die Erkl„rung. Bei Memorymapped I/O Verbindungen k”nnen neben
          Einzelbits in einer 'Verbindung' auch mehrere Leitungen
          gleichzeitig gezogen werden. Voraussetzung ist allerdings, daá sie
          auf einer Adresse und dort direkt nebeneinander liegen, sowohl im
          Output-Prozessor als auch im Input-Prozessor ! Bei
          Einzelverbindungen muá die Anzahl der Bits somit auf 1 gesetzt
          werden. Wenn mehrere Bits ausgew„hlt werden, so ist das unter
          Startbit angegebene Bit das erste und die in Richtung h”herer
          Bitpositionen folgenden sind die weiteren.

        7.Nun erscheint ein Auswahlfenster welches alle gerade im Simulator
          befindlichen Module zur Auswahl anzeigt. Man kann das gewnschte
          Modul ausw„hlen mit dem eine Verbindung hergestellt werden soll.
          Wenn, wie in diesem Beispiel, nur ein Modul geladen ist, so wird
          auch nur dieses angezeigt und kann direkt mit Return best„tigt
          werden.

        8.Nun wird ein Auswahlmen angezeigt, welches alle Inputpins des
          gew„hlten Moduls enth„lt. Das gewnschte Modul ausw„hlen und mit
          Return bet„tigen. An dieser Stelle ist auch eine Dummybezeichnung
          m”glich die fr Memorymapped I/O steht und fr eine vollst„ndige
          Festlegung noch eine Adresse und ein Startbit ben”tigt (Anzahl der
          Bits ist bei der Outputdefinition bereits festgelegt worden).

        9.Man hat nun eine Verbindung vollst„ndig definiert und befindet sich
          im Connectioneditor. Das Fenster zeigt alle Verbindungen an die
          definiert sind. Mit den Cursortasten UP & DOWN kann man eine
          Verbindung ausw„hlen und mit der DEL-Taste l”schen. Eine weitere
          Verbindung kann man definieren indem man mit der DOWN-Taste so weit
          nach unten geht bis man im Auswahlmen fr das Output-Pin ist und
          dann weiter wie unter Punkt 3 verf„hrt.

       Der Connection-Editor kann beliebig mit Shift-F4 aufgerufen und mit
       ESC verlassen werden ohne daá sich etwas an den Verbindungen „ndert.

       Einmal definierte Verbindungen bleiben auch beim Verlassen des
       Simulators erhalten.


      I. Scriptfiles

       Mit Scriptfiles (=Tastaturmakros) lassen sich Tastenfolgen, die immer
       wieder ben”tigt werden, sehr einfach in ein File (Scriptfile) ablegen
       und anschlieáend beliebig oft (auch innerhalb eines anderen Script-
       Files) wiederholen.
       Angenommen man will sich w„hrend der Programmentwicklung einen 100
       Zeichen langen Bereich im Codespeicher ab Adresse 1200h immer wieder
       in das File f:\Testdat.dat ablegen. Dazu ist der Befehl

             Datfile Code(IN),1200h,100,f:\Testdat.dat




                                     - Seite 48 -




       n”tig. Damit man nicht jedesmal diese lange Befehlszeile eingeben muá,
       kann man sich ein entsprechendes Scriptfile anlegen. Aus naheliegenden
       Grnden w„hlt man fr das Scriptfile einen kurzen Namen. Hier im
       Beispiel soll der Name 'DF' verwendet werden. Um das Scriptfile zu
       erzeugen geht man folgendermaáen vor. Zun„chst wird mit der Zeile

             Createscript DF<Ret>


       die Scriptaufzeichnung gestartet. Im Statusbereich (rechter unterer
       Rand) des Commandfensters sind jetzt die Zeichen 'Sw' fr
       (S)cript(w)rite zu sehen. Sie signalisieren, daá momentan alle
       Tastendrcke aufgezeichnet werden.
       Der gewnschte Inhalt des Scriptfiles kann nun direkt angegeben
       werden. Hier im Beispiel mit der Zeile:

             Datfile 0,1200h,100,f:\Testdat.dat<Ret>


       Um die Scriptaufzeichnung zu beenden muá nun die <ESC>-Taste bet„tigt
       werden. Die Zeichen 'Sw' im Statusbereich des Commandfensters sind
       jetzt wieder verschwunden.
       Es existiert nach dieser Prozedur im aktuellen Verzeichnis ein File
       mit dem Namen 'DF.SCR'.
       Ausgefhrt wird das Scriptfile mit der Zeile

             Script DF


       Da der Name des erzeugten Scriptfiles nicht mit einem Simulatorbefehl
       identisch ist, kann das Scriptfile auch mit der Zeile

             DF<Ret>


       ausgefhrt werden. Es handelt sich beim definierten Scriptfile also
       praktisch um einen neuen Simulatorbefehl. Dies kann man auch dazu
       benutzen, sich Abkrzungen fr Simulatorbefehle zu definieren. Um z.B.
       alle Ausdrcke im Watchfenster zu l”schen muá man den Befehl:

             clear watch


       eingeben. Die Abkrzung 'cw' fr diesen Befehl erzeugt man mit der
       folgenden Vorgehensweise:

             createscript cw<Ret>
             clear watch<Ret>
             <Esc>


       Jetzt kann der Befehl 'clear watch' mit der Tastenfolge 'cw<Ret>'
       ausgel”st werden.
       Es k”nnen auch Scriptfiles erstellt werden, die beim Aufrufen
       Argumente erwarten. Dazu geht man folgendermaáen vor. Als Beispiel
       soll der Befehl 'Watch <Expression>' durch die Befehlszeile 'w
       <Expression>' aufgerufen werden k”nnen.
       Beim Starten der Aufzeichnung fr ein Scriptfile mit Argumenten mssen



                                     - Seite 49 -




       gleich, entsprechend der gewnschten Anzahl an Argumenten, Argumente
       angegeben werden. Fr das Beispiel kann die Befehlszeile

             createscript w 0@0<Ret>


       verwendet werden. Der Ausdruck '0@0' steht dabei fr ein beliebiges
       gltiges Argument welches w„hrend der Aufzeichnung des Scripts als
       tats„chliches Argument verwendet werden kann.
       Jetzt wird der abzukrzende Befehl angegeben.

             watch


       Dabei darf das eine Leerzeichen direkt nach 'watch' nicht vergessen
       werden. An der Stelle an der das Argument eingesetzt werden soll,
       drckt man einfach die fensterspezifische Funktionstaste F7. Dadurch
       wird ein Fenster ge”ffnet welches alle vorhandenen Argumente zur
       Auswahl anbietet. Man w„hlt mit den Cursorsteuertasten das gewnschte
       Argument aus und best„tigt es mit der <Ret>-Taste. Das ausgew„hlte
       Argument wird automatisch in der Commandozeile eingefgt. Bei der
       Abarbeitung des Scriptfiles wird sp„ter natrlich das jeweils aktuelle
       Argument eingefgt. Der Cursor steht im Beispiel jetzt am Ende der
       Commandozeile mit folgendem Inhalt:

             watch 0@0


       Durch Bet„tigen der <Ret>-Taste wird der Befehl schlieálich noch
       ausgefhrt. Abschlieáend wird mit des <Esc>-Taste die
       Scriptaufzeichnung beendet. Das Scriptfile ist jetzt bereit zur
       weiteren Benutzung. Der Aufruf erfolgt entweder durch

             script w <Expression>


       oder durch

             w <Expression>


       Bei solchen Scriptfiles k”nnen natrlich auch meherere Argumente
       verwendet werden. Eine Begrenzung erfolgt durch die Maximall„nge von
       255 Zeichen fr Commandzeilenbefehle. Ein einmal definiertes
       Scriptfile kann bis zu einer Schachtelungstiefe von 8 auch innerhalb
       anderer Scriptfiles verwendet werden. Diese werden nicht in das
       aktuelle Scriptfile eingefgt, sondern aufgerufen. Das hat zur Folge,
       daá sp„tere nderungen an Scriptfiles, die innerhalb von anderen
       Scriptfiles verwendet werden sich auch dort sofort auswirken.
       Grunds„tzlich werden Scriptfiles immer lokal erzeugt. Das bedeutet,
       daá sie nur innerhalb des aktuellen Projekts direkt verwendet werden
       k”nnen.

       Bei der Ausfhrung von Scriptfiles wird, wenn kein Pfad angegeben ist,
       zuerst im aktuellen Verzeichnis nach dem gewnschten Scriptfile
       gesucht. Wenn das gewnschte Script dort nicht existiert, wird im
       Unterverzeichnis 'HASM\SCRIPT' nach dem gewnschtenScript gesucht.
       Damit k”nnen 'Globale' Scriptfiles definiert werden, die in allen
       Projekten verfgbar sind. Solche global verfgbaren Scriptfiles mssen


                                     - Seite 50 -




       sich in diesem Verzeichnis ('HASM\SCRIPT') befinden.
       Durch verschieben oder kopieren eines lokal erstellten Scriptfiles in
       das Unterverzeichnis 'HASM\SCRIPT' wird ein lokales Scriptfile global
       verfgbar. Dieses Scriptfile kann danach global, das heiát von allen
       Projekten verwendet werden. Dies ist insbesondere fr
       Befehlsabkrzungen von Vorteil.
       Aus der Suchreihenfolge ergibt sich, daá ein Projektbezogenes
       Scriptfile ein eventuell vorhandenes 'globales' Scriptfile verdr„ngt.
       Beim Starten des Simulators wird immer automatisch das Scriptfile mit
       dem Namen 'Autoexec.SCR' ausgefhrt. Allerdings nur, wenn dieses
       vorhanden ist.

       Es gibt zwei 'Befehle' die im Zusammenhang mit Scriptfiles von
       besonderer Bedeutung sind. Zum einen k”nnen beliebige Zeilen, die mit
       einem '>' beginnen als 'Befehl' abgeschickt werden, ohne daá eine
       Fehlermeldung generiert wird. Solche Zeilen werden nicht ausgefhrt,
       haben also nur Kommentarcharakter. Mit Hilfe solcher Zeilen kann man
       w„hrend eines Scriptfiles Informationen und Hinweise an den Benutzer
       bermitteln.
       Der zweite Sonderbefehl 'Pause' kann dazu benutzt werden, Scriptfiles
       an bestimmten Stellen anzuhalten und auf einen Tastendruck warten zu
       lassen.


      J. Lokale Erweiterungen der Simulator-/Monitortabelle

       Wenn man im Arbeitsverzeichnis eines Projektes ein File mit dem Namen
       des Projektes oder genauer mit dem Namen des Simulatorfiles (*.SIM)
       aber der Erweiterung '.TAM' (oder '.TAS') anlegt, so wird der Inhalt
       dieses Files automatisch am Ende der normalen Prozessortabelle mit
       eingelesen.

       Man kann ber diesen Mechanismus lokal (nur in diesem Projekt)
       zus„tzliche Eintr„ge im Registerfenster erzeugen. Man kann zus„tzliche
       Funktionen realisieren oder Portpins mit speziellen weiteren
       Funktionen und/oder Namen.

       Der konkrete Grund zur Implementierung dieser M”glichkeit war, daá man
       ber zus„tzliche Funktionen in einer Monitortabelle sehr einfach und
       elegant Erweiterungen eines Monitorprogrammes
       (demo\80c552\download\dl??.asm) im Betrieb testen kann.

       Beipiel:
       Man hat eine kleine Routine zur Periodendauermessung der Frequenz an
       einem Inputpin geschrieben. Diese Routine ist so im Monitorteil
       eingebunden, daá man sie durch Schicken eines kleinen 'f' aktivieren
       kann. Zus„tzlich zum 'f' erwartet die Routine als zweites Byte die
       Anzahl der fr die Messung zu verwendenden Perioden. Als Ergebnis
       liefert die Routine eine Cyclenzahl (1 Byte) zurck aus der auf die
       Periodendauer rckgerechnet werden kann.
       Man schickt dem Monitor also 2 Bytes ( 'f' <Periodenzahl> ) und erh„lt
       darufhin ein Byte ( <Cyclenzahl> ) als Ergebnis zurck.
       Um diese Routine Testen zu k”nnen erzeugt man ein File mit dem Namen
       des 'SIM'-Files und der Erweiterung 'TAM'. Dieses File muá die
       folgende Zeile enthalten:
                                    ffreq SO('f').SO(IN).SI(-1)
       šber diese Zeile wird eine Funktion definiert mit deren Hilfe man die
       oben erw„hnte Routine testen kann.
       Man kann im Commandfenster schreiben: print freq(2)


                                     - Seite 51 -




       und erh„lt als Ergebnis die gewnschte Cyclenzahl. Man kann die
       Funktion auch mit 'watch freq(2)' ins Watchfenster bringen, so daá sie
       w„hrend der Programmausfhrung st„ndig ausgefhrt wird. Schlieálich
       kann man den Verlauf des Wertes ber einen l„ngeren Zeitraum
       protokollieren indem man ein Datfile schreiben l„át. Dies k”nnte zum
       Beispiel mit dem folgenden Befehl geschehen:
                     Datfile freq(2),0,100,freq.dat
       In diesem Beispiel wird die Routine 'freq' mit dem Arbument 2
       insgesamt 100 mal aufgerufen und die entsprechenden Cyclenwerte
       zusammen mit der Wertnummer ins File 'freq.dat'geschrieben.


      K. Syntax von Ausdrcken im Simulator/Monitor

             Zahlen    <digits><suffix> = <zahl>
             ------        \      \
                            \      \_ Suffix  b/B  o/O  d/D/-  h/H
                             \               ----------------------
                              \        Basis    2    8   10    16
                               \
                                \_ digits = 0-9,a-f,A-F


             Zeichen   '<char>'  (z.B. 'a')
             -------




































                                     - Seite 52 -




             Operatoren
             ----------
             Operator   Typ           Beschreibung
             :          Spezial       Programcodebits Relativ
             @          Spezial       Segment-Operator
             *          Operator      Multiplizieren
             /          Operator      Dividieren
             %          Operator      Modulo (Rest nach Division)
             -          Operator      Subtrahieren
             +          Operator      Addieren
             <<         Binary        links schieben
             >>         Binary        rechts schieben
             &          Binary        Bitweise 'UND'
             ^          Binary        Bitweise 'EXOR'
             |          Binary        Bitweise 'ODER'
             +^         Spezial       šbertr„ge bestimmen
             +|         Spezial       Parity-Bit bestimmen
             +-         Spezial       PC und Cyclen Update
             <          Relational    Kleiner als
             <=         Relational    Kleiner oder gleich
             >          Relational    Gr”áer als
             >=         Relational    Gr”áer oder gleich
             ==         Relational    Gleich
             !=         Relational    Nicht gleich
             =          Zuweisung     Variable <- Wert
             *=         Zuweisung     Variable <- Variablenwert * Wert
             /=         Zuweisung     Variable <- Variablenwert / Wert
             %=         Zuweisung     Variable <- Variablenwert - Wert
             -=         Zuweisung     Variable <- Variablenwert - Wert
             +=         Zuweisung     Variable <- Variablenwert + Wert
             ^=         Zuweisung     Variable <- Variablenwert >> Wert
             |=         Zuweisung     Variable <- Variablenwert >> Wert
             &=         Zuweisung     Variable <- Variablenwert >> Wert
             <<=        Zuweisung     Variable <- Variablenwert >> Wert
             >>=        Zuweisung     Variable <- Variablenwert >> Wert
             (Space)    Null          Trennungszeichen


       Priorit„t in einem Ausdruck ohne Klammern entsprechend der Reihenfolge
       ! ( ':' hat h”chste Priorit„t / '.' hat niedrigste Priorit„t )

             Ausdruck <expr> = mathematisch sinnvoller Ausdruck betehend aus
             --------          Zahlen, Operatoren, Labels, Speichersegment-
                               zugriffen, Funktionen und runden Klammern.

                 - Alle Ausdrcke werden mit 32 Bit Integer Werten berechnet.
                 - Vergleichende Operatoren liefern 0 fr falsch
                                              und FFFFFFFFh fr wahr.


             Beispiele: 10h+10>>10b     ergibt 6
                        (10h+10b)>>1    ergibt 9
                        ((w1=1)&2)      ergibt 2 wenn w1=1 sonst 0








                                     - Seite 53 -




      L. Simulatortabelle erstellen

       Das Erstellen einer Simulatortabelle gliedert sich in mehrere
       Teilschritte:

        1.Speicherbereiche und Register des zu simulierenden Prozessors (oder
          Platine !) anlegen.
        2.Segmentarten des Assemblers abbilden.
        3.Anweisungen zur Initialisierung des Prozessors erzeugen
        4.Eigentlichen Befehlssatz des Prozessors (m”glichst strukturiert)
          durch Formeln nachbilden.
        5.Cyclische Anweisungen anlegen (wenn n”tig)
        6.Inhalt des 'Register'-Fensters im Simulator festlegen
        7.Interface zu anderen Modulen Vorbereiten
        8.Einfachen Zugriff auf CPU-interne Register vorbereiten

       Die Vorgehensweise soll hier anhand des Prozessors 80C552 von Philips
       nachvollzogen werden. Zum besseren Verst„ndnis sollte man die
       Entsprechende Tabelle ('Tabellen\80C552.TAS') mit heranziehen.

       1. Speichersegmente anlegen

       Der 80C552 kann zun„chst einmal 64k Programmspeicher adressieren. Um
       diesen Programmspeicher im Simulator bereitszustellen gengt eine
       Zeile mit folgenden Inhalt:

                    mB0 10000h      ; Code-Speicher
                     \\\\____/
                      \\\   \
                       \\\  Anzahl der Elemente dieses Speichersegments
                        \\Segmentnummer
                         \Elementart (B=Byte, W=Wort, D=Doublewort)
                          'm'= Kennung fr Speichersegmentdefinition (Memory)


       Zu beachten ist folgendes:
        - Alles was in einer Zeile nach einem Strichpunkt (';') steht ist
          Kommentar und fr die Interpretation irrelevant.
        - Die Segmentnummer 0 ist fest reserviert fr den Programmspeicher.
          In dieses Segment wird beim Starten des Simulator automatisch das
          auszufhrende Programm geladen. Deshalb muá dieses Segment
          mindestens so groá sein, daá das auszufhrende Programm hinein
          passt, wenn es nicht ohnehin so groá gemacht wird, was der
          Prozessor maximal adressieren kann.
        - Die Elementart des Codesegments ist auch bei den meisten 16-Bit
          Prozessoren 'Byte', da auch diese den Programmspeicher Byteweise
          adressieren (k”nnen).

       Auf den mit dieser Zeile 'erzeugten' Speicher kann nun innerhalb des
       Simulators mit dem Ausdruck 0@Adresse zugegriffen werden. Allgemein
       kann auf eine beliebige Stelle in einem Segment mit

             SegmentNummer@Adresse  zugegriffen werden.


       Im weiteren besitzt der 80C552 noch 256 Byte internes Daten-RAM und
       bis zu 64k externes RAM. Bereitzustellen mit den beiden Zeilen:




                                     - Seite 54 -




             mB1 256        ; Internes RAM
             mB3 10000h     ; Externer RAM-Speicher


       Um die SF-Register (Special-Funktion-Register) des 80C552 zu
       simulieren kann man noch ein Segment mit 256 Bytes reserviert deren
       obere 128 Adressen direkt den Inhalt der SF-Register repr„sentieren
       sollen.

             mB2 256       ; Obere H„lfte SF-Register


       Die untere H„lfte dieses Segments, die im wirklichen Prozessor gar
       nicht existiert, soll fr Simulatorinterne Vorg„nge Reserviert sein.

       An drei Stellen greift der Simulator selbst„ndig auf besondere
       Adressen eines Segmentes zu. Es handelt sich dabei um:

             - Programmz„hler
             - Differenz-Cyclenzahl
             - Flag-Register


       Zwei dieser drei Register werden hier konkret im 16-Bit-Segment 4
       realisiert.

             mW4 3        ; 0 = Programm-Counter
                          ; 1 = Timer 2
                          ; 2 = Flag-Register


       Diese Register sind alle 16-Bit breit. Fr den Timer 2 wurde hier ein
       Register bereitgestellt, da er nur lesbar und nicht beschreibbar sein
       soll und deshalb nicht direkt im SFR-Bereich realisiert werden kann.

       Das dritte Register auf das der Simulator direkt Zugriff hat ist die
       Differenzcyclenzahl. Da der Wert dieses Registers bei einem Befehl
       niemals gr”áer als 255 werden kann reicht hierfr ein 8-Bit Register
       aus. Ich habe es deshalb auf Adresse 2 in den unteren nicht benutzten
       Teil der SF-Register-Segmentes gelegt.

       Damit der Simulator auf diese Register direkt zugreifen kann mssen
       sie ihm bekannt gemacht werden. Dies geschieht ber folgende drei
       Zeilen:

             P4 0    ; Programmz„hler -> Simulator
             C2 2    ; Differenz-Cyclenzahl -> Simulator
             F4 2    ; Flag-Position -> Simulator


       'P', 'C' und 'F' (Groáschreibung beachten !) sind die entsprechenden
       Schlsselbuchstaben auf die dann jeweil die entsprechende
       Segmentnummer und die Adresse in diesem Segment folgen. Diese drei
       Zeilen mssen in jeder Tabelle vorhanden sein.
       In der Tabelle wird noch ein weiteres Segment erzeugt.

             mW5 8     ; ADU-Kan„le




                                     - Seite 55 -




       Dieses Segment wird ben”tigt um eine realistische Funktionsweise des
       AD-Umsetzers zu erreichen (weiteres an entsprechender Stelle).

       2. Abbilden der Segmentarten des Assemblers
     Die Segmentarten die der Assembler verwendet, werden in der
     entsprechenden Assemblertabelle festgelegt. Beim 80C552 sind es folgende
     Zeilen die diese Segmentarten festlegen:

             (Auschnitt aus der Datei 'Tabellen\MCS-51.TAB'
              die in die Datei 'Tabellen\80C552.TAB' importiert wird)


              Zeile
                  1    ; Segmentdefinitionen
                  2    ; -------------------
                  3    s CODE 0 10000h 0
                  4    s C 0 10000h 0
                  5    s XDATA 0 10000h 1
                  6    s X 0 10000h 1
                  7    s EXTERN 0 10000h 1
                  8    s DATA 0 100h 2
                  9    s D 0 100h 2
                 10    s STACK 0 100h 2
                 11    s S 0 100h 2
                 12    s INTERN 0 100h 2
                 13    s I 0 100h 2
                 14    s BIT 0 80h 3 -> Intern 20h+@1/8 ((@2-1)/8)+1
                 15    s B 0 80h 3 -> Intern 20h+@1/8 ((@2-1)/8)+1
                 16    s BANKS 0 4 4 -> Intern @1*8 @2*8
                 17    s SFREG 80h 100h 5 -> Overlay
                 18    s SFBIT 80h 100h 6 -> Overlay


       Das 4.Argument in jeder Zeile nach dem 's' bezeichnet die
       Clustergruppe. In der Regel gibt es mehrere Zeilen mit einer
       Clustergruppe, was bei der Programmierung bedeutet, daá mehrere Namen
       fr die selbe Segmentart zur Verfgung stehen. Fr den Simulator muá
       jeweils der erste Eintrag fr eine Clustergruppe als
       Abbildungsfunktion realisiert werden.
       Damit mssen realisiert werden:

             CODE     (realisiert Clustergruppe 0)
             XDATA    (realisiert Clustergruppe 1)
             DATA     (realisiert Clustergruppe 2)
             BIT         (realisiert Clustergruppe 3)
             BANKS    (realisiert Clustergruppe 4)
             SFREG    (realisiert Clustergruppe 5)
             SFBIT    (realisiert Clustergruppe 6)


       Fr die Segmentart CODE ergibt sich zwingend die Funktion:

             fCODE 0@in


       Zwingend deshalb weil das Segment 0 immer fr den Codespeicher
       reserviert ist, wie im 1. Abschnitt bei der Segmentdefinition bereits
       erw„hnt.



                                     - Seite 56 -




       Als Parameter bekommen alle derartigen Funktionen die Adresse im
       jeweiligen Segment und mssen dann den richtigen Inhalt zurckliefern.

       Analog fr alle weiteren Segmentarten beim 80c552:

             fDATA 1@in
             fSFREG 2@in
             fXDATA 3@in
             fBIT (1@(20h+((in>>3)&15))>>(in&7))&1
             fBANKS in
             fSFBIT ((2@(in&0F8h))>>(in&7))&1


       Die unfangreichsten Funktionen mssen fr Bitadressen des Prozessors
       erzeugt werden, da aus der Bitadresse zun„chst die Speicheradresse
       extrahiert werden muá und dann die ensprechende Bit-Position
       herausmaskiert und an die niederwertigste Position geschoben werden
       muá.

       Zus„tzlich zu den Segmentarten sollte bei jedem Prozessor noch eine
       Funktion fr 'Equ'-Ausdrcke realisiert werden damit auch diese im
       Variablenfenster angezeigt werden k”nnen.

             fEqu in


       3. Anweisungen zur Initialisierung

       Laut Datenblatt eines Prozessors mssen nach einem RESET einige
       Register bestimmte Inhalte besitzen. Unter anderem wird auch der
       Programmz„hler stets in eine definierte Ausgangslage versetzt.

       Beim 80C552 bedeutet das, daá der Programmz„hler auf 0 gesetzt werden
       muá. Dies wird im Simulator durch die Zeile

             i 4@0=0


       erreicht. 'i' ist der Schlsselbuchstabe fr Initialisierungs-
       anweisungen die immer nach einem Hardwarereset ausgefhrt werden
       mssen. In der Zeile kann nach dem Schlsselbuchstaben ein beliebiger
       Ausdruck folgen. Es k”nnen in einer Tabelle beliebig viele
       Initialisierungszeilen angelegt werden.
       Natrlich k”nnen hier auch Simulatorinterne Variablen initialisiert
       werden.

       4. Befehlssatz des Prozessors

       Um den Befehlssatz eines Prozessor effektiv in einen fr den Simulator
       geeigneten 'Baum' umsetzen zu k”nnen, sollte man sich den Befehlssatz
       zun„chst in Form einer Tabelle veranschaulichen. Zweck dieser šbung
       ist es Strukturen im Befehlssatz zu erkennen die eine pr„gnante
       Umsetzung in einen 'Baum' erm”glichen. In Befehlss„tzen sind sehr
       h„ufig solche Strukturen enthalten, da sie sich auch bei einer
       Hardware-Realisierung positiv auswirken, und deshalb bei der
       Konzipierung des Prozessors teilweise bewuát 'eingebaut' wurden bzw.
       sich automatisch ergaben.




                                     - Seite 57 -




       Grunds„tzlich ist aber auch eine 'brut force' L”sung m”glich um einen
       Simulator-Kern zu realisieren.

       Was macht ein Prozessor ?
        - Er schaut an der Stelle die ihm der Programmz„hler angibt nach
          einem Befehl.
        - analysiert den vorgefundenen Befehl
        - erkennt daraus wie viele Bits (Bytes) zu diesem Befehl geh”ren und
          wie viele Cyclen er fr diesen Befehl brauchen darf
        - setzt den Programmz„hler auf den n„chsten auszufhrenden Befehl
        - fhrt den Befehl aus

       Um an der Stelle die der Programmz„hler angibt den Programmcode zu
       untersuchen stellt der Simulator den ':'-Operator bereit. Dieser
       Operator erm”glicht es relativ zum Programmz„hler eine bestimmte
       Bitzahl herauszuschneiden und diese dann beliebig in Ausdrcken zu
       verwenden.

               Beispiel:                 Programmz„hler = 6
                                        /
                 Code-Adresse     4 5  6  7  8  9  A  B
                 Code-Speicher   12 34 56 78 9A BC DE F0

                     Ausdruck   -->  Ergebnis
                      0:4      -->      5
                      4:4      -->      6
                      0:8      -->      56
                      0:16     -->      5678
                      9:2      -->      3
                     12:3      -->      4
                       \ \
                        \ Anzahl der gewnschten Bits
                         relative Startposition in Bits !


       Der entscheidende Vorteil dieses Zugriffs gegenber dem direkten
       Zugriff ber 0@Adresse ist, daá Byte- oder Wortgrenzen keine Rolle
       spielen. Der Programmzeiger selbst sitzt zwar auf einer Grenze l„át
       aber von dieser Stelle aus bitgenaue Zugriffe zu. In obigem Diagramm
       sind einige Beispiele dargestellt.

       Analyse des Befehls bedeutet bei HSIM eine Art 'Case'-Anweisung die
       fr jeden Fall einen ausfhrbaren Ast besitzt. Der Simulator  stellt
       nur eine sehr einfache baum„hnliche Struktur zur Verfgung mit der
       diese Ver„stelung in einzelne Zweige realisiert werden kann.
       Startzeile einer solchen Struktur ist eine Zeile die am Anfang einen
       '.' (Punkt) mehr besitzt als die Letzte Zeile die mit einem Punkt
       beginnt. Das heiát die erste Zeile mit einem Punkt am Anfang ist eine
       solche Startzeile.

       Format einer Startzeile:

             .4:1 2
              \_/  \
                \   Anzahl der m”glichen ste (kein Ausdruck - feste Zahl !)
                 Ausdruck der als Ergebnis die Nummer des auszuw„hlenden
                 Astes ergibt.




                                     - Seite 58 -




             Beispiel:

                     .4:1 2
                     .2@0E0h+=1     ; Wird ausgefhrt wenn 4.Bit = 0
                     .2@0E0h-=1     ; Wird ausgefhrt wenn 4.Bit = 1


       In diesem Beispiel wrde der Akku (befindet sich auf Adresse 0E0h im
       Segment 2) um 1 erh”ht wenn das 4.Bit des Prozessor-Befehls 0 ist und
       um 1 erniedrigt wenn das 4.Bit 1 ist.

       Derartige Baumstrukturen lassen sich (fast) beliebig schachteln !

       Die einfachste (und zugleich speicherhungrigste, wartungsfeindlichste)
       Art den Befehlssatz eines 8-Bit-Prozessors zu implementieren bei dem
       der Befehl immer vollst„ndig im 1.Byte codiert ist w„re folgende:

             .0:8 256
             .Ausdruck_fr_Befehl_00
             .Ausdruck_fr_Befehl_01
             .Ausdruck_fr_Befehl_02
             .   ..
             .   ..
             .Ausdruck_fr_Befehl_FF


       Dies ist die 'brut-foorce'-Methode einer Befehlsdekodierung. Die
       Anzahl der zu kodierenden Befehle betr„gt genau 256. In der von mir
       verwendete Struktur fr den 80C552 in der nur noch 146 Befehle codiert
       werden mssen sieht in etwa wie folgt aus:

             .4:1 2                  ; Abtrennen der Registerbank-Befehle
             ..((0:4)<<3)+5:3 128    ; Sonstige Befehle (128 verschiedene !)
             ..
                   ..
             ..
             .
             ..2@5=(2@0D0h&18h)|5:3.0:4 16  ; 16 verschiedene RB-Befehle
             ..
                   ..
             ..


       Da das 4.Bit des 1.Befehlsbytes bereits bei der Abtrennung der RB-
       Befehle bercksichtigt wird, muá es bei der weiteren Aufspaltung der
       sonstigen Befehle herausgeschnitten werden. Das erreicht man mit einem
       Ausdruck der wie folgt aussieht:

                ((0:4)<<3)+5:3


       Bei der Aufspaltung der 16 verschiedenen RB-Befehle besteht der
       Ausdruck eigentlich aus zwei Teilausdrcken die durch ein '.'
       (Alt+250) voneinander getrennt sind. Als Astnummer fr die Aufspaltung
       wird immer der letzte Ausdruck verwendet. Der erste Teilausdruck
       liefert somit also kein Ergebnis nach 'auáen', wird aber Berechnet,
       und setzt somit das Byte unter der Adresse 5 im Segment 2 auf einen
       Wert der sich aus den unteren 3 Bit des 1.Code-Bytes und aus 2 Bits
       des Programmstatuswortes des 80C552 ergibt. Und - Oh Wunder das


                                     - Seite 59 -




       Ergebnis ergibt immer die absolute Adresse im internen RAM auf die
       sich der im folgenden auszufhrende Registerbank-Befehl bezieht.

       Bis zu diesem Punkt handelte es sich (fast) ausschlieálich um die
       Analyse des anstehenden Befehls. Nun steht aber an den entsprechenden
       Stellen mehrere Dinge fest:

             - um welchen Befehl es sich handelt.
             - um vieviel der Programmz„hler erh”ht werden muá
             - wieviele Cyclen der Befehl zur Ausfhrung ben”tigt
             - eigentliche Aktion des Befehls


       Diese Tatsachen mssen nun fr den Simulator umgesetzt werden.

       Erstes sehr einfaches Beispiel bei dem der Befehl nur aus einem Byte
       besteht

             Beispiel:  DEC A   --> Inhalt des Akku um 1 erniedrigen
                      (1-Byte-Befehl, ben”tigt einen Prozessorcyclus !)


             Umsetzung:  ..4@0+=1.2@2+=1.2@0E0h-=1.PAR(0)
                           \____/ \____/ \_______/ \____/
                                \      \         \      \
                                 \      \         \    Aktualisieren des PSW
                                  \      \        Eigentliche Aktion des
                                   \      \            Befehls
                                    \      Cyclenzahl um 1 erh”hen
                                     Programmz„hler um 1 erh”hen


       Die Syntax der erlaubten Ausdrcke lehnt sich stark an die unter 'C'
       gebr„uchliche Syntax an. Der Teilausdruck '4@0+=1' ist „quivalent mit
       dem Ausdruck '4@0=4@0+1'. Beide erh”hen den Programmz„hler um 1. Es
       gibt genaugenommen doch einen kleinen Unterschied, der aber erst im
       Zusammenhang mit der Interface-Realisierung wichtig wird und auch erst
       dort erkl„rt werden soll.

       Bis auf die Anweisung 'PAR(0)' handelt es sich um normale Ausdrcke
       mit Zuweisungen. 'PAR(0)' ist ein Funktionsaufruf. Die Funktion selbst
       ist mit

             fPAR 2@0D0h&=0FEh.2@0D0h|=(2@0E0h+|8) ; Parity-Flag akt.


       auch in der Tabelle definiert. Da Funktionsaufrufe in einem Ausdruck
       an ihrem Argument erkannt werden muá auch bei dieser Funktion, die
       eigentlich gar kein Argument erwartet ein Argument (hier '0')
       angegeben werden.

       In dieser Funktion wird ein besonderer Befehl zur Berechnung des
       Parity-Bits verwendet, da eine explizite Formel relativ umst„ndlich
       (aber natrlich trotzdem m”glich) w„re.

       W„hrend der Implementierung eines neuen Prozessors ist es sinnvoll
       alle Befehle die noch nicht richtig umgesetzt sind zun„chst mit '0'
       abzus„ttigen. In diesem Fall bleibt der Simulator bei einem noch nicht
       implementierten Befehl einfach stehen (da sich der Programm-Z„hler


                                     - Seite 60 -




       nicht mehr erh”ht !) und gibt die Meldung 'Nicht implemetierter OP-
       Code ..' aus. Fr Befehle die der Prozessor gar nicht kennt (freie OP-
       Codes) sollte aus der '0' eine '2' gemacht werden, um die Meldung
       'Ungltiger OP-Code' zu erzeugen. Eine '1' macht sich beim
       Programmlauf berhaupt nicht bemerkbar und kann dann verwendet werden
       wenn der Befehl bereits w„hrend der Dekodierung vollst„ndig ausgefhrt
       wurde.

             0 -> Nicht implemetiert
             1 -> o.k. (keine Meldung)
             2 -> Ungltiger OP-Code


       Wenn im Laufe der Dekodierung zu einer Zeile mit einer 1 der
       Programmz„hler nicht ver„ndert wird, bleibt der Simulator natrlich
       auch stehen, allerdings ohne Meldung !

       Nun ein Beispiel bei dem der Befehl aus 2 Bytes besteht

             Beispiel:  ANL A,<Adr> --> Inhalt des Akku UND-verknpfen mit
                                        dem Inhalt der Adresse <Adr> im
                                        internen RAM (oder SFR !)
                     (2-Byte-Befehl, ben”tigt 1 Prozessorcyclus !)


             Umsetzung:  ..4@0+=2.2@2+=1.2@0E0h&=(1+8:1)@(8:8).PAR(0)
                           \____/ \____/ \___________________/ \____/
                                \      \         \               \
                                 \      \         \    Aktualisieren des PSW
                                  \      \    Eigentliche Aktion des Befehls
                                   \      Cyclenzahl um 1 erh”hen
                                    Programmz„hler um 2 erh”hen


       Abh„ngig vom h”chtwertigen Bit der Adresse wird hier entweder auf das
       interne RAM (Adr < 128) oder auf die SFR zugegriffen.

       Als weiteres Beispiel einen relativen bedingten Sprungbefehl

             Beispiel:  JZ <Rel-Adr> --> Relativer Sprung wenn der
                                         Akku 0 enth„lt
                   (2-Byte-Befehl, ben”tigt 2 Prozessorcyclen !)


             Umsetzung:
               ..4@0+=2.2@2+=2.4@0+=(2@0E0h==0)&(8:8-(256&(8:1==1)))
                 \____/ \____/ \___________________________________/
                   \       \         \
                    \       \         Sprung oder nicht Sprung !?
                     \      Cyclenzahl um 2 erh”hen
                   Programmz„hler um 2 erh”hen


       Der Ausdruck (2@0E0h==0) in der Umsetzung liefert als Ergebnis -1 wenn
       der Akku (2@0E0h) 0 ist und 0 wenn der Akku nicht 0 ist. Wenn man
       dieses Ergebnis UND-Verknpft mit dem Sprungoffset und das Ergebnis
       auf den Programmz„hler addiert, so ergibt sich genau dann ein Sprung
       wenn der Vergleich positiv ausf„llt.



                                     - Seite 61 -




       Anmerkung: Solche Sprnge k”nnen auch ber einen Unterbaum mit zwei
       sten realisiert werden. Dies ist besonders dann vorzuziehen wenn
       abh„ngig von der Bedingung noch andere Aktionen gemacht werden mssen.

       Abschlieáendes Beispiel - Unterprogrammaufruf mit zugeh”rigem Return

             Beispiel:  LCALL <Adr> --> Unterprogrammaufruf
                  (3-Byte-Befehl, ben”tigt 3 Prozessorcyclen !)


             Umsetzung:
             ..4@2|=1.4@0+=3.2@2+=3.PUSH(4@0&255).PUSH(4@0>>8).4@0=8:16
               \____/ \___________/ \_________________________________/
                  \   Programmz„hler   Rcksprungadresse -> Stack
                   \   & Cyclenzahl    Call ausfhren
                    \
               Meldung eines Calls an den Simulator ber das Flagregister !


             benutzte Funktion:
               fPUSH 2@81h+=1.1@(2@81h)=in
                     \______/ \__________/
                           \       \
                            \        Argument der PUSH-Funktion an
                             \       die Stelle schreiben auf die
                              \      der Stackzeiger zeigt.
                               \
                                Stackzeiger um 1 erh”hen


       Besonderheit dieses Befehls ist die Meldung eines CALL's an den
       Simulator ber das Flagregister. šber diesen Mechanismus kann der
       Simulator Unterprogramme erkennen und diese gegebenenfalls in einem
       Schritt ausfhren (-> Funktionstasten F10/F8).

       Im Flagregister sind insgesamt zwei Bitpositionen fr Meldungen an den
       Simulator vergeben:

                 Bitposition    Art der Meldung an den Simulator
                     0         Unterprogrammaufruf
                     1         Return aus einem Unterprogramm


       Wenn das entsprechende Bit gesetzt ist, so erkennt der Simulator die
       entsprechende Meldung.

             Realisierung des zugeh”rigen Return-Befehls:

             ..4@2|=2.2@2+=2.4@0=(1@(2@81h)<<8)+1@(2@81h-1).2@81h-=2
               \____/ \____/ \____________________________/ \______/
                  \      \     eigentlicher Rcksprung      Stackzeiger
                   \      \                                 Aktualiseren
                    \      Return ben”tigt 2 Cyclen
                     \
                   RETURN-Meldung an den Simulator


       Sonderausdruck 'n' in einer Zeile der Tabelle als Befehlsumsetzung



                                     - Seite 62 -




       Wenn in einer Tabelle an Stelle der eigentlichen Umsetzung des Befehls
       (nach den Punkten !) einfach ein 'n' steht, dann bedeutet das, daá fr
       diese Zeile der Ausdruck (Befehlsumsetzung der n„chsten Zeile
       verwendet wird. Damit k”nnen bestimmte Bereiche eines Baumes nachdem
       sie zun„chst in einzelne ste aufgespalten worden sind wieder
       vereinigt werden zu einer einzigen Umsetzung. Wenn mehrere 'n'-Zeilen
       hintereinander stehen, so wird fr alle Zeilen die erste
       Befehlsumsetzung nach den 'n'-Zeilen verwendet. (Anm. d. Autors: Macht
       zun„chst einen komischen Eindruck, spart aber unter Umst„nden sehr
       viel Speicher und Schreibarbeit !)

       ACHTUNG !!!
       In der Tabelle z.B. fr den 80C552 steht vor dem Befehlssatz des
       Prozessors eine Zeile die nur ein 'O' enth„lt. Diese Zeile schaltet
       die Definition der Prozessorbefehle und der Befehlscyclischen
       Anweisungen (nicht der Cyclencyclischen Anweisungen !) in der Tabelle
       ab !!!
       Anstelle der Tabelle wird eine in HSIM integrierte C-Routine verwendet
       die die Befehle des Prozessors nachbildet.
       Das hat zun„chst den Nachteil daá jetzt nichts mehr ver„ndert werden
       kann, bringt aber einen deutlichen Geschwindigkeitsvorteil (Faktor 5-
       10). In der Tabelle ist trotzdem die komplette Nachbildung enthalten,
       damit durch auskommentieren des O's auch eine modifizierte Tabelle
       verwendet werden kann (mit reduzierter Geschwindigkeit !).
       Wenn in einer Tabelle ein 'O' enthalten ist, fr die HSIM beine C-
       Routine zur verfgung hat, so verwendet HSIM einfach die Tabelle. Im
       Simulator kann mit dem Befehl 'Status' festgestellt werden ob momentan
       die Tabelle oder eine C-Routinen verwendet wird. Wenn nach der
       Bezeichnung 'Overlay' eine NULL steht, so ist keine C-Routine
       vorhanden - jede andere von 0 verschiedene Zahl deutet auf das
       vorhandensein einer entsprechenden C-Routine hin.

       Anmerkung:
       Wenn Sie eine Tabelle geschrieben haben und sie um die Simulation zu
       beschleunigen in Form einer C-Routine integriert haben m”chten,
       schicken Sie mir die Tabelle auf einer Diskette (Rckporto nicht
       vergessen !) und ich werde Ihnen innerhalb weniger Tage HSIM mit Ihrer
       Tabelle zukommen lassen. Als Gegenleistung darf ich Ihre Tabelle mit
       HSIM weiterverbreiten.

       5. Cyclische Anweisungen anlegen

       Bei Cyclischen Anweisungen handelt es sich um Ausdrcke die Periodisch
       das heiát ohne Rcksicht auf den gerade auszufhrenden Befehl immer
       wieder ausgefhrt werden.
       Ein einfaches Beispiel w„re ein Echtzeittimer. Ein solcher Timer máte
       nach jedem Prozessorcyclus um einen bestimmten Wert erh”ht werden. Um
       einen 16-Bit Timer zu realisieren gengt folgende Zeile:

             b1 4@1+=2@2
              \\\______/
               \\    \
                \\    Eigentliche Aktion: Timer um Cyclenzahl erh”hen
                 \\__
                  \  Bedingungsausdruck der angibt wann die folgenden
                   \ ausgefhrt werden mssen (hier 1 = immer erfllt !)
                    \
                     Schlsselbuchstabe fr Befehls-Cyclische Anweisung



                                     - Seite 63 -




       Der so realisierte Timer ist nicht abschaltbar und z„hlt immer genau
       die Cyclen des Prozessors, indem er auf den Wert von 4@1 immer die
       Anzahl der Cyclen des letzten ausgefhrten Befehls (2@2) addiert.

             Konkretes Beispiel beim 80C552 w„re der Timer 2.

                 ; Timer 2 als normaler Z„hler mit Prescaler
             b2@0EAh&3==1 2@3+=2@2.4@1+=(2@3>>((2@0EAh>>2)&3))
                      2@3&=((1<<((2@0EAh>>2)&3))-1).2@0ECh=4@1.2@0EDh=4@1>>8


       Die in zwei Zeilen aufgeteilte Realisierung muá innerhalb der Tabelle
       in einer Zeile stehen !
       Neben diesen Anweisungen die fr jeden Befehl des Prozessors
       ausgefhrt werden gibt es noch Cyclencyclische Anweisungen. Die Syntax
       dieser Anweisungen entspricht der der Befehlscyclischen Anweisungen,
       nur daá der Schlsselbuchstabe ein `c` ist.

       Prinzipiell k”nnen alle cyclencyclischen Anweisungen auch als
       befehlscyclische Anweisungen realisiert werden, da die Anzahl der
       abgelaufenen Cyclen im Differenzcyclen-Register steht.

       6. Inhalt des Register-Fensters

       Der Inhalt dieses Fensters kann w„hrend der Laufzeit nicht ver„ndert
       werden. Das Registerfenster sollte auf einen Blick die wichtigsten
       Register des Prozessors mit ihrem Inhalt veranschaulichen.
       In der Regel geh”rt dazu eine Art Akku, der Prozessorstatus und
       natrlich der Programmz„hler.
       Um den Akku des 80C552 darzustellen gengt folgende Zeile:

             v1 0 Akku=%02lX 2@0E0h     ; Akku anzeigen
               \ \\________/ \____/
                \ \     \      \
                 \ \     \      Ausdruck fr darzustellenden Wert
                  \ \     Darstellungsformat(keine Leerzeichen !)
                   \ Spalte
                    Zeile


       'v' (kleines 'v') ist der Schlsselbuchstabe fr Darstellungen im
       Register-Fenster. Zeile und Spalte bezeichnen den Anfangsort ab dem
       der gewnschte Ausdruck dargestellt werden soll.
       Die mit Darstellungsformat bezeichnete Zeichenkette muá einem Format
       entsprechen wie es auch unter der Programmiersprache 'C' verwendet
       wird. Einzige Ausnahme ist, daá keine Leerzeichen enthalten sein
       drfen. Das Darstellungsformat muá im weiteren so angelegt werden, daá
       es genau eine 'long int'-Zahl als Argument erwartet.

             Beispiele fr gltige Formate: (Argument = 138)
                             'Akku=%li'     -->   Akku=138
                             'PSW=%lx'      -->   PSW=8a
                             'PC:%4lX'      -->   PC:  8A
                             'PC:%04lX'     -->   PC:008A


       Der Ausdruck fr den darzustellenden Wert kann natrlich auch
       komplexer sein. Um Bespielsweise das Register R0 des 80C552 richtig
       anzuzeigen muá folgende Zeile angelegt werden:


                                     - Seite 64 -




             v2 15 %02lX 1@(2@0D0h&18h)    ; R0 anzeigen


       Mit dieser Zeile ist der Tatsache Rechnung getragen, daá sich das
       Register R0 in Abh„ngigkeit vom Inhalt des PSW (Programmstatuswort) an
       verschiedenen Stellen im internen RAM befinden kann. Mit anderen
       Worten wenn der Programmierer die Registerbank wechselt, wird
       automatisch das aktuelle Register R0 angezeigt !

       Innerhalb des Registerfensters k”nnen auch Buchstaben dargestellt
       werden, die von bestimmten Werten abh„ngen. Diese M”glichkeit wird in
       der Simulatortabelle fr den 80C552 nicht benutzt. Als Beispiel fr
       diese M”glichkeit sei an dieser Stelle auf die Bausteintabelle fr den
       8255 zurckgegriffen. Dort gibt es eine Zeile nach folgendem Muster:

             v0 3 +---%c--+ 'O'^((1@3&10h==10h)&('I'^'O'))


       Mit dieser Zeile wird eine Art von Klammer (+------+) erzeugt, die zur
       n„heren Charakterisierung in der Mitte einen Buchstaben besitzt.
       Dieser Buchstabe kann entweder ein 'I' fr Input oder ein 'O' fr
       Output sein. An der Stelle an der der Buchstabe im Formatstring
       erscheinen soll muá mit '%c' definiert werden. Beim Ausdruck fr den
       anzuzeigenden Wert muá als Ergebnis der Hexcode fr das gewnschte
       Zeichen herauskommen. Im Beispiel wird, wenn das 4. Bit der
       Speicherstelle 3 im Memorysegment 1 gesetzt ist, anstelle des 'O' ein
       'I' angezeigt.

       7. Interface zu anderen Modulen Vorbereiten

       Das Interface zu anderen Modulen (Prozessoren) erfolgt bei HSIM ber
       bestimmte Speicheradressen, was normalerweise als Memorymapped
       bezeichnet wird. Das bedeutet, daá Ausgaben an andere Module so
       erfolgen, daá auf bestimmte Speicheradressen normal geschrieben wird.
       Der eigentliche Modulbergreifende Zugriff erfolgt dabei nur bei
       Schreibzugriffen auf spezielle Portadressen, die als solche dem
       Simulator mitgeteilt werden mssen.
       Um dem Simulator mitzuteilen, daá eine Speicherstelle als Ausgabeport
       verwendet werden soll, mssen diese Speicherstellen als solche mittels
       'I'-Zeilen (Interface-Zeilen) in der Tabelle angemeldet werden.

             Mit der Zeile (Beispiel)

              Ipo 2@144 P1.0,P1.1,P1.2,P1.3,P1.4,P1.5,P1.6,P1.7
              |\/ \___/ \_____________________________________/
              | \     \   Pinbezeichnungen durch Komma getrennt
              |  \     Adresse fr die die Pinbezeichnungen gelten sollen
              |   Kennung: <p>inbezeichnung <o>utput
              Kennung fr <I>nterface-Zeile


       wird die Adresse 144 des Memorysegmentes 2 als Ausgabeport definiert.
       (Zur Erinnerung: Beim 80C552 Prozessor kann unter dieser Adresse das
       Ausgabeport P1 angesprochen werden)
       Nachfolgend k”nnen entsprechend der Bitzahl des Ports oder eigentlich
       der Bitzahl des verwendeten Segments Bezeichnungen der Einzelbits
       durch Kommas voneinander getrennt angegeben werden. Diese
       Bezeichnungen werden bei der Definition von Verbindungen im Simulator
       als Auswahlm”glichkeit angeboten (->Connetion-Editor). Diese


                                     - Seite 65 -




       M”glichkeit der Definition von Output-Pins ist bei Ports direkt am
       Prozessor sehr sinnvoll und einfach. Bei praktisch allen Prozessoren
       (mit externem Adress- und Datenbus) ist es aber m”glich an beliebigen
       Stellen im Adressraum Output-Ports (Latches) anzulegen. Damit man auch
       ber solche User-Ports andere Module ansteuern kann gibt es eine
       weitere Form Output-Pins fr den Simulator anzumelden.

             Mit der Zeile

              Ipo 3@-1 ?XData
              |\/ \__/ |\___/
              | |   |  |  Dummybezeichnung fr Memorymapped I/O-Pins
              | |   |   \
              | |   |    Kennung daá Ort und Anzahl der Output-Pins vom
              | |   |     Anwender zur Laufzeit angegeben werden k”nnen
              | |    \
              | |     Adresse fr die die Pinbezeichnungen gelten sollen
              | |      Das Segment (hier 3) muá immer angegeben werden
              | |      Wenn die Adresse -1 ist, so kann sie vom Anwender
              | |      zur Laufzeit angegeben werden.
              |  \
              |   Kennung: <p>inbezeichnung <o>utput
              Kennung fr <I>nterface-Zeile


       kann der gesamte Adressraum des Segmentes 3 (hier Externes
       Datensegment) als memorymapped I/O fr Output verwendet werden.

       Als Gegenstck zu den Output-Pins muá fr Eingangspins die Zeile

              Ipi 2@21 P1.0,P1.1,P1.2,P1.3,P1.4,P1.5,P1.6,P1.7
              |\/ \__/ \_____________________________________/
              | \    \   Pinbezeichnungen durch Komma getrennt
              |  \    Adresse fr die die Pinbezeichnungen gelten sollen
              |   Kennung: <p>inbezeichnung <i>nput
              Kennung fr <I>nterface-Zeile


       angegeben werden. Die Bezeichnungen sind in diesem Beispiel die
       selben, weil der Port 1 beim 80C552 als Input- und Output-Port
       verwendet werden kann. Bei vielen Prozessoren k”nnte sogar die
       zugeh”rige Adresse gleich der Ouputadresse sein, wenn wirklich nur ein
       physikalisches Register im Prozessor vorhanden ist. Nicht so beim
       80C552, bei ihm muá streng zwischen dem Wert im Outputregister und dem
       gelesenen Wert beim Input unterschieden werden, da er sogenannte
       Opencollektor Ausg„nge besitzt. Aus diesem Grund muá fr die Eingabe
       eine beliebige aber andere Simulatorinterne Adresse vergeben werden
       (im Beispiel Segment 2, Adresse 21). Der Inhalt dieser Speicherstelle
       soll den Wert der von auáen an den Port angelegten Pegel enthalten.

       Nun muá aber, wenn der Prozessor den Port lesen will noch eine
       Sonderbehandlung erfolgen. Port lesen heiát Lesezugriff auf die
       Adresse 2@144. Bei einem solchen Lesezugriff muá als Wert die UND-
       Verknpfung von Port-Output-Wert und dem Eingabewert zurckgeliefert
       werden (Opencollektor = UND-Verknpfung !). Erreicht wird dies mit der
       Zeile





                                     - Seite 66 -




              Ifi 2@144 2@21&in
              |\/ \___/ \_____/
              | \     \      Leseformel
              |  \     Adresse bei der die Leseformel wirksam sein soll
              |   Kennung: <f>ormel <i>nput
              Kennung fr <I>nterface-Zeile


       Diese Zeile weiát der Adresse 2@144 eine Formel zu, deren Ergebnis als
       Lesewert Verwendung findet. Damit kann auf den tats„chlichen Inhalt
       einer Speicherstelle (hier:2@144) im gleichen Modul eigentlich nicht
       mehr zugegriffen werden, da immer das Ergebnis der Formel 'gelesen'
       wird.
       Auf den Schreibzugriff hat diese Formel keinen Einfluá, dieser Zugriff
       kann ganz normal erfolgen. Um nun aber einen gezielten Eingriff beim
       Ausgabewert in Abh„ngigkeit vom Momentanzustand machen zu k”nnen muá
       man sogenannte READ-MODIFY-WRITE-Befehle der Art 2@144^=1 (Anstelle
       von 2@144=2@144^1) verwenden. Wenn man nur den Wert haben will, so muá
       man eine Dummyoperation (z.B.: 2@144^=0) verwenden.
       Beim Schreiben von einem anderen Modul auf den so definierten Port 1
       wird die Verbindung vom Anwender mittels der Bezeichnungen
       P1.0,P1.1,... hergestellt, daá der eigentliche Schreibvorgang dann auf
       die Adresse 21 des Segmentes 2 erfolgt und nicht auf Adresse 144
       bemerkt man bei der benutzung des Simulators nicht.

       Der Gesamtvorgang sieht nun fr eine angenommene Verbindung von
       P1.0(Output) nach P1.1(Input) folgendermaáen aus:

             Output-Pin              Input-Pin (meistens anderes Modul)
                     beim Schreiben
               P1.0  --------------->  Bit 1 von 2@21
             (Bit 0 von 2@144)                  \
                                                 \  normales Lesen
                                                  &--(allg. Formel)--->
                                                 /
                                                /   READ-MODIFY-WRITE
                                  Bit 1 von 2@144 <------------------->
                                                    oder Schreiben


       Mit diesem Mechanismuá k”nnen normale Portpins, die nur durch Software
       ausgewertet werden sehr effizient realisiert werden. Bei Input-Pins,
       die zum Beispiel direkt einen Hardwarez„hler Takten sollen ist diese
       Realisierung sehr Zeitaufwendig, da das Inputpin st„ndig ber
       Cyclische Anweisungen abgefragt werden muá, ob es sich ge„ndert hat
       und entsprechend der Z„hlerstand ver„ndert werden. Viel eleganter w„re
       es, wenn gleich die Seite die das Inputpin ver„ndert den Z„hler im
       Zielmodul bedienen wrde. Zu diesem Zweck ist es m”glich einer
       Speicherstelle auch eine Receive-Funktion zuzuordnen. Im Klartext
       bedeutet das, wenn von einem Modul ber eine Verbindung (Connection)
       auf eine Speicherstelle in einem anderen Modul zugegriffen wird, so
       wird, wenn dieser Speicherstelle im (anderen Modul) eine Receive-
       Funktion zugeordnet ist, diese Ausgefhrt.
       Realisiert wird eine Receive-Funktion mit der Zeile







                                     - Seite 67 -




             (Beispiel aus Simulatortabelle fr PIC16C57)

             Ifr 2@12 2@13=(2@13<<1)+(2@12&1).2@14=2..... usw.
             |\/ \__/ \_______________________________...
             | \     \       Receiveformel
             |  \     Adresse bei der die Receiveformel wirksam sein soll
             |   Kennung: <f>ormel <r>eceive
             Kennung fr <I>nterface-Zeile


       Das Abarbeiten einer Receive-Formel kann nur ber eine Verbindung
       (Connection) ausgel”st werden. Bei jedem Zugriff wird die Formel genau
       einmal abgearbeitet. Das eigentliche Ver„ndern der Speicherinhalte im
       Zielmodul wird dabei bereits VOR dem Ausfhren einer eventuell
       definierten Receivefunktion im Hintergrund durchgefhrt.

       8. Einfacher Zugriff auf CPU Register

       Alle Variablen die im Rahmen des Assemblerprogramms vom jeweiligen
       Programmierer definiert worden sind, k”nnen unter ihrem Namen sowohl
       ins Watchfenster aufgenommen werden als auch im Commandfenster mit dem
       SET-Befehl ver„ndert werden. Damit dies auch mit CPU-internen
       Registern m”glich wird, k”nnen in der Simulatortabelle Zeilen des
       folgenden Typs integriert werden:

             dAkku 6 CPU      ; Variable 'Akku' anlegen
             \\__/ \ \_/
              \ \   \  \
               \ \   \  Segmentbez. in dem sich der Akku befindet
                \ \   \  (durch genau 1 Leerzeichen abgetrennt !)
                 \ \   \
                  \ \   Adresse des Akku in diesem Segment
                   \ \   (durch genau 1 Leerzeichen abgetrennt !)
                    \ \
                     \ Name unter dem die Speicherstelle angesprochen
                      \          werden soll
                       \
                        Kennung: 'd'eclare Variable


       Diese Zeile erzeugt im Lokalbereich 0 fr Variablen die versteckte
       Variable 'Akku'. Der Wert der dieser Bezeichnung ('Akku') zugeordnet
       ist befindet sich im Segment 'CPU' an der Adresse 6. Mit der
       Schreibweise CPU(6) erh„lt man den selben Wert, man muá allerdings
       wissen, daá sich der Akku unter der Adresse 6 verbirgt ! Beim erzeugen
       solcher Zugriffsvariablen ist es sinnvoll sie genau so zu benennen,
       wie sie im Registerfenster bezeichnet sind, um Verwirrungen m”glichst
       zu vermeiden ! Wenn das verwendete Segment nicht ohnehin schon
       vorhanden ist, muá es mit der entsprechenden Zeile bei den
       Segmentabbildungen (siehe dort) mit eine 'f'-Zeile eingefgt werden.
       In dieser 'f'-Zeile wird die wirkliche Segmentnummer erst eingesetzt.
       Wenn 'Akku' in diesem Sinn richtig angemeldet worden ist, dann muá der
       Befehl

             SET Akku=Akku+1


       im Command-Fenster den Akku um eins erh”hen. Entsprechend kann die
       Bezeichnung 'Akku' jetzt in beliebigen Ausdrcken verwendet werden.


                                     - Seite 68 -




      M. Bausteintabelle erstellen

       Was ist ein Baustein ?
       Mit Baustein werden in diesem Zusammenhang alle Integrierten digitalen
       Bauelemente (ICs) bezeichnet, die nicht mittels Programm erst eine
       Funktion vom Programmierer bekommen mssen. Bausteine haben damit eine
       'fest verdrahtete' Funktion, die nicht ver„ndert werden kann. Dies
       bedeutet nicht, daá diese Bausteine nicht innerhalb gewisser fester
       Grenzen konfiguriert werden k”nnen ! Als Beispiel kann man hier den
       Baustein mit der Bezeichnung 8255 nennen.
       Bei diesem Baustein handelt es sich um einen Portbaustein der an sehr
       viele Mikroprozessoren angeschlossen werden kann. Dieser Baustein
       enth„lt drei 8-Bit breite Ein-Ausgabeports, die in bestimmten Grenzen
       konfiguriert werden k”nnen. Dieser Baustein ist in Form der Datei
       '8255.TBA' im Unterverzeichnis Tabellen bereits in seinen wesentlichen
       fr HSIM modelliert. Diese Tabelle kann fr die folgenden Ausfhrungen
       als anschauliches Beispiel dienen.
       Grunds„tzlich handelt es sich bei den Bausteintabellen (*.TBA) um
       Simulatortabellen (*.TAS). Es k”nnen damit bis auf einige Ausnahmen
       alle M”glichkeiten genutzt werden die auch fr Simulatortabellen zur
       Verfgung stehen.
       Als wichtigste nderung wird das Memorysegment 0 im Gegensatz zu
       Simulatortabellen nicht automatisch mit dem Auszufhrenden Programm
       geladen, da es dieses Programm nicht gibt (logisch). Grunds„tzlich
       kann aber auch ein interner 'Prozessor' bei Bausteinen verwendet
       werden (beim 8255 nicht n”tig). Das ist auch der Grund weshalb
       innerhalb von Bausteintabellen ein Befehlsbaum, zumindest als Dummy,
       angelegt werden muá. Dieser Dummy kann wie folgt aussehen.

             ; Simulatortabelle (Dummy)
             ; ----------------
             .0 1
             .1+-1


       Entsprechen mssen auch der Programmz„hler, der Cyclenz„hler und das
       Simulator-Flagregister angelegt und beim Simulator angemeldet werden.

             P1 0  ; Programmz„hler -> Simulator
             C1 1  ; Differenz-Cyclenzahl -> Simulator
             F1 2  ; Flag-Register -> Simulator


       Das entsprechende Memorysegment, hier 1, muá natrlich auch angelegt
       sein, so daá der Zugriff auf die angemeldeten Register m”glich ist.
       Source- und Variablenfenster gibt es nicht, da wie bereits erw„hnt fr
       Bausteine kein Program ben”tigt wird.
       Damit wird das Registerfenster zum zentralen Fenster von Bausteinen.
       Wichtige interne Vorg„nge, Einstellungen und I/O-Pins sollten im
       Registerfenster durch entsprechene 'v'-Zeilen angezeigt werden. Fr
       das konkrete Beispiel (8255) mssen zumindest das
       Konfigurationsregister des Bausteins und die Zust„nde der 24 Portbits
       angezeigt werden. Dies kann wie folgt aussehen.








                                     - Seite 69 -




             ; Bestimmte Registerinhalte anzeigen
             ; ----------------------------------
             v0 1 M%1lX (1@3>>5)&3
             v0 3 +---%c--+ 'O'^((1@3&10h==10h)&('I'^'O'))
             v1 1 A:%08lX Binary(1@4)
             v0 12 M%1lX (1@3>>2)&1
             v0 14 +---%c--+ 'O'^((1@3&2==2)&('I'^'O'))
             v1 12 B:%08lX Binary(1@5)
             v2 3 +-%c+ 'O'^((1@3&8==8)&('I'^'O'))
             v2 7 +-%c+ 'O'^((1@3&1==1)&('I'^'O'))
             v3 1 C:%08lX Binary(1@6)
             v3 12 Ctrl=%02lX 1@3
             v4 1 Nur_Betriebsart_0_implementiert 0


       Die Syntax entspricht exakt derjenigen die auch in Simulatortabellen
       verwendet werden kann. Die Funktion 'Binary()' dient zur Vereinfachung
       einer bin„ren Darstellung und ist auch in der Bausteintabelle
       definiert. Sie stellt, wie der Name schon vermuten l„át einen Wert in
       bin„rer Schreibweise dar, was insbesondere bei Portpins, bei denen es
       auf jedes einzelne Bit ankommt, sinnvoll ist. Beim Verwenden der
       Bausteintabelle ergibt sich ein Erscheinungsbild fr das
       Registerfenster nach folgendem Vorbild:

             _--------------2:Register--------------+
             | M0+---I--+ M0+---I--+                |
             | A:00000000 B:00000000                |
             |   +-I++-I+                           |
             | C:00000000 Ctrl=1B                   |
             | Nur_Betriebsart_0_implementiert      |
             |                                      |
             +2:8255--------------------------------+


       Zentrales und wichtigstes Element bei der Nachbildung von Bausteinen
       sind jedoch die Interfacefunktionen. Mit ihrer Hilfe werden sowohl die
       Register zur Ansteuerung des Bausteins als auch eventuelle Input- und
       Outputpins mit ihren Bezeichnungen nach 'auáen' bekannt gemacht. Fr
       die interne Funktionsweise statischer Bausteine werden praktisch
       ausschlieálich Receivefunktionen verwendet. Dabei kann eine
       Receivefunktion weitere normale Funktionen aufrufen, oder durch den
       Zugriff auf spezielle Speicherstellen Readfunktionen ausl”sen. Als
       Anschauungsbeispiele k”nnen die Bausteintabellen '8255.TBA' und
       'shift.TBA' (=Schieberegister mit Latch) herangezogen werden.


      N. Monitortabelle erstellen

       Voraussetzungen:
        - Funktionstchtige Microcontrollerplatine mit der der PC ber eine
          serielle Leitung Verbindung aufnehmen kann.
        - Monitorprogramm auf der Controllerseite (wird manchmal auch als
          Talker bezeichnet) welches das lesen und setzen beliebiger Register
          und Speicherstellen erlaubt und zus„tzlich gesteuerten Single-Step-
          Betrieb des Controllers erm”glicht.

       Grunds„tzlich ist das Erstellen einer Monitortabelle nicht an ein
       besonderes Monitorprogramm im Controller gebunden. Man erh„lt aber ein
       erheblich leistungsf„higeres Debuggingwerkzeug wenn sich der Monitor


                                     - Seite 70 -




       im Controller mit sehr kurzen Befehlen ansprechen l„át und seinerseits
       mit kurzen pr„gnanten Ergebnissen antwortet, denn die serielle
       Schnittstelle ist relativ langsam und bestimmt mit ihrer
       bertragungsleistung die Leistung des gesamten Debuggers.

       Das Erstellen einer Monitortabelle selbst kann in mehrere Teilschritte
       aufgegliedert werden:
        0.Serielle Schnittstelle zum Controller vorbereiten
        1.Speicherbereiche und Register des zu simulierenden Prozessors (oder
          Platine !) durch Schreib- und Lesefunktionen in den Simulator
          abbilden.
        2.Segmentarten des Assemblers abbilden.
        3.Steuerfunktionen fr den Programmlauf auf der Controller-Platine
          erzeugen.
        4.Inhalt des 'Register'-Fensters im Simulator festlegen

       Zwei Funktionen die beliebig in Ausdrcken verwendet werden k”nnen
       sind speziell fr den Monitorbetrieb implementiert worden.
       Es handelt sich um die Funktionen:

             SI(Arg)   "Serial-In"
             SO(Arg)   "Serial-Out"


       Diese beiden Funktionen dienen ausschlieálich der Kommunikation
       mittels der seriellen Schnittstelle, die natrlich vorher richtig
       Initialisiert werden muá (-> Abschnitt 0).
       Die Funktion SO(Arg) dient zur Ausgabe von Bytes ber die serielle
       Schnittstelle an den Controller. Das Argument wird dabei immer als ein
       Byte interpretiert. Die Funktion SI(Arg) fhrt einen Lesecyclus an der
       seriellen Schnittstelle aus und liefert einen `long int`-Wert zurck.
       Das Argument der Funktion SI hat spezielle Bedeutungen:
        < 0 : Ein negatives ganzzahliges Argument lieát abs(Arg) Zeichen von
          der seriellen Schnittstelle ein.
        = 0 : Null als Argument lieát so viele Zeichen von der seriellen
          Schnittstelle bis keine Zeichen mehr kommen (k”nnen auch Null
          Zeichen sein !)
        = 1 : Lieát eine HEX-Zahl von der seriellen Schnittstelle ein (Die
          Zahl muá aus fr HEX-Zahlen zul„ssigen ASCII-Zeichen bestehen !)
        > 1 : reserviert fr zuknftige Erweiterungen (Zahlenformate)

       ACHTUNG:
       Alle Beispiele im folgenden beziehen sich auf eine Controllerplatine
       mit dem 80C552 als CPU, dem Monitorprogramm `DL26.ASM` (auf der
       Controllerseite) und der Monitortabelle `Tabellen\80C552.TAM`. Als
       Verbindung zwischen Controllerplatine und PC dienen die Leitungen
       MASSE, TXD und RXD.

       1. Serielle Schnittstelle vorbereiten

       Fr den Monitormodus von HSIM muá grunds„tzlich immer eine serielle
       Verbindung zu einem funktionf„higem Controllerboard hardwarem„áig
       vorhanden sein.
       Um diese Verbindung Softwareseitig aufzubauen muá HSIM die
       Schnittstelle am Computer wissen, an der der Controller angeschlossen
       ist, und mit welcher Baudrate (šbertragungsgeschwindigkeit) dieser
       Controller erwartet, daá man mit ihm in Verbindung tritt.
       Diese Informationen werden HSIM in der Tabelle ??.TAM mit der
       folgenden Zeile mitgeteilt:


                                     - Seite 71 -




             S<COM> <Baudrate> [<IRQ> [<Parit„t> [<Bits> [<Stopbits>]]]]
                \       \         \        \          \        \
                 \       \         \        \          \        0,1,2
                  \       \         \        \          7,8
                   \       \         \        \
                    \       \         \        0=ohne Parit„t,1=mit Parit„t
                     \       \         \
                      \       \         3,4
                       \       \
                        \       ...38400 (Teilweise auch h”her !)
                         \
                          1=COM1,2=COM2,3=COM3,4=COM4


       Die ersten beiden Parameter in dieser Zeile sind obligatorisch, alle
       weiteren Parameter sind optional, mssen aber in der angegebenen
       Reihenfolge auftauchen.
       Standardinitialisierung fr eine Verbindung ber COM1 mit einer
       Geschwindigkeit von 9600 Baud w„re:

             S1 9600


       Im Normalfall ist mit dieser einen Zeile bereits alles erledigt.
       Lediglich fr den Fall, daá die Geschwindigkeit oder andere Parameter
       auf der Controllerseite ge„ndert werden sollen, sind noch weitere 'S'
       und 's'-Zeilen n”tig.
       Voraussetzung ist natrlich, daá das Betriebssystem des Controllers
       dies untersttzt !
       In diesem Fall kann mit einer Zeile

             s<Expression die den Controller umschaltet>


       der Controller mit entsrechenden Befehlen ber die Serielle
       Schnittstelle in den gewnschten Zustand gebracht werden. Dabei muá
       beachtet werden, daá der Controller direkt nach der Umschaltung mit
       den ge„nderten Parametern arbeitet und jetzt Beispielsweise mit einer
       anderen Geschwindigkeit sendet und empf„ngt. Die Verbindung zum
       Controller ist damit so lange unterbrochen, bis auch auf der
       Computerseite (PC) die n”tigen nderungen bei der seriellen
       Schnittstelle durch gefhrt worden sind. In der Regel muá deshalb
       direkt nach einer 's'-Zeile eine 'S'-Zeile mit den neuen, nach der
       Umstellung gltigen Parametern vorhanden sein.

             Beispiel einer Baudraten„nderung von 9600 Baud auf 38400 Baud
             aus der Tabelle DL29.TAM (->CPU:80C552 mit Betriebssystem
             DL29.ASM):













                                     - Seite 72 -




             ; Serielle Schnittstelle konfigurieren
             ; ------------------------------------
             ;COM Baudrate IRQ Parity Datenbits Stopbits
             ; 1      9600    -1   0       8        1 (=Default)
             ; zun„chst mit 9600 Baud starten
             S1 9600

             ; Wechsel auf 38400 Baud
             ; ----------------------
             sSO('F').SO(08Dh).SO(0FEh)
             S1 38400


       2. Speicherbereiche und Register abbilden

       Fr den Simulator wurden alle Segmente im PC nachgebildet - Fr den
       Monitor gengt es wenn fr alle Segmente eine Schreib- und eine
       Lesefunktion angelegt wird, die es dem Monitor erm”glicht alle Inhalte
       zu lesen und zu setzen.
       Aus diesem Grunde gibt es speziell fr den Monitorbetrieb eine
       zus„tzliche Speichersegmentdefinition:

             ;     Code-Speicher
             m@B0 SAD(IN).SO('N').SI(-1) SAD(5@0).SO('S').SO(IN)
              \\\\\____________________/ \_____________________/
               \\\\    Lesefunktion    Schreibfunktion
                \\\\_
                 \\\ Segmentnummer
                  \\\_
                   \\ (`B`=Byte/`W`=Wort/`D`=Doppelwort)
                    \\_
                     \ Kennung fr Segmentdefinition durch
                      \          Lese- und Schreibfunktion
                       \
                        Kennung fr Speichersegmentdefinition (Memory)


             Zus„tzlich ben”tigte Funktionen:
                fSAD SO('P').SO(IN>>8).SO(IN)
                fSA 5@0=IN


       Diese Zeile teilt dem Monitor mit, wie er auf den Codespeicher
       zugreifen kann. Beim Lesen muá zun„chst mittels der Funktion SAD die
       Adresse gesetzt werden an der gelesen werden soll. Dann wird der
       eigentliche Lesebefehl (Hier das Zeichen `N') gesendet. Abschlieáend
       muá dann noch das Zeichen welches den Inhalt der entsprechenden
       Speicherzelle darstellt gelesen werden. Das Ergebnis des letzten
       Teilausdruckes ist gleichzeitig der Rckmeldewert an bergeordnete
       Funktionen. Zum Lesen einer Speicherstelle wird nur ein Argument (die
       Adresse) ben”tigt und diese wird direkt als Argument bergeben.
       Beim Schreiben werden zwei Argumente ben”tigt. Da in HSIM nur einfache
       Funktionen mit einem Argument implementiert sind, muá beim Schreiben
       ein spezieller Mechanismus angewendet werden. Wenn HSIM auf eine
       Speicherstelle schreiben will, die sich im Controller befindet, so
       wird immer zun„chst die Funktion SA mit der gewnschten Adresse als
       Argument aufgerufen. Anschlieáend wird dann erst die eigentliche
       Schreibfunktion mit dem Wert als Argument aufgerufen. Im obigen
       Beispiel wird das Argument der Funktion SA an der Adresse 0 im Segment


                                     - Seite 73 -




       5 zwischengespeichert, so daá sich beim anschlieáenden Aufruf der
       eigentlichen Schreibfunktion an dieser Stelle die Adresse zu finden
       ist. Die Funktion SAD bermittelt die momentan gltige Adresse (unter
       5@0) an den Controller. Die Funktion SAD ist nicht unbedingt n”tig,
       erh”ht aber die Lesbarkeit der Monitortabelle.

       Bei Ausdrcken innerhalb des Monitors kann nun genau wie beim
       Simulator mit 0@Adresse auf den Codespeicher zugegriffen werden. Der
       Zugriff ist also identisch, unabh„ngig davon, ob sich das Segment im
       PC oder im Controller befindet.

       Genau wie beim Simulator ist das Segment mit der Nummer 0 fr das
       Codesegment reserviert. Alle anderen Nummern k”nnen beliebig vergeben
       werden.

       3. Abbilden der Segmentarten des Assemblers

       Analog wie bei der Erstellung einer Simulatortabelle !!!

       4. Steuerfunktionen fr den Programmlauf

       Insgeamt gibt es 10 Steuerfunktionen fr den Programmablauf:

             START(0)
             DOSTEP(0)
             DOCALL(0)
             STOP(0)

             DORUN(0)
             CHKBRK(0)
             DOBRK(0)

             SETBRK(Arg)
             DELBRK(Arg)
             DELALL(0)


       Diesen 10 Funktionen sind spezifische Aufgaben zugeordnet. Sie mssen
       in der Tabelle angelegt werden, obwohl sie in der Tabelle selbst
       normalerweise nicht verwendet werden. Diese Funktionen werden vom
       Monitor eigenst„ndig zu bestimmten Zeiten aufgerufen und abgearbeitet.

        START: Die Startfunktion wird direkt nach dem Download des Programms
              aufgerufen und hat die Aufgabe den Prozessor (Monitor) in den
              Single-Step-Betrieb zu bringen und den Programmz„hler an den
              Anfang des Anwenderprogramms zu setzen. Nach dem einmaligen
              ausfhren dieser Funktion muá es m”glich sein in beliebiger
              Reihenfolge Werte im Controller zu lesen, zu schreiben und die
              Funktionen DOSTEP und DOCALL aufzurufen.
        DOSTEP: Beim Aufrufen der Funktion DOSTEP soll der Prozessor jeweils
              genau einen Maschinenbefehl ausfhren.
        DOCALL: Beim Aufrufen der Funktion DOCALL soll der Prozessor jeweils
              genau einen Maschinenbefehl ausfhren. Unterprogrammaufrufe
              sollen aber in einem Schritt vollst„ndig ausgefhrt werden. Wenn
              der Monitor im Controller diese Funktion nicht zur Verfgung
              stellt, dann sollte die Funktion DOCALL den selben Inhalt wie
              die Funktion DOSTEP besitzen, da die vier Steuerfunktionen in
              jedem Fall vorhanden sein mssen.



                                     - Seite 74 -




        STOP: Diese Funktion wird beim Beenden von HSIM einmal aufgerufen und
              sollte dazu verwendet werden den Prozessor in einen definierten
              Endzustand zu versetzen. Ausgehend von diesem Endzustand sollte
              wieder ein Download mit anschlieáender Start-Funktion m”glich
              sein.
        DORUN: Die Funktion 'DORUN' sollte die Abarbeitung des
              Anwenderprogramms in Echtzeit starten. Um dem Monitor im PC
              mitzuteilen daá diese Funktion verfgbar ist, sollte der
              Returnwert ungleich Null sein. Nach dem Ausfhren der Funktion
              'DORUN' fhrt der Monitor im PC st„ndigt die Funktion 'CHKBRK'
              aus, um festzustellen ob das Anwenderprogramm noch l„uft. Das
              setzt natrlich voraus, daá der Controller neben dem
              Anwenderprogramm daá er jetzt ausfhrt auch noch die
              Monitorroutinen bedient ! Die Funktion 'DORUN' zeigt
              unterschiedliches Verhalten je nachdem wie der Monitor im
              Controller programmiert ist. Bei sehr einfachen
              Monitorprogrammen im Controller die nicht ber einen
              Hardwareinterrupt aufgerufen werden, kann diese Funktion nur
              einmal aufgerufen werden und die Verbindung zur
              Controllerplatine ist dann unterbrochen, weil das
              Monitorprogramm im Controller nicht mehr ausgefhrt wird (Jetzt
              wird das Anwenderprogramm ausgefhrt !). Es gibt in diesem Fall
              nur drei M”glichkeiten wieder aus diesem Zustand herauszukommen:
              - Anwenderprogramm ist keine Endlosschleife und springt am Ende
                    selbst„ndig wieder in den Monitor zurck.
              - Es sind Breakpoints im Monitor des Controllers m”glich und die
                    Programmausfhrung trifft auf einen solchen !
              - Beenden von HSIM und Rcksetzen der Controllerplatine
              Wenn im Controller ein Monitorprogramm l„uft, welches ber einen
              Hardwareinterrupt aktiviert wird, also auch im Hintergrund
              laufen kann, so gibt es zus„tzlich noch die M”glichkeit das
              Anwenderprogramm per Hand anzuhalten und zwar durch Bet„tigen
              der Taste F5 am PC, was letztendlich das Ausfhren der Funktion
              'DOBRK' bedeutet.
        CHKBRK: Die Funktion 'CHKBRK' muá eine 1 liefern wenn der Monitor im
              Controller im Singlestep-Betrieb ist, also das Hauptprogramm
              steht. Durch die Funktion 'DORUN' muá dies (zumindest
              kurzzeitig) ge„ndert werden k”nnen !
        DOBRK: Die Funktion 'DOBRK' sollte den Monitor nach dem Ausfhren
              einer 'DORUN' Funktion wieder in den Singlestep-Betrieb
              zurckschalten k”nnen. (-> 'DORUN'-Funktion) Wenn diese Funktion
              implementiert ist, sollte sie als Returnwert einen Wert ungleich
              Null liefern !
        SETBRK: Die Funktion 'SETBRK' wird vom Monitor mit der
              Breakpointadresse an der der Halt erfolgen soll als Argument
              aufgerufen. Der Returnwert teilt dem Monitor im PC mit ob der
              Monitor im Controller berhaupt eine eigene Breakpointverwaltung
              bereitstellt. Wenn der Monitor im Controller eine eigene
              Breakpointverwaltung besitzt, so muá entweder die Funktion
              'DELBRK' oder die Funktion 'DELALL' zus„tzlich implementiert
              werden, damit der Monitor im PC einmal gesetzte Breakpoints auch
              wieder l”schen kann. Eine eigene Breakpointverwaltung im
              Controller erh”ht die Ausfhrungsgeschwindigkeit der Programme
              im 'RUN'-Modus sehr stark. Bei geschickter Programmierung des
              Talkers im Controller wird praktisch eine Echtzeitausfhrung
              erreicht. Wenn im Controller keine eigene Breakpointverwaltung
              vorhanden ist, so wird beim Starten des Programms mit DORUN
              (=F5) keine quasi Echtzeitausfhrung erreicht wenn Breakpoints
              in HSIM gesetzt sind. Es l„uft im Hintergrund eine (m”glichst


                                     - Seite 75 -




              schnelle) Einzelschrittabarbeitung. Anders ist es wenn in HSIM
              keine Breakpoints gesetzt sind. In diesem Fall l„uát das
              Programm quasi in Echtzeit ab.
        DELBRK: Die Funktion 'DELBRK' wird vom Monitor mit der
              Breakpointadresse die gel”scht werden soll als Argument
              aufgerufen. Der Returnwert zeigt dem Monitor an ab diese
              Einzell”schung fr Breakpoints implementiert ist. Wenn die
              Einzell”schung ber 'DELBRK' nicht implementiert ist, so muá
              unbedingt 'DELALL' fr eine Gesamtl”schung aller Breakpoints
              implementiert sein.
        DELALL: Die Funktion 'DELALL' muá so implementiert sein, daá ihre
              Ausfhrung alle Breakpoints im Controller l”scht.

       5. Inhalt des 'Register'-Fensters festlegen

       Analog wie bei der Erstellung einer Simulatortabelle !!!


      O. Beispielsitzung im Monitorbetrieb

       Bei vollst„ndiger Implementierung l„uft eine Monitorsitzung wie folgt
       ab. Zun„chst wird HSIM mit der Befehlszeile

             HSIM sorce.sim /Monitor


       gestartet.
       Bei diesem Startvorgang wird zun„chst das gesamte Maschinenprogramm
       mittels Schreibzugriffe auf das Segment 0 'Downgeloaded', das heiát in
       das Mikrocontrollerboard geschrieben. Im Anschluá daran wird durch
       Aufruf der Funktion START() der Controller veranlaát an den Anfang des
       Anwenderprogrammes zu springen und dort im Singlestepmodus zu
       verharren. Mit anderen Worten es wird ein bestimmter Anfangszustand
       eingenommen. Bevor HSIM auf Benutzereingaben wartet, werden alle
       Register und Variablen, die am Bildschirm dargestellt werden sollen,
       vom Controller gelesen. Dazu geh”rt auch der Programmz„hler. Mit
       seiner Hilfe wird der zugeh”rige Sourcecode gesucht und dargestellt.
       Die Initialisierung ist jetzt abgeschlossen und HSIM wartet nun auf
       Benutzereingaben.
       Wenn man jetzt durch Drcken der Taste F8 einen Einzelschritt
       ausfhren will, so reagiert HSIM darauf mit dem ausfhren der Funktion
       DOSTEP(). Danach werden alle darzustellenden Werte neu gelesen und der
       Programmz„hler (Sourcefenster) aktualisiert.
       Beim Drcken der Taste F10 l„uft intern genau das gleiche ab, nur daá
       jetzt nicht die Funktion DOSTEP() sondern DOCALL() aufgerufen. Wenn
       man die schnelle Abarbeitung des Programms durch Drcken der Taste F5
       startet, so gibt es unterschiedliche Reaktionen von HSIM. Je nachdem
       ob im Controller eine eigene Breakpointverwaltung realisiert ist oder
       nicht. Im einen Fall wird die Funktion DORUN() ausgefhrt und im
       anderen wiederholt die Funktion DOSTEP(), so wie es im folgenden
       Diagramm veranschaulicht ist.










                                     - Seite 76 -




             Breakpointverwaltung
             im Controller ?  Ja           st„ndiges Aktualisieren der
             F5 ------------> DORUN() ---> gewnschten Fenster und
              \                      /     gegebenenfalls berprfen ob
               \Nein            Nein/      der Controller auf einen
                \                  /       Breakpoint gestoáen ist.
                 \Ist ein Breakpt./
                  \ vorhanden ?  /   Ja     St„ndig DOSTEP() Funktionen
                   -----------------------> ausfhren und dabei die
                                            gewnschten Fenster
                                            aktualisieren


       Wenn im Controller keine Breakpointverwaltung realisiert ist, so ist
       man mit dem st„ndigen Ausfhren von DOSTEP() Funktionen sehr weit von
       einer Echtzeitabarbeitung des Programms entfernt. Um auch ohne
       Breakpointverwaltung im Controller eine nahezu Echtzeitverarbeitung
       starten zu k”nnen, wird die Funktion DORUN() auch dann ausgefhrt wenn
       kein Breakpoint definiert ist.
       Wenn der Runmodus durch st„ndiges Ausfhren von DOSTEP() Funktionen
       realisiert  wird, so wird beim wiederholten Drcken der Taste F5 das
       Ausfhren dieser Funktion einfach eingestellt und die Ausfhrung ist
       damit gestoppt.
       Ein durch Ausfhren der DORUN() Funktion gestarteter Runmodus wird
       durch Ausfhren der DOBRK() Funktion beendet.
       Am Ende jeder Monitorsitzung wird zum Abschluá die Funktion STOP()
       ausgefhrt.


      P. Historie des Programms

       Version 0.9:
        - Abholen der Ausdrcke im Watch-Fenster erst nach dem Starten des
          Programms
        - Segmentabbildungen jetzt ber normale Funktionen

       Version 1.0:
        - Overlays fr einzelne Prozessoren
        - Stimulus-File implementiert
        - Andere Bildschirmaufl”sungen (EGA/VGA)
        - Status-Editor eingebaut
        - Interfacem”glichkeiten eingebaut (Multiprozessorsimulationen sind
          jetzt erst sinnvoll !)

       Version 1.1:
        - Umschalten der Baudrate in der Tabelle (Monitor)
        - Vereinfachter Zugriff auf CPU-interne Variablen und Register durch
          'd'-Zeile in der Tabelle
        - Nach Hinweis Fehler bei Paritaet und Uebertrag beseitigt
        - Scriptfiles (=Tastaturmakros) implementiert
        - Bausteine jetzt m”glich
        - Datfiles erzeugen mit Ausdruck
        - Lokale Tabellenerweiterungen bei Simulator und Monitor








                                     - Seite 77 -








     V.  Nachwort

       Trotz meiner Bemhungen strukturiert und systematisch zu programmieren
       sind Fehler ab einer gewissen Komplexit„t des Problems und des
       Programms nicht mehr zu vermeiden. So ist anzunehmen, daá auch in HASM
       und HSIM noch einige Fehler schlummern.

       Wenn bei der Benutzung Fehler auftauchen, die auf den Assembler oder
       den Simulator zurckzufhren sind, dann lassen Sie es mich bitte
       wissen !
       Z”gern Sie nicht, mich auf Fehler hinzuweisen. Sp„tere Benutzer des
       Programms werden es Ihnen danken.

       Es w„re allerdings sch”n, wenn Sie mir die Fehlersuche erleichern
       wrden, indem Sie mir:

          - den Fehler m”glichst umfassend beschreiben !
          - ein Testfile mitgeben bei dem der Fehler auftritt
          - die benutzte Version von HASM angeben und
          - m”glichst die verwendete(n) Tabelle(n) mitgeben.

       Ich werde dann mein m”glichstes tun um dem Fehler auf die Schliche zu
       kommen und ihn zu beseitigen.

       Ansonsten wnsche ich Ihnen viel Spaá bei der Benutzung von HASM/HSIM
































                                     - Seite 78 -